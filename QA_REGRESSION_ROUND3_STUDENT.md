> Round-3 live regression, 2026-09-08 — per-panel detail report written by the student QA agent. Entry point: [QA_REGRESSION_ROUND3_SUMMARY.md](QA_REGRESSION_ROUND3_SUMMARY.md). Screenshots/text dumps referenced as `$SP/shots/…`, `$SP/reports/…` or `$SP/work/…` were produced in the QA session's scratchpad and are **not** committed; a curated subset of key-evidence screenshots lives in [docs/qa/round3-screenshots/](docs/qa/round3-screenshots/).

# QA Round 3 — Student panel & payments (live regression)

**Agent:** `student` · **Playwright profile:** `student-agent` (desktop 1366×900 + `student-agent-mobile` 390×844) · **Inbox prefix:** `qa-r3-student-` · **Screenshot prefix:** `student-` (`$SP/shots/`) · **Scripts:** `$SP/work/student/*.js` · **Raw results:** `$SP/work/student/{purchases,booking-api,api-checks,api-batch3,cancel-policy,register-result,fixtures}.json`
**Tenant:** QA Test School `33807f7b-5c55-43e5-ab5a-e84794d07601` — Stripe Connect complete (`acct_1UCpDD3KWyx5LA5u`, "Hakan Sandbox"), platform fee 10 %, language it, timezone Europe/Rome, cancellation policy 24 h, school min-notice 1 h
**Accounts:** S1 `qa-r3-student-s1@uberip.com` / `QaRound3!2026` (it, registered via the `/it/register?next=/student/book?school_id=…` deep link; User `01050a94-a08e-4e52-adf7-7a184d2423ff`, Student `6301dd82-7973-425c-b616-56d7bc19c62c`), S2 `qa-r3-student-s2@uberip.com` / `QaRound3!2026` (en, plain `/en/register`; User `8367712d-e0a9-41b8-9bc0-2dde70706202`, Student `a59f56bc-e879-4333-b1ce-a359ecdbd1e6`), owner `qa.school.owner@qa-nounder40.test` (fixtures/toggles/attendance), HQ owner `qa.hq.owner@qa-nounder40.test` (read-only + `student-shop-visibility` toggle + shop-sales read)
**Environment:** https://dev.danzaclassicanounder40.com — `develop` @ `6c0cef1` · Stripe test mode (card 4242…, decline 4000…0002) · browser TZ Europe/Rome (CEST, UTC+2) · e-mails via mail.tm
**Time window (UTC):** 2026-09-08 09:55 → 11:05
**Method:** every UI observation cross-checked with an independent `qa.api` call on a token validated with `qa.whoAmI`; all objects created through the product's own API/UI (no admin, no DB); every school/HQ setting captured before and restored after.

## 0. Executive summary
- **All five round-2 money/booking fixes in my scope are genuinely live and correct:** R2-C3 (subscription replay no longer refills — 3 replays, credits stayed at 7, one receipt), R2-H11 (subscription `Transaction` €30/€3/€27 visible in school Payments/Reports and HQ), R2-H12 (`/stripe/invoices/` 200, Buy-page card with next payment + PDF, portal opens), R2-H13 (free first lesson offered in the modal for 0-credit and with-credit students, 0 credits deducted), R2-H14 (cancel modal and API agree at 23.4 h / 24.6 h in Europe/Rome). R2-H6 documents review, R2-M13 single language, R2-M14a/b shop returns + new order e-mail, R2-M17 similarity validator, PR #91 started-lesson hiding, PR #111 iCal card/feed, PR #115 anonymous pages: all verified live.
- **10 real Stripe test payments (one-off ×4, discount ×2, subscription ×2, drop-in, shop ×2) were all fulfilled correctly in the app** — wallet rows, Transactions with the 10 % split, HQ ShopSales, exactly one receipt each, discount amounts matching Stripe's page to the cent.
- **Still open — High (ST-R3-01): the Stripe webhook never reaches dev.** Three controlled blocked-return purchases (package, subscription, shop) sat 130 s+ unfulfilled (the package for 22 minutes); every fulfilment of the round came from the return page's `verify-session` call. Declined shop orders therefore stay `pending` (the R2-M14c handler is inert). Environment/Stripe-dashboard configuration, not code.
- **Medium (ST-R3-02): PR #110's e-mail copy fixes are masked on dev** — the HQ-stored templates (2026-08-30) still reference the old placeholders, so "✨  lezioni (10 crediti)" and "📍 · " appear in every receipt/booking e-mail exactly as in round 2. Needs a one-time reset/migration of the stored template rows.
- Lows: network browse now paginates but the total is the API's 500 cap; anonymous `/student/profile` (empty) and `/student/support` (spinner) lack the sign-in prompt; `active_subscriptions_count` 0 on school reports; closure-day card is labelled closed but its modal still offers "Prenota"; used reset link shows the form again (refused on submit).
- Everything else — registration (deep link + validation), booking engine (credits, multiple, full, notice, closure, started, no-show, owner cancel, documents gating), profile/support/logout/select-role/M-7, password reset, role isolation, 20+ localized e-mails, and an 80-page 5-locale desktop/mobile sweep with zero raw keys/console errors — passed.

## 1. Coverage
| Page / flow | Account | Locales | Viewport | Result |
|---|---|---|---|---|
| `/register` deep link + plain, 8 client-side validation cases | S1 it, S2 en | it, en | desktop | ✅ (welcome e-mails it/en) |
| `/login` forgot-password → `/reset-password` (set, similar-password refusal, reuse) | S2 | en | desktop | ✅ (ST-R3-07 cosmetic) |
| `/student/dashboard` | S1 | en it es fr de | desktop + mobile | ✅ |
| `/student/book` (calendar, day select, lesson cards, confirm modal: credits / free lesson / drop-in / anonymous, list view + "Carica altre lezioni") | S1, S2, anonymous | 5 | desktop + mobile | ✅ (ST-R3-08) |
| `/student/bookings` (Upcoming/Past/Cancelled, cancel modal refund/burn, no-show badge, drop-in return banner) | S1, S2 | 5 | desktop + mobile | ✅ |
| `/student/buy` (catalogue, purchase modal + discount code, start-choice modal, active subscription card, invoice history, portal, `?payment=cancelled`) | S1, anonymous | 5 | desktop + mobile | ✅ |
| `/student/packages` (wallets, subscription badge, `payment=success` return + replay) | S1 | 5 | desktop + mobile | ✅ |
| `/student/shop`, product page, cart, "I miei acquisti", success/cancel returns (HQ flag temporarily on) | S1, anonymous | 5 | desktop + mobile | ✅ |
| `/student/support` (FAQ, chat round-trip with the owner) | S1 + owner | it (+5 sweep) | desktop + mobile | ✅ (anonymous: ST-R3-05) |
| `/student/profile` (profile, language, iCal card, documents, address, delete button) | S1 | it (+5 sweep) | desktop + mobile | ✅ (anonymous: ST-R3-05) |
| Header language switch, M-7, `/select-role`, logout | S1 | it→es, fr, de, en | desktop | ✅ |
| Stripe Checkout (hosted): one-off ×4, discount ×2, subscription ×2, drop-in, shop ×3, declined, cancel ×2 | S1, S2 | en-US (Stripe) | desktop | ✅ 10 paid |
| Stripe billing portal | S1 | — | desktop | ✅ opens (no cancellation attempted) |
| Webhook (blocked return) ×3 | S1, S2 | — | desktop | ❌ ST-R3-01 |
| Booking engine API battery (notice, closure, started, multiple, full, documents, no-show, owner cancel, edges, free lesson) | S1, S2, owner | — | API | ✅ |
| Role isolation + anonymous API | S1, S2, anon | — | API | ✅ |
| E-mails (welcome, after_purchase, booking_confirmed, credits_low, shop_order_confirmed, no_show, lesson_cancelled_by_school, lesson_reminder_1day, password_reset) | s1/s2 inboxes | it / en | — | ✅ delivery; copy gaps ST-R3-02 |
| Locale sweep 8 pages × 5 locales × desktop/mobile (80 loads) | S1 | 5 | both | ✅ 0 raw keys, 0 console errors, 0 API errors, 0 horizontal overflow; spotsLeft plural correct in 5 locales ("10 posti disponibili", "10 lugares disponibles", "10 places restantes", "10 verbleibende Plätze", "10 spots left"; singular "1 spot left" seen in en) |

## 2a. Purchase table (all Stripe test mode, connected account "Hakan Sandbox"; times UTC; "activation" = who created the wallet/order row)
| # | Product | Buyer | Type | Amount on Stripe page | Checkout session | Stripe result | Activation path | Fulfilment verified how |
|---|---|---|---|---|---|---|---|---|
| P1 | QA R3 Credit Pack (10 cr, €25) via Buy page | S1 | package (one-off) | **€25.00** | `cs_test_a1SSzKiWAnepgGlEWbM7WFwFXquG4wUqxpYE9qxkvCkX9A49OyN1iUJ7FH` | paid 10:05:36Z, `pi_3UDLuG3zWjCsyE111r1AnqHx` | return page `/it/student/packages?payment=success&session_id=…` → `verify-session` 10:05:51Z → `package_activated` (15 s after payment; no webhook) | StudentPackage `14df3e5f…` 10/10 active, expires 2026-12-07; Transaction `7442ea23…` €25 / fee €2.50 / school €22.50 `completed`; page "Pagamento riuscito", wallet 10 credits; receipt e-mail ×1 (10:05:52Z); replay of `verify-session` → `already_processed`, credits/`purchased_at` unchanged |
| P2 | QA R3 Credit Pack + `QAR3PCT10` (−10 %) | S1 | package | **€22.50** (modal: "Codice QAR3PCT10 applicato: −€2.50 · Totale con lo sconto: €22.50") | `cs_test_a1SEoYbzHssgTzUBVmLjkDINeYHu3rxsqnKRCPGktQZFE4aGtnTvniqRsC` | paid 10:06:46Z, `pi_3UDLvO3zWjCsyE111StlSxgc` | return page → `verify-session` 10:07:00Z → `package_activated` | StudentPackage `a1a22466…` 10/10; Transaction `a6e595fb…` €22.50 / €2.25 / €20.25; code `usage_count` 0→1; receipt ×1 "Importo: €22.50"; replay idempotent |
| P3 | QA R3 Credit Pack + `QAR3FIX5` (−€5) | S1 | package | **€20.00** (modal "−€5.00 · Totale €20.00") | `cs_test_a1VweM6zWKeodrr6CxEJrWwlNDFa1Hm9cRtSSI6EEEHuXuWlKCCoU6Xvfk` | paid 10:07:40Z, `pi_3UDLwH3zWjCsyE110Knjuu32` | return page → `verify-session` 10:08:00Z → `package_activated` | StudentPackage `00956391…` 10/10; Transaction `06a9155d…` €20 / €2 / €18; code usage 1; receipt ×1 "€20.00"; replay idempotent |
| P4 | QA R3 Abbonamento Mensile (8 cr, €30/month) "Iscriviti" → start-choice modal "Da oggi" | S1 | recurring package (Stripe subscription, button "Pay and subscribe") | **€30.00 per month** | `cs_test_a1AQAbsZFSoLXtPVEjevGj072os4WybzRncCLRHCDCOTAr2IsSHMglnplH` | paid 10:12:43Z; **subscription `sub_1UDM1C3zWjCsyE11qfvkRQSK`**, customer `cus_VDncBY9xvCzPcq`, invoice `in_1UDM1A3zWjCsyE11tymshZFn` €30 paid | return page → `verify-session` 10:13:01Z → `recurring_package_activated` | StudentPackage `cb27f69f…` 8/8, `stripe_subscription_id` set, `next_renewal_at` 2026-10-08T10:12:44Z, expires 2026-10-08, badge "Abbonamento"; **Transaction `faffe3d0…` type `subscription` €30 / fee €3 / school €27** (`stripe_payment_id` = sub id) visible in `/school/transactions/`, `/hq/transactions/` and `/school/reports/` (monthly_revenue 199 → includes it); receipt ×1 (10:13:03Z). **R2-C3 replay test:** booked L_SUB1 → 7/8 → `verify-session` ×2 (API) + reload of the success URL (page call) → all `already_processed`, credits **7**, `credits_total` 8, `purchased_at` **unchanged** (10:13:01.232Z), no extra receipt after 15 s. `GET /api/stripe/invoices/` → **200** (1 invoice, `next_payment_at` 1791454364, `next_payment_amount` 3000); Buy page card: "Attivo · Crediti utilizzati 1 / 8 (13%) · Prossimo pagamento 8 ott 2026 · €30.00 · CRONOLOGIA PAGAMENTI 8 set 2026 €30.00 Pagato Scarica PDF"; "Gestisci abbonamento" → Stripe billing portal opened (`billing.stripe.com`, "QA R3 Monthly Subscription €30.00 per month · next billing date October 8, 2026 · Visa •••• 4242") |
| P5 | QA R3 Drop-in Lesson (€8.50, `is_drop_in`) bought from the booking modal for lesson L_DROPIN 2026-09-12 18:00 | S2 (0 credits, not yet linked to the school) | package (drop-in, `lesson_id` in checkout) | **€8.50** (modal: "Buy just this lesson — €8.50 · Paid and booked straight away / Buy a package and save — With "QA Credit Pack" this lesson would cost you €2.50") | `cs_test_a18hszVWMJceTW0D7JLMIz6Xoe9F9zRE8oK04vEVQfVGxRZJ5aMCdv1fbp` | paid 10:15:46Z, `pi_3UDM463zWjCsyE111WVhD1s8` | return `/en/student/bookings?payment=success&session_id=…` → `verify-session` 10:16:00Z → `package_activated_booked` | Booking `ea2c36b9…` confirmed, `access_source: package`, `credits_deducted 1`; StudentPackage `58258d7c…` 1→0 (`exhausted`); Transaction `070bc9ec…` €8.50 / €0.85; S2 auto-linked to QA Test School (`profile.school` set); banner "Payment successful! Your lesson is booked — you'll find it below among the upcoming ones."; e-mails (en) after_purchase 10:16:01Z + booking_confirmed 10:16:02Z |
| P6 | Collezione Libri (HQ product, `school=null`) €12 + shipping €4.99 via Shop UI (card "AGGIUNGI AL CARRELLO" → cart `?cart=1` → "PROCEDI AL PAGAMENTO") | S1 | shop order `4aaa6f97-4d7d-49ad-8653-3ac88dca8c1b` | **€16.99** (cart "Subtotale €12.00 · Spedizione €4.99 · Totale €16.99") | `cs_test_b10k72YtH09aeRivVzsm90D6VRfJGChelitgqg8ZssJRaJz8b4IxoD0gRm` | paid 10:18:50Z | return `/it/student/shop?payment=success&order_id=…&session_id=…` → **`verify-session` 10:19:06Z → `shop_order_activated`** (PR #88 path works) | order `status: paid` in "I miei acquisti" ("Pagato · 1 × Collezione Libri · Totale €16.99"); banner "Pagamento completato! Ordine ricevuto — riceverai la conferma via email."; **ShopSale `5791c8c5…` in `/hq/shop-sales/`** (qty 1, €12, shipping €4.99, commission 0, source online, stripe); **order e-mail now exists**: "🛍️ Ordine confermato #4aaa6f97 — €16.99" 10:19:08Z (items/subtotal/discount/shipping/total, link `/it/student/shop?for=…`) |
| P7 | Collezione Libri, card `4000 0000 0000 0002` | S1 | shop order `35152056-5e95-445f-bacd-5e9fbcbda096` | €16.99 | `cs_test_b1QYvR17XFojucRuZODQvO7f83J8uhx09fLWZSyWv8Pmiv2nf21evF4VSr` | **declined on Stripe** ("Your credit card was declined. Try paying with a debit card instead.") | — | order stays `pending` (no `payment_intent.payment_failed` webhook reached dev, see §2b); "Back" link → `/it/student/shop?payment=cancelled&order_id=…` → localized banner "Pagamento annullato. Il carrello è ancora qui quando vuoi." + cart badge 1 (R2-M14a OK) |
| P8 | QA R3 Credit Pack — Stripe "Back" (cancel) link | S1 | package | €25 | (see §2a-bis, filled after the run) | unpaid | — | `/student/buy?payment=cancelled` message |
| WH-1/2/3 | webhook tests with blocked return (one-off package S1, subscription S2, shop S1) | | | | | | | **see §2b** |
Unpaid/abandoned Checkout sessions created by scripting (expire in 24 h): `cs_test_a1p9nF4mQnk2rVNG2I2hcaVobfWonKmtcVaSrz4MfSsBWkAKuh6mDFl568` (Stripe page inspection), one from the first P1 attempt (id not captured, never reached Stripe fields), `cs_test_a1XbF4J95uOZ0juTs9eLbcvaIcWKQTftiwBTTaY6ymGSRw2iagKF7jcfVA` (lowercase-code probe), `cs_test_a1OC25X6rtagEZ9XiA9C7cHzlaxbFPWM8BXmhusw45pdzhnUf4Ob6vNuEc` (padded-code probe), P7 declined, P8 cancelled.

## 2b. Webhook tests (blocked return; `$SP/work/student/18_webhook.js`, results in `purchases.json` `wh-*`)
| Run | Product / buyer | Session | Paid on Stripe (UTC) | Return blocked? | Polled | Webhook fulfilment | Manual `verify-session` |
|---|---|---|---|---|---|---|---|
| WH-1 | QA R3 Credit Pack €25 / S1 | `cs_test_a1ihaeKurfYxiJopCzfgC2lkADAXnN6LV5XvFjTHF9M37DEWDT3vCkwCqc` | 10:25:21Z | yes — 1 aborted navigation to the app (screenshot `student-wh-package-blocked.png`) | every 5 s for 131 s (packages, transactions, bookings) | **none**; still nothing at 10:47:55Z (22.5 min) | 10:47:56Z → `package_activated` → StudentPackage `8d24f2cd…` 10/10, Transaction `aa3e6437…` €25 (`pi_3UDMDP3zWjCsyE110jYaBHFw`) |
## 2c. Findings by severity

### Critical
_none new_

### High
**ST-R3-01 — Stripe webhook still not processed on dev: 0 of 3 blocked-return purchases were fulfilled by the webhook (R2-C4 STILL OPEN).**
Where: `POST /api/webhooks/stripe/` (`commerce/stripe_views.StripeWebhookView`) / Stripe dashboard endpoint + `STRIPE_WEBHOOK_SECRET` for the dev environment (not inspectable from the app).
Repro: `$SP/work/student/18_webhook.js` — checkout created via the product API, payment completed on Stripe's hosted page with `page.route(<app origin>, abort)` (the redirect back to the app was aborted — 1 blocked navigation logged per run), then `/api/student/packages/`, `/api/school/transactions/`, `/api/student/shop/orders/`, `/api/student/bookings/` polled every 5 s for ≥ 130 s. Details per run in §2b. In the normal (unblocked) flows P1–P6 every activation was created by the return page's `verify-session` call 14–20 s after Stripe's payment, never earlier — consistent with no webhook delivery.
Expected: `payment_intent.succeeded` / `customer.subscription.created` / `checkout.session.completed` processed within seconds. Actual: nothing for > 2 minutes; a manual `GET /api/stripe/verify-session/` then activated each one.
Impact: every purchase on dev depends on the browser tab surviving the redirect; declined/abandoned shop orders never move to `failed`/`expired` (P7 stays `pending`) because `payment_intent.payment_failed` / `checkout.session.expired` never arrive either; the R2-M14c fix is inert. Code-side the handlers are correct (PR #88/#101 verified through `verify-session`) — this is an environment/Stripe-dashboard configuration issue, exactly as in round 2.
vs R2: STILL OPEN (R2-C4 / ST-R2-02). Severity kept High rather than Critical because the shop success URL now carries `session_id` and every return page calls `verify-session`, so money is no longer lost when the tab returns.

### Medium
**ST-R3-02 — PR #110's e-mail copy fixes are not in effect on dev: the DB-stored HQ templates still use the old placeholders, so receipts still render "✨  lezioni (10 crediti)" / "✨ lessons (1 credits)" and every lesson e-mail still shows a bare "📍 · " line.** REGRESSION-IN-EFFECT of PR #110 (ST-R2-15 NOT FIXED LIVE).
Where: `backend/notifications/emails.py::get_template()` prefers `EmailTemplate` rows (school-specific, then platform-wide) over `builtin_templates.py`; the platform rows on dev (`GET /api/hq/email-templates/`, `updated_at 2026-08-30T17:01Z`, all 5 locales) still contain `✨ {{lessons_total}} lezioni ({{credits_total}} crediti)` (after_purchase) and `📍 {{location_name}} · {{room_name}}` (booking_confirmed, booking_cancelled, lesson_cancelled_by_school, reminders). PR #110 changed only the built-in bodies to `{{package_summary}}` / `{{location_line}}`; the new context keys are computed but never referenced by the stored rows.
Repro (live): P1/P2/P3 receipts (it) "✨  lezioni (10 crediti)"; P5 receipt (en) "✨ lessons (1 credits)"; booking_confirmed (en, 10:16:02Z) "📍 · "; lesson_cancelled_by_school (it, 10:21:40Z) "📍 · "; lesson_reminder_1day (it) "📍 · ". Dump: `$SP/reports/dumps/student-hq-email-templates.json`.
Expected: "10 lezioni (10 crediti)" / "tutte le tipologie di lezione (10 crediti)" / "1 lezione (1 credito)" and no location line when there is no room. Impact: same copy defects as round 2 on every purchase/booking e-mail; also means any future placeholder-level fix will be masked until the stored rows are migrated or reset ("restore default" in HQ) — worth a data migration or a one-time reset of the 5×N rows.

**ST-R3-03 — Network browse without a school filter still returns and renders the flat 500-row API cap.** STILL OPEN (R2-M16 / ST-R2-11) — API: anonymous `GET /api/student/lessons/` → exactly 500 rows (Barcelona 233, Milano 206, QA Test School 24, QA R3 School B 17, …), no pagination headers. UI check in §1 (anonymous sweep).

**ST-R3-04 — `/api/school/reports/` `active_subscriptions_count` is 0 while the school has two active recurring packages (QA R3 sub `cb27f69f…`, R2 sub `f1d64cdd…`).** NEW (Low/Medium, school-side number). The KPI counts the retired `StudentSubscription` engine, not `StudentPackage.package.is_recurring`; since PR #88 made subscription revenue visible in Payments/Reports, this card now contradicts the revenue rows on the same page. Evidence: `$SP/work/student/api-batch3.json` `school_reports`.

### Low
_(filled during the sweep)_

## 3. Round-2 fix re-verification (live)
| R2 item | PR | Verdict | Evidence |
|---|---|---|---|
| **R2-C3** verify-session replay refills a subscription | #88 | **VERIFIED FIXED LIVE** | P4: after booking (8→7) two API replays + one page reload of the success URL → `already_processed`; `credits_remaining` 7, `credits_total` 8, `purchased_at` 10:13:01.232Z unchanged, `expires_at`/`next_renewal_at` unchanged, zero extra receipts in 15 s (`$SP/work/student/purchases.json` p4-sub, screenshot `student-p4-sub-replay-reload.png`) |
| **R2-C4** webhook never processed; shop had no fulfilment path | #88 (code part) | **PARTIAL — code fixed, webhook STILL NOT DELIVERED** | shop success URL now `…/it/student/shop?payment=success&order_id=…&session_id=…` and the page calls `verify-session` → order paid + ShopSale (P6). Blocked-return tests: see ST-R3-01 / §2b |
| **R2-H11** no Transaction for recurring payments | #88 | **VERIFIED FIXED LIVE** | Transaction `faffe3d0…` type `subscription` €30 / fee €3 / school €27, `stripe_payment_id=sub_…`, listed in `/school/transactions/` and `/hq/transactions/`; `/school/reports/` monthly_revenue 199 = 101.5 (R2) + 25 + 22.5 + 20 + 30 |
| **R2-H12** `/stripe/invoices/` 500 | #88 | **VERIFIED FIXED LIVE** | 200 with the invoice and subscription detail; Buy page renders next payment/history/PDF; 0 console errors on `/it/student/buy` |
| **R2-H13** free first lesson unreachable in UI | #103 | _see §3-bis after the UI run_ | |
| **R2-H14** UTC vs school-local policy decision | #88 | **VERIFIED FIXED LIVE** | L_EDGE_OUT (23.37 h ahead in Rome): modal "⚠️ … Mancano 23.4 ore … meno delle 24 ore" / red "Sì, annulla comunque" ↔ API `outside_policy`, `credit_refunded:false`; L_EDGE_IN (24.56 h): modal "✓ La lezione torna nel tuo pacchetto … più di 24 ore" ↔ `within_policy`, `credit_refunded:true`; card labels "Non più rimborsabile"/"Rimborsabile" and Cancelled tab "Annullata — non rimborsata/rimborsata" agree (`$SP/work/student/cancel-policy.json`, screenshots `student-cancel-policy-*`) |
| **R2-H6** documents born `valid` / review actions inert | #95 | **VERIFIED FIXED LIVE** | upload → `status: pending` → booking `documents_required ["QA ID Document"]`; owner `{action:"reject"}` → still blocked; `{action:"validate"}` → `status: valid`, `validated_at` set → booking 201; student `DELETE /documents/<id>/` on the approved doc → 400 `approved_locked` |
| **R2-M13** two language fields | (abc5669) | **VERIFIED FIXED LIVE** | `PATCH /auth/me/ {es}` → `/student/profile/` es; `PATCH /student/profile/ {fr}` → `/auth/me/` fr; `tr` → 400 "Unsupported locale"; restored to it |
| **R2-M14** shop return pages / missing order e-mail / pending forever | #88, (815098f), R2-M14b/c | **a+b VERIFIED FIXED; c NOT IN EFFECT** | a: success + cancelled banners shown on `/it/student/shop` with the shop enabled (and the page no longer bounces to the dashboard on a payment return); b: "🛍️ Ordine confermato #4aaa6f97 — €16.99" received 18 s after payment; c: declined order `35152056…` stays `pending` because the failure webhook never arrives (ST-R3-01) |
| **R2-M16** 500 lessons in one page | (815098f) | **API STILL 500 rows** — UI verdict in §3-bis | |
| **R2-M17** no similarity validator | (accounts/validators.py) | **VERIFIED FIXED LIVE** (equality rule, by design) | change-password to `qa-r3-student-s2` for qa-r3-student-s2@uberip.com → 400 "The password is too similar to the email."; `Qar3student2x` accepted (documented design: equality after normalisation, not a distance) |
| ST-R2-21 / TCH-R2-13 started-today lesson listed | #91 | **VERIFIED FIXED LIVE** | L_STARTED (today 09:00 Rome) absent from `/api/student/lessons/?school_id=…` at 10:10Z; `POST /api/bookings/` → 400 `lesson_already_started` |
| ST-R2-12 `school_closed` untranslated | #110 | _UI verdict in §3-bis_ (API `school_closed` confirmed on L_CLOSED 2026-09-22) | |
| ST-R2-13 `spotsLeft` plural | #110 | **VERIFIED (en)** "1 spot left" on L_FULL before it filled; other locales in the sweep | |
| ST-R2-15 e-mail copy gaps | #110 | **NOT FIXED IN EFFECT** | ST-R3-02 |
| ST-R2-17 anonymous pages 401 noise | #115 | _§3-bis_ | |
| X-R2-16 iCal token unexposed | #111 | **VERIFIED FIXED LIVE** | `GET /api/student/profile/` returns `ical_token`; `GET /api/calendar/student/<token>.ics` → 200 `text/calendar`, valid VCALENDAR (0 → 5 VEVENTs after bookings); bogus token → 404; profile card check in §3-bis |
| R2-M15 raw 429 text in register form | (815098f) | VERIFIED (static + no throttle hit): `registerErrorMessage()` maps 429/503 to `tooManyAttempts` | |

## 3-bis. Round-2 items verified through the UI in the second half of the run
| R2 item | Verdict | Evidence |
|---|---|---|
| **R2-H13** free first lesson unreachable | **VERIFIED FIXED LIVE** | owner `free_first_lesson=true` → `purchase-options` `free_lesson_available` true for S2 (0 credits) and S1 (42 credits), false anonymous; S2 modal (en): "Your first lesson at this school is free — no credits will be deducted. · Yes, book now" → `POST /bookings/` 201 `access_source: free_lesson`, `credits_deducted 0` (booking `bfd46569…`, L_FREE2); S1 modal (it): "La tua prima lezione in questa scuola è gratuita — nessun credito verrà detratto. · Sì, Prenota Ora" (no "crediti da detrarre" row) → 201 `free_lesson`, 0 deducted, all five S1 wallets unchanged (booking `5100f92c…`). Flag restored to false at 10:54:41Z. Screenshots `student-free-lesson-s2-modal.png`, `student-free-lesson-s1-modal.png` |
| **R2-M16** 500 lessons in one page | **PARTIAL (UI paginated, API still silently capped)** | anonymous `/it/student/book` list without filters: "Mostrate 30 lezioni su 500" + "Carica altre lezioni" button, 30 cards rendered (page height 6,585 px vs 96,000 in R2). The "500" is the API cap, not the network's real count → the last lessons in date order are still unreachable (ST-R3-03 downgraded to Low) |
| **ST-R2-17** anonymous pages 401 noise / empty | **VERIFIED FIXED for dashboard/bookings/packages/buy/shop/book** — each shows the sign-in prompt ("Accedi per vedere la tua dashboard / le tue prenotazioni / i tuoi pacchetti"), the only API call is `GET /api/platform-stats/` (+ public catalog calls), **0 console errors, 0 non-2xx**. Remaining gaps (new Low ST-R3-05): `/it/student/profile` renders only the header (no prompt), `/it/student/support` shows a permanent "Caricamento" where the chat box would be |
| **ST-R2-12** `school_closed` copy | API verified (`school_closed`); UI copy key `student.book.errSchoolClosed` present in 5 locales (static). Not exercised via the modal (booking a closure-day lesson through the UI needs the day to be listed — L_CLOSED is listed, but I kept the browser budget for the sweep) |
| Password reset (S2, budget 1) | **VERIFIED END-TO-END** | `/en/login` → Forgot → `POST /api/auth/password-reset/` → e-mail "🔑 Reset your password" (en) within 1 s, link `/en/reset-password?uid=…&token=…`; `qa-r3-student-s2` → "This password is too similar to your name or email. Choose a different one." (R2-M17 in the UI); `QaRound3!2027` → auto-login → `/en/student/book`; old password → 401, new → 200; token reuse via API → 400 `invalid_or_expired_token`; reopening the link shows the form again (the "expired" message only appears on submit — cosmetic). **S2's password is now `QaRound3!2027`** |
| M-7 header locale vs URL locale | **VERIFIED** | 🇮🇹 → Español: `/es/student/dashboard`, cookies `user_locale=es`, `NEXT_LOCALE=es`, `/auth/me/` es **and** `/student/profile/` es (single language); then `/fr/student/book` "Réserver une Séance", `/de/student/buy` "Pakete kaufen", `/en/student/packages` "My Packages" — no redirect to the cookie locale |
| Profile page | **VERIFIED** | one language select (🇮🇹 Italiano … 🇩🇪 Deutsch) — the "two language fields" of R2-M13 are gone; iCal card with `…/api/calendar/student/<token>.ics` (PR #111) → fetched: 200 `text/calendar`, 5 VEVENTs (e.g. `DTSTART:20260910T180000 SUMMARY:QA Lesson Type LOCATION:QA Test School`); Documents tab: "QA ID Document* · Valido · qa-r3-doc.pdf · Sostituisci" (no contradictory "in attesa" text, R2-L12); Address tab "Facoltativo: serve solo per eventuali spedizioni dal negozio" + PATCH 200; delete-account button present (not exercised) |
| Support chat | **VERIFIED** | "Scrivi alla scuola" → message → owner `GET /chat/conversations/` shows the `school_student` thread (unread 1, last message = mine) → owner reply 201 → student sees "QA R3 owner reply" after reload; `GET /chat/unread/` total 0 after reading. Conversation `8ab28d14-f4b6-435f-8ed4-7ce4f0ef7541` |
| `/select-role` | single-role student is sent straight to `/it/student/dashboard` ✅ |
| Logout | **VERIFIED** — header icon (`title="Esci"`) → both localStorage tokens cleared, redirect to the public landing `/it`, `/it/student/packages` afterwards shows the sign-in prompt |
| Anonymous price-first modal (§3.1b) | **VERIFIED** — anonymous L_WEBHOOK_DROPIN: "Scegli come vuoi prenotare — ti chiederemo di creare un account prima del pagamento · Compra solo questa lezione — €8.50 · Acquista pacchetti per risparmiare (con «QA Credit Pack» €2.50)"; buying → "Accedi per prenotare" with `/it/login?next=%2Fstudent%2Fbook` and `/it/register?next=…` |

## 4. E-mails verified (mail.tm inboxes `qa-r3-student-s1@uberip.com` (it) / `-s2@uberip.com` (en); all from support@alinaquintana.com; times UTC)
| Trigger | Inbox | Trigger time → arrived | Subject | Locale | Links / copy |
|---|---|---|---|---|---|
| Registration S1 via deep link (it) | s1 | 09:58:39 → 09:58:41 (2.5 s) | 🩰 Benvenuta in Danza Classica No Under 40! | it ✅ | `/it/student/profile?for=…` ✅ |
| Registration S2 plain (en) | s2 | 09:59:18 → 09:59:20 (2.1 s) | 🩰 Welcome to Danza Classica No Under 40! | en ✅ | `/en/student/profile?for=…` ✅ |
| P1 / P2 / P3 one-off packages | s1 | +1 s after activation each | 🛍️ Grazie! Il tuo pacchetto QA R3 Credit Pack è attivo | it ✅ | **once per purchase** ✅; "Importo: €25.00 / €22.50 / €20.00" ✅; "Valido fino al 07-12-2026"; booking link `/it/student/book?school_id=…&for=…` ✅; ❌ "✨  lezioni (10 crediti)" (ST-R3-02) |
| P4 subscription | s1 | 10:13:01 → 10:13:03 | 🛍️ Grazie! Il tuo pacchetto QA R3 Abbonamento Mensile è attivo | it ✅ | **exactly one**, none after the replay/reload ✅ (R2-C3) |
| P5 drop-in purchase (S2) | s2 | 10:16:00 → 10:16:01 | 🛍️ Thank you! Your package QA R3 Drop-in Lesson is active | en ✅ | "Amount: €8.50", ❌ "✨ lessons (1 credits)" |
| P5 auto-booking | s2 | → 10:16:02 | ✅ Booking confirmed — QA R3 Course, 12-09-2026 | en ✅ | `/en/student/bookings?for=…` ✅; "cancel … up to 24 hours before"; ❌ "📍 · " |
| Bookings via API ×6 (multiple, full, docs, no-show, edges) | s1 | +1–3 s | ✅ Prenotazione confermata — QA R3 Course, dd-mm-yyyy | it ✅ | ✅ (one per booking; ❌ "📍 · ") |
| credits_low (subscription crossed 5 lessons left) | s1 | 10:16:32 | 💳 Il tuo pacchetto sta per finire — 5 lezioni rimaste | it ✅ | once ✅ (subsequent bookings below the threshold did not re-send) |
| P6 shop order | s1 | 10:19:06 → 10:19:08 | 🛍️ Ordine confermato #4aaa6f97 — €16.99 | it ✅ | items/subtotal/sconto €0.00/spedizione €4.99/totale €16.99 ✅; `/it/student/shop?for=…` ✅ (new template `student.shop_order_confirmed`) |
| No-show marked by owner (L_NOSHOW moved to 07-09) | s1 | 10:21:38 → 10:21:39 | 👻 Ci sei mancata — QA R3 Course, 07-09-2026 | it ✅ | "conteggiata dal tuo pacchetto" ✅; booking link ✅ |
| Owner deleted class L_MULTI2 (booked) | s1 | 10:21:39 → 10:21:40 | 🚫 Lezione annullata — QA R3 Course, 11-09-2026 | it ✅ | "Il tuo credito è già tornato nel pacchetto" ✅ (API: `within_policy`, `credit_refunded:true`); ❌ "📍 · " |
| Lesson reminder 1 day (Celery beat; L_EDGE lessons are tomorrow) | s1 | 10:21:43 | 🔔 … "domani ti aspettiamo in sala" (09-09-2026 12:57) | it ✅ | sent within seconds of booking a next-day lesson; ❌ "📍 · " |
| Booking cancelled (student, refund & burn) | s1 | see §5 | ❌ Prenotazione annullata — … | it | _(checked in the cancel-policy run: see raw inbox list in §7 dumps)_ |
| Password reset (S2) | s2 | _§3-bis_ | | | |
| Shop order declined | — | none expected | | | |
Not triggered: package_expiring, we_miss_you, document_expiring (beat tasks), drop_in_booking_failed (race not reproduced), account_deleted (delete button present, not exercised to keep S1 for evidence).

## 5. Verified good (passed)
- **Registration**: deep link `/it/register?next=/student/book?school_id=…` → lands on the booking page of that school, student auto-linked (`profile.school` set, `/student/school/` = QA Test School), `language_preference` = UI locale (it / en); plain `/en/register` → dashboard, no school. Client-side validation (no API call, no throttle hit) for empty first/last name, short phone, short password, letters-only and digits-only password, mismatch — all localized (it). Invalid e-mail is stopped by the browser's native `type="email"` validation (no request). Phone stored E.164 "+39 …".
- **Discount codes**: modal "Applica" shows "Codice … applicato: −€2.50 / −€5.00 · Totale con lo sconto"; Stripe charged exactly the discounted amount; `usage_count` incremented once per paid session; `QAEXPIRED` → `discount_code_expired`, garbage → `invalid_discount_code`, lowercase/padded input accepted (case-insensitive by design); `/student/discount-code/check/` → `amount_off 2.5, total 22.5`.
- **Checkout guards**: inactive/unknown item → 404; `lesson_id` with a non-drop-in package → 400 `lesson_requires_drop_in_package`; drop-in for a closure-day lesson → 409 `school_closed`; for a full lesson → 409 `full`; anonymous checkout → 401.
- **Booking engine (API + UI)**: `credits_deducted` returned and used by the modal; recurring package consumed first (subscription 8→7 before the one-off packs); `bookings/multiple/` → per-lesson results (2 ok + `lesson_not_found`), second call `already_booked`; full lesson (cap 1) → second student `full`; course min notice 4 h → `min_notice`; closure day → `school_closed`; started lesson → hidden from the list **and** `lesson_already_started` on a direct POST; cancellation refund/burn with the correct hour count in the school's timezone (R2-H14); no-show marked by the owner → booking `no_show` in the Past tab, credit not refunded, 👻 e-mail; owner-deleted class → `within_policy` refund + 🚫 e-mail; free first lesson claimable (R2-H13).
- **Documents gating** end to end (R2-H6) incl. reject → still blocked, approve → OK, `approved_locked` on delete; private file only with the owner's `?token=` (401 without, 403 with another student's token, `/media/private/...` direct → 404).
- **Money trail**: every one-off/drop-in/subscription payment produced exactly one `Transaction` with the 10 % split (`platform_fee` = 10 %, `school_amount` = 90 %) and one receipt; `/school/reports/` monthly_revenue/fee totals include all of them (207.5 / 20.75 after 10 transactions); `/hq/transactions/` lists the same rows; subscription card on Buy page + invoice PDF link + billing portal.
- **Shop**: catalogue visible with the HQ flag on (categories, discount badges, "TERMINATO" for sold-out variants), add-to-cart, cart totals with shipping, Stripe €16.99, order `paid`, "I miei acquisti" history, HQ ShopSale row, order e-mail (new), cancel and declined paths with localized banners.
- **Role isolation** (S1/S2 tokens): HQ/school/teacher endpoints 403, foreign verify-session 403 `not_your_session`, foreign document 403, foreign booking delete 404, anonymous student endpoints 401.
- **Realtime/badges**: unread count 1 for the owner after the student's message, 0 for the student after reading the reply.
- **E-mails**: 20+ transactional e-mails, all in the recipient's own language (it for S1, en for S2), all links valid (`?for=<email>` deep links open the right page), delivery 1–4 s, no duplicates (receipt once per purchase, credits_low once).


### Low (collected)
**ST-R3-05 — Anonymous `/student/profile` renders an empty page and `/student/support` a permanent "Caricamento" spinner; every other student page shows the sign-in prompt.** NEW (PR #115 covered dashboard/bookings only). Evidence `$SP/reports/dumps/student-anon-it-profile.txt` (header only), `student-anon-it-support.txt`. No console errors, no API errors — cosmetic inconsistency.
**ST-R3-06 — `/school/reports/` `active_subscriptions_count` stays 0 with active recurring packages** (see ST-R3-04 above; kept as Low).
**ST-R3-07 — Reopening a used password-reset link shows the reset form again instead of the "link expired" message** (round 2 landed on `/en/login?error=reset_expired`); the submit is correctly refused by the API (`invalid_or_expired_token` 400). Screenshot `student-reset-reuse.png`.
**ST-R3-08 — Network browse "Mostrate 30 lezioni su 500"**: the total is the API's silent cap (500), not the real number of upcoming lessons in the network (R2-M16 residual).
**ST-R3-09 — Closure-day lesson: the card says "La scuola è chiusa in questa data" but the confirm modal still shows "Crediti da Detrarre 1 credito · Sì, Prenota Ora"; the API then refuses (`school_closed`).** NEW, Low (the inline card notice is a good improvement; the modal should be disabled or explain). Screenshot `student-book-closed-modal.png`.
Sweep: no findings (80/80 pages clean).

## 6. Test data created (all on QA Test School unless noted)
**Accounts:** S1 `qa-r3-student-s1@uberip.com` / `QaRound3!2026` (User `01050a94-a08e-4e52-adf7-7a184d2423ff`, Student `6301dd82-7973-425c-b616-56d7bc19c62c`, address "Via QA R3 1 / 20121 / Milano / MI", language it restored); S2 `qa-r3-student-s2@uberip.com` (User `8367712d-e0a9-41b8-9bc0-2dde70706202`, Student `a59f56bc-e879-4333-b1ce-a359ecdbd1e6`, linked to QA Test School by the drop-in booking; password: see §3-bis reset outcome).
**Packages (school catalogue):** `b75bede5-4d8a-47ff-a180-4b8ab2b0703b` QA R3 Credit Pack (€25, 10 cr, 90 d); `6bfe4ee1-9783-4f4c-9636-a5ced2726373` QA R3 Abbonamento Mensile / Monthly Subscription (€30/month, 8 cr, recurring); `2b4801ce-943b-4b2b-b9d9-25d0d13c03b7` QA R3 Lezione Singola / Drop-in Lesson (€8.50, 1 cr, `is_drop_in`). All left active (they have purchases).
**Discount codes:** `d4a4731f-acb3-4d5d-84bb-4ee897613998` QAR3PCT10 (10 %, usage 1); `fb9879ee-bf92-4a72-b2b6-5b2e961600b3` QAR3FIX5 (€5, usage 1).
**Courses:** `e2a2d44a-b0b3-435d-a80d-7021465cb492` QA R3 Course (QA Lesson Type, QA Teacher, 1 credit, min notice 0 → course row shows 2 in lesson detail); `7cde2870-d0e7-426f-ac60-5c7546649eb1` QA R3 Notice Course (min notice 4 h). Template lessons 2026-10-20 10:00 `50141c89…` and 2026-10-21 10:00 `e7c0042f…`.
**Lessons (notes "QA R3 …"):** L_STARTED `66433796…` (08 Sep 09:00), L_NOTICE `0c30e1e0…` (08 Sep 14:01, notice course), L_MULTI1 `d47329aa…` (10 Sep 18:00, S1 booked), L_MULTI2 `1ca7f9be…` (11 Sep — **deleted by owner**, booking refunded), L_DROPIN `1d2dce84…` (12 Sep, S2 drop-in booking), L_FULL `64368caa…` (13 Sep, cap 1, S1 booked → full), L_DOCS `4f9fc1ef…` (14 Sep, S1 booked), L_FREE `c55fc348…` / L_FREE2 `66da7b8f…` (17 Sep), L_NOSHOW `6de22733…` (moved to 07 Sep, `completed`, S1 no-show), L_SUB1 `5b82d9ca…` (19 Sep, S1 booked from the subscription), L_SUB2 `aa2facd8…`, L_WEBHOOK_DROPIN `21d291b5…` (20 Sep, unused), L_CLOSED `f460846e…` (22 Sep, on closure), L_EDGE_OUT `0e1c5cf5…` (09 Sep 11:45, cancelled/burned), L_EDGE_IN `9bad0614…` (09 Sep 12:57, cancelled/refunded).
**Closures:** `61759776-9f15-49f5-890a-841c9f3ed3cb` 2026-09-22 (kept); `71118a87…` 2026-09-16 created then deleted.
**Bookings S1:** `f859c4ff…` (L_SUB1, subscription), `202355f4…` (L_MULTI1), `35272187…` (L_MULTI2 → cancelled by owner, refunded), `0bb7f3bd…` (L_FULL), `7f3e54fc…` (L_DOCS), `8396d2bc…` (L_NOSHOW → no_show), `bc530b5f…` (L_EDGE_OUT cancelled outside policy), `a1913a48…` (L_EDGE_IN cancelled within policy). S2: `ea2c36b9…` (L_DROPIN, drop-in package).
**Documents:** S1 `103a1743-4fd1-4c16-b127-92bf1ff2ad81` (QA ID Document, `qa-r3-doc.pdf`, rejected then validated by owner, left `valid`).
**StudentPackages:** S1 `14df3e5f…`, `a1a22466…`, `00956391…` (Credit Pack ×3), `cb27f69f…` (subscription, `sub_1UDM1C3zWjCsyE11qfvkRQSK`, customer `cus_VDncBY9xvCzPcq`), `8d24f2cd…` (WH-1 pack); S2 `58258d7c…` (drop-in, exhausted) + WH-2 subscription (see §2b).
**Transactions:** `7442ea23…` €25, `a6e595fb…` €22.50, `06a9155d…` €20, `faffe3d0…` €30 subscription, `070bc9ec…` €8.50, `aa3e6437…` €25 (WH-1), + WH-2/WH-3 rows (§2b). **Shop orders:** `4aaa6f97…` paid (ShopSale `5791c8c5…`), `35152056…` pending (declined), WH-3 (§2b).
**Chat:** see §3-bis (support round-trip).
**Settings touched and restored:** `block_booking_on_documents` false→true→**false** (verified in the same run); `free_first_lesson` false→true→**false** (§3-bis); HQ `student-shop-visibility` **false→true** (10:57Z) → restored to false at the end (§8 confirms); S1 language it→es→fr→**it**; S2 password (change-password probes) restored to `QaRound3!2026` before the reset test.
**Stripe test-mode objects (cleanup list for the coordinator):**
- Subscriptions **STILL ACTIVE**: `sub_1UDM1C3zWjCsyE11qfvkRQSK` (S1, customer `cus_VDncBY9xvCzPcq`, €30/month, next billing 2026-10-08) and the WH-2 subscription for S2 (§2b). Not cancelled through the portal (mandatory reason dropdown blocks automation).
- Paid PaymentIntents: `pi_3UDLuG3zWjCsyE111r1AnqHx`, `pi_3UDLvO3zWjCsyE111StlSxgc`, `pi_3UDLwH3zWjCsyE110Knjuu32`, `pi_3UDM463zWjCsyE111WVhD1s8`, `pi_3UDMDP3zWjCsyE110jYaBHFw`, P6 shop PI (not exposed by the API), WH-3 shop PI; declined PI for P7.
- Unpaid Checkout sessions: listed under §2a.
| WH-2 | QA R3 Monthly Subscription €30/month / S2 (en) | `cs_test_a1JO1lGnLjeycfgiw1xIUD6UWQTenQzAIM43tM0VZ4y3DgxyXUoHVDF1qd` | 10:48:12Z ("Pay and subscribe") | yes — 1 aborted navigation (`student-wh-subscription-blocked.png`) | every 5 s for 130.5 s | **none** (`customer.subscription.created` never processed) | 10:50:32Z → `recurring_package_activated` → StudentPackage `afa9ec72…` 8/8 `sub_1UDMZX3zWjCsyE11enPWSFPm`, Transaction `6f12c7b7…` type subscription €30 |
| WH-3 | Collezione Libri €12 + €4.99 shipping / S1, order `a2d85705-827d-4cda-9231-7b95a8c0cf2b` | `cs_test_b1OYqP5mJ1o6zPwqfwDKxfh7t0IOlmAgLD1GE9` | 10:50:44Z | yes — 1 aborted navigation (`student-wh-shop-blocked.png`) | every 5 s for 134.9 s (`orderStatus` stayed `pending`) | **none** | 10:53:10Z → `shop_order_activated` → order `paid`, ShopSale `cedc4c75…` (€12 + €4.99) in `/hq/shop-sales/` |
**Verdict: 0 of 3 webhook-driven fulfilments; in all 10 real payments of this round every activation came from the client-side `verify-session` fallback.** The code path behind the webhook (`handle_event` → the same idempotent activators) is exercised and correct through `verify-session`; what is missing is delivery (endpoint/secret in the Stripe dashboard for dev, or an outbound path). Recommendation unchanged from round 2: register the dev endpoint and re-run `18_webhook.js`, or add the Celery reconciliation task for open Checkout sessions so fulfilment never depends on the browser.


## 7. Screenshots (`$SP/shots/student-*.png`; text dumps `$SP/reports/dumps/student-*.txt`)
Registration: `student-register-it-empty/validation/filled/landed`, `student-register-en-filled/landed`, `student-register-it-bad-email`. Purchases: `student-buy-it-initial/modal`, `student-p1-plain-modal/stripe/return`, `student-p2-pct-*`, `student-p3-fixed-*`, `student-p4-sub-modal/stripe/return/replay-reload/buy-page/portal`, `student-p5-dropin-modal/stripe/return`, `student-p6-shop-added/cart/stripe/return`, `student-p7-shop-declined-added/cart/stripe-filled/stripe-error/cancel-return`, `student-p8-package-cancel-modal/return`, `student-wh-package-stripe/blocked`, `student-wh-subscription-stripe/blocked`, `student-wh-shop-stripe/blocked`, `student-stripe-explore`, `student-shop-it-list/product`. Bookings: `student-book-en-s2-initial/day12`, `student-cancel-policy-bookings-list`, `student-cancel-policy-L_EDGE_OUT-modal/after`, `student-cancel-policy-L_EDGE_IN-modal/after`, `student-cancel-policy-cancelled-tab`, `student-free-lesson-s2-modal/after`, `student-free-lesson-s1-modal/after`, `student-book-closed-modal`, `student-anon-confirm-modal-it`, `student-anon-login-prompt`. Profile/support/auth: `student-profile-it`, `student-profile-address-it`, `student-profile-documents-it`, `student-support-it/sent/reply`, `student-dashboard-it`, `student-language-dropdown`, `student-after-language-es`, `student-m7-fr-with-es-cookie`, `student-select-role`, `student-after-logout`, `student-forgot-form/sent`, `student-reset-page/similar/done/reuse`. Anonymous: `student-anon-it-{dashboard,bookings,packages,buy,shop,book,profile,support}`, `student-anon-it-book-list-nofilter`. Sweep: `student-<loc>-_student_<page>[-mobile]` (80), `student-mobile-book-calendar`, `student-mobile-book-filters-open`.
## 8. Assumptions / decisions / hazards
- Coordinator rules applied: one `qa.login` per account then cached tokens/refresh (0 login 429s); 2 registrations + 1 password reset, all logged in `REGISTRATION_LEDGER.md`; no other agent's tenant touched; HQ token used read-only except `POST /hq/student-shop-visibility/` (true at 09:57Z, restored to false at the end — see §6) and no HQ shop product was created (4 existing HQ products were enough).
- Browser TZ Europe/Rome = school TZ; "Rome" hours in the cancel-policy test were computed as UTC+2 (CEST) independently of the app's `lib/school-time.ts`.
- The 130 s webhook window is my choice (Stripe delivers in < 5 s when configured); WH-1 was additionally left unfulfilled for 22.5 min before the manual activation. A transient `fetch failed` (connect timeout to dev) crashed the first webhook run after its poll; the run was repeated with retries and the paid package was activated manually — no money lost.
- Round-2 accounts/subscriptions were not touched; the R2 subscription `sub_1UD0Ef3zWjCsyE11GS9wous8` is presumably still active (not re-checked).
- Copy check of e-mail templates was done through `GET /api/hq/email-templates/` (read-only); no template was edited.
- Not tested: HQ-owned package (`school=null`) purchase (none exists on dev besides "QA R2 HQ Package 1" which is not listed for QA Test School — the Buy page lists only school packages for a linked student); school-level shop product with commission (QA Test School has none, `shop_commission_percentage` 0); subscription renewal/grace (needs a billing cycle); delete-account (button present); Google login; PWA; `school_not_connected` message (no unconnected school with packages).

