# School Panel — QA Round 4 Report (agent: school)

> **Artifacts note (coordinator):** paths written as `$SP/...` refer to the QA session scratchpad (Playwright profiles, scripts, ~2,000 screenshots and text dumps) which is not committed; the key-evidence screenshots named in this report are in [docs/qa/round4-screenshots/](docs/qa/round4-screenshots/). Round-4 environment: dev `develop @ f4155c9`, 2026-09-11.

**Agent:** School-panel QA agent (`school`) · **Playwright profiles:** `school` (desktop 1366×900), `school-mobile` (390×844), private sub-profiles `school-staff`, `school-admin`, `school-sweep` (all owned by this agent) · **Inbox prefix:** `qa-r4-school-*@uberip.com`
**Tenant:** QA R4 School B (`75e2de47-02db-4e36-a2bd-4e18b7a501ca`, slug `qa-r4-school-b`, `language=it`, fee 10 %, timezone Europe/Rome, `School.owner` = user `7694be95-7ca5-48b0-84d6-119ab519dc33`, founder membership id `44`)
**Accounts (password `QaRound4!2026`):** `qa-r4-school-b@uberip.com` (owner / founder), `qa-r4-school-admin@uberip.com` (admin, membership `47`, user `a87b7cfd-0665-43cd-a6f5-afdadd0f7b83`; temporarily promoted to owner twice for the R3-H4 probes, demoted back), `qa-r4-school-staff@uberip.com` (staff, membership `48`, user `0b3be338-0ca5-4a15-8aad-ec4bfb14cf13`), `qa-r4-school-t1@uberip.com` (teacher `e65014b8-df3b-4035-9fe3-455bf854cf7e`, onboarded, user `ccfda330-55c0-4f88-be19-39fcb6ad7354`), `qa-r4-school-t2@uberip.com` (teacher `713f1a0b-dc31-4a4e-9183-541a7d0a0eac`, invite pending), students `qa-r4-school-s1@uberip.com` (it; user `e78a54ef-f9ee-4aeb-9361-d324fd145aaa`, Student `a5e1aeb4-5cc6-451f-afeb-d25dc1e4ebf6`, SchoolStudent `0a55b278-269e-4a5a-bc32-94a871250067`) and `qa-r4-school-s2@uberip.com` (de; user `9f345332-7af1-4a8a-b641-ab36a14aa0a2`, Student `bc5b3c13-49c9-4fae-b785-79795459bb3e`, SchoolStudent `e8f1d045-8dab-4af7-be7a-2ebd7c85fca6`; registered 19:37:43 UTC and deleted by the school at 19:41:25 — see §6). Read-only use of the cached R3 School C owner token for ONE `GET /school/locations/` (to obtain a real foreign location id for the R2-H3 probe).
**Time window:** 2026-09-11 18:29 – 19:42 UTC · **Deployed commit:** develop @ `f4155c9` (CI run 34468639545)
**Method:** every flow driven through the real UI (Playwright / Chromium, own persistent profiles, desktop + mobile) at least once and cross-checked with the same REST calls the UI makes (`qa.api`, every token decoded with `qa.whoAmI` first); every surprising UI observation re-verified with an independent API call; e-mails verified end to end on mail.tm (subject, per-recipient locale, delivery seconds, links). Sweeps: 24 owner pages × 5 locales desktop (120 pages), 20 staff pages × it/de, 20 admin pages (it), 25 owner pages on mobile (390 px) — every dump scanned with `qa.scanText`, every page measured for horizontal overflow. Scripts `$SP/work/school/00–29_*.js`; full request log `$SP/work/school/apilog.jsonl` (≈ 420 logged calls); sweep result `$SP/work/school/sweep-owner-desktop.json`; state/ids `$SP/work/school/state.json`.

---

## 0. Executive summary

**Every round-3 School fix is live and works through the real UI and API, with two partial exceptions.** R3-H4 (founder protection) is closed in both directions — a co-owner gets `403 cannot_remove_founder` / `cannot_change_founder_role` (the UI shows the localized "L'account del fondatore non può essere rimosso da qui…"), and a promoted co-owner can be demoted back to admin. R3-H2's gate refuses `staff` (`403 section_forbidden payments`), R3-M5 finally makes a `valid` document with a past `expires_at` block booking (API `400 documents_required` and the student's booking modal), R3-M6's summary POST now *updates* (200, same id, note/method kept, `paid_at` set), R3-M7's Reports page no longer drags the phone sideways, R3-M9/M11/M12/M16/L5/L10 and the R2 items (H3/H5/H6/H7/H8/M5–M10/M13) all re-verified live. The i18n sweep closed most of R3-L13 (calendar modal, year view, "N lezioni", CSV headers, `email_taken`, package placeholders, closure/founder errors) and 120 owner pages in five locales show 0 raw keys, 0 console errors, 0 desktop overflow.

**Partially closed:** R3-M6 — the viewset path `PATCH /api/school/compensation-payments/<id>/` (API-only; the UI posts to the summary endpoint) still stores `status:"weird2"` and `amount:-3` and leaves `paid_at: null` on a `paid` row, and the summary POST accepts an arbitrary `status` string (which nulls `paid_at`); `amount: 0` is accepted on purpose for `pending` rows and refused for `paid` (`amount_required`). R3-L13 — the Team page `confirm()` ("Are you sure you want to remove this team member?") and the compensation-plan placeholders ("e.g. Standard, Senior, Guest", "e.g. 5", "e.g. 9 (or leave empty)") are still English in every locale.

**New this round:** two mobile pages overflow once they have data — Course detail (444 px) and Manual credits (407 px) at 390 px (SCH-R4-01, Medium). Lows: a JSON-array request body still 500s on four school endpoints (R3-M8 leftover), `POST /school/team/resend/` on an *active* member e-mails a fresh setup-account link that re-runs `complete-invite` (200) on the live account, the credits-low warning is per package so a student holding two packages receives two "sta per finire" mails within 20 s (the first while 10.5 credits sit in her other package), single-select filters on the Manual-credits page, a case-insensitive duplicate attendance status, `max_capacity: 0` accepted on a class, a file-less student document that the school can mark "valid", and a consumed `/setup-account` link that still renders the form.

Counts: **0 Critical · 0 High · 1 Medium · 8 Low** (plus observations).

---

## 1. Coverage table

| Page / flow | owner | admin | staff | Locales (en/it/es/fr/de desktop) | Mobile (it) | Result |
|---|---|---|---|---|---|---|
| Dashboard | ✅ real KPIs (1 studente, 3 lezioni/sett., 1 abbonamento) | ✅ same | ✅ (didactic sidebar only) | 5/5 clean | ✅ | ok |
| Team (invite admin+staff via UI, onboard from e-mail, edit modal, remove, resend, founder guards, co-owner scenario) | ✅ UI + API | ✅ (owner row locked, no "owner" option) | 403 `section_forbidden` + redirect | 5/5 | ✅ | R3-H4 fixed; **SCH-R4-04** (English `confirm()`), **SCH-R4-05** (resend on active member) |
| Teachers (+invite ×2 via UI, onboard t1 from e-mail, edit, e-mail collision, grants, resend, two-click remove) | ✅ UI + API | — | GET 200, writes 403, redirect | 5/5 | ✅ | ok; `email_taken` localized (R3-L13 fixed) |
| Locations + rooms (capacity 0, cost −10, foreign location, unknown id) | ✅ UI + API | — | GET 200, POST 403 | 5/5 | ✅ | R2-H3 fixed |
| Closures (single/range/partial, end<start UI + API, partial without time) | ✅ UI + API | — | POST 403 | 5/5 | ✅ | R3-L10 fixed |
| Compensation plans + simulate + payments (upsert, paid_at, bounds) | ✅ UI + API | ✅ | 403 + redirect | 5/5 | ✅ | R3-M6 **PARTIAL** (SCH-R4-02); **SCH-R4-04** placeholders |
| Courses (wizard with closure banner, plain create + probes, detail, edit, class page move, bulk, delete with/without bookings) | ✅ | — | allowed (courses perm) | 5/5 | ❌ detail overflow (**SCH-R4-01**) | R3-M9 fixed |
| Lessons (add like-the-course, add on closure, weekly spanning closure, move, two-click cancel) | ✅ UI + API | — | ✅ | 5/5 | ✅ | R2-H7/M7 fixed; seats released (R3-L10) |
| Calendar (day/week/month/year, add-class modal, closure day) | ✅ | ✅ | ✅ | 5/5 (year initials localized) | ✅ | R3-L13 fixed for calendar |
| Packages (one-off, recurring, drop-in, custom half, UI create/duplicate/edit/deactivate/delete, validation) + discount codes + `/subscriptions` redirect | ✅ UI + API | ✅ | GET 200, POST 403 | 5/5 | ✅ | ok |
| Attendance statuses (create, duplicate, second default, delete default → promotion) | ✅ UI + API | — | 403 + redirect | 5/5 | ✅ | R3-L10 fixed; **SCH-R4-08a** case dup |
| Settings (rules save cycle, language, closures) | ✅ UI | — | 403 + redirect | 5/5 | ✅ | R2-H5 fixed; no timezone control in the UI (§8) |
| Profile (identity PATCH, website scheme, validation, HQ-only fields, staff guard) | ✅ UI + API | ✅ | GET 200, identity PATCH 403 | 5/5 | ✅ | R3-M11/L10 fixed |
| Students (self-registration via deep link, list/search, edit modal, sheet, free-lesson flag, XLS/PDF export, delete by owner, staff delete 403) | ✅ UI + API | — | delete 403 | 5/5 | ✅ | R2-H8 fixed |
| Credits (API grants + bounds, modal grant 0.5, list/filters) | ✅ UI + API | — | allowed (manualCredits perm) | 5/5 | ❌ overflow (**SCH-R4-01**) | **SCH-R4-07** single-select filters |
| Documents (types CRUD, student upload, pending→reject/validate/expiry/flag via API and UI sheet, gate incl. expiry, `approved_locked`) | ✅ UI + API | — | validate allowed (documents perm) | 5/5 | ✅ | R2-H6 + R3-M5 fixed; **SCH-R4-08c** file-less |
| Attendance (school-side enrol on past lesson, marking via UI and API, future refusal, invalid status, credits untouched, no-show mail in the student's language) | ✅ UI + API | — | allowed | 5/5 | ✅ | R2-M5/M10/M13 fixed |
| Inbox (student + teacher threads, unread badge, "Visualizza Profilo Studente", quick reply insert + send, priority/status selects) | ✅ UI + API | ✅ | ✅ | 5/5 | ✅ | R3-M16 fixed |
| Payments (Stripe onboarding gate per sub-role, page buttons) | ✅ "Continua l'Onboarding" | ✅ same buttons | 403 + redirect | 5/5 | ✅ | R3-H2 gate fixed |
| Reports (5 tabs, 4 localized CSV exports, KPIs) | ✅ | ✅ | 403 + redirect | 5/5 | ✅ no overflow (5 tabs) | R3-M7 fixed; CSV headers localized |
| Register / setup-account (public) | ✅ via e-mailed links (it) | ✅ | ✅ | it (+ de for s2) | ✅ register on mobile | ok; **SCH-R4-09** consumed link |

Section-guard matrix (staff token, 34 GETs): 200 on profile, memberships, permissions, locations, rooms, closures, courses, courses-overview, lesson-types, lessons, lessons-feed, attendance-statuses, teachers, compensation-plans, students, documents, document-types, packages, credits/grants, quick-replies, attendance, chat, unread, onboard/status; `403 section_forbidden` on team, compensation-payments, compensation-summary, subscriptions, discount-codes, transactions, reports (+detailed/packages/student-classes). Staff writes: locations/rooms/closures/compensation-plans/attendance-statuses/packages/discount-codes/teachers (POST+PATCH)/team/compensation-summary/profile-identity/stripe-onboard → 403; students delete → 403 (R2-H8); credits grant 201, document-types 201, quick-replies 201, courses-create 200, classes 200, course PATCH 200 (all within the `manualCredits`/`documents`/`inbox`/`courses`/`lessons` permissions of the SchoolRole matrix). Cross-role: teacher/student → `/api/school/*` 403 `not_a_school_member` (except `/school/memberships/` → 200 `[]`); staff → `/hq/*` 403 "HQ only.", `/teacher/*` / `/student/*` 403; anonymous → 401 everywhere. Staff UI: locations, teachers, teachers/invite, compensation, packages, subscriptions, payments, reports, settings, settings/statuses, team redirect to `/school/dashboard` in it and de; sidebar shows Dashboard · Calendario · Corsi · Lezioni · Allieve · Documenti · Messaggi · Crediti manuali · Profilo.

---

## 2. Findings

### Medium

#### SCH-R4-01 — Course detail and Manual-credits pages overflow horizontally on a phone once they have data — NEW (R3-M7 class)
- **Where:** `frontend/src/app/[locale]/school/courses/[id]/page.tsx` (lessons list + filter strip), `frontend/src/app/[locale]/school/credits/page.tsx` (grants table).
- **Repro (19:04 UTC, owner, mobile profile 390×844, `it`):** `/it/school/courses/85fb7b9e-fe9f-42ab-be44-0e1c7d6d0093` → `document.documentElement.scrollWidth 444 > clientWidth 390`; `/it/school/credits` (4 grants listed) → `407 > 390`. The other 23 mobile pages, including Reports and all five of its tabs, measure `ok` (`$SP/work/school/16_mobile_owner.js` output).
- **Expected:** no page-level horizontal scroll; wide tables scroll inside their own container (the rule PR #148 applied to Reports). **Actual:** the body scrolls sideways on both pages.
- **Impact:** the two pages a school opens most on the go (a course's lesson list, the manual-credit ledger) are clipped on a phone.
- **Evidence:** `school-mobile-courses_85fb7b9e_fe9f_42ab_be44_0e1c7d6d0093-it.png`, `school-mobile-credits-it.png`.
- **vs R3:** NEW (R3's mobile sweep had no course-detail page and an empty credits page).

### Low

#### SCH-R4-02 — Compensation payment: the viewset PATCH path and the `status` field are unvalidated — R3-M6 PARTIAL
- **Where:** `backend/teachers/views.py` `TeacherCompensationPaymentViewSet` (ModelSerializer, no `validate_amount`/`validate_status`); `SchoolCompensationPaymentsSummaryView.post` accepts any `status` string (model `status = CharField(max_length=20, default="pending")`, no choices).
- **Repro (18:36–18:38 UTC, owner):** `POST /api/school/compensation-summary/ {teacher_id:T1, month:"2026-09", amount:44, status:"pending", payment_method:"bank_transfer", note:"QA R4 settembre"}` → 201 `87c94daf…`; `PATCH /api/school/compensation-payments/87c94daf…/ {"status":"paid"}` → 200 but `paid_at: null`; second summary POST `{amount:50, status:"paid"}` → **200 same id, note and method kept, `paid_at` set** (the R3-M6 fix works on this path). Then: summary POST `{…status:"weird"}` → **200 `status:"weird"`, `paid_at:null`** (a settled record un-paid by a typo); `PATCH /compensation-payments/87c94daf…/ {"status":"weird2"}` → **200**; `PATCH … {"amount":-3}` → **200 stored −3** (the summary POST correctly answers `400 amount_negative` for the same value); `PATCH … {"status":"paid"}` → 200 with `paid_at: null`. Summary POST `{amount:0,status:"pending"}` → 201 (by design per commit `e0c251e`), `{amount:0,status:"paid"}` → `400 amount_required`.
- **Expected:** one validation for both write paths (status ∈ {pending, paid}, amount ≥ 0, `paid_at` set whenever the row becomes paid). **Actual:** the plain viewset stores anything; the UI is unaffected because `compensation/page.tsx:338` posts to the summary endpoint only.
- **Impact:** owner/admin-only, API-only; a malformed client can corrupt a teacher payment record silently. Rated Low for that reason (the coordinator may fold it into R3-M6 as PARTIAL).
- **Evidence:** `apilog.jsonl` 18:36:43–18:38:14. Record restored (50 / paid / `paid_at` 18:38:14Z / note / bank_transfer); the €0 row and the −3 value were removed.

#### SCH-R4-03 — A JSON-array request body still answers 500 on four school endpoints — R3-M8 leftover
- `POST /api/school/team/ [1,2]` → **500**; `POST /api/school/teachers/ [1,2]` → **500**; `POST /api/school/classes/ [1,2]` → **500**; `POST /api/school/attendance/<lesson>/ [1,2]` → **500** (the view accepts a list by design, but not a list of non-objects). The same shape is handled on `credits/grant` (400 `{"body":["Expected a JSON object."]}`), `compensation-summary`, `team` PATCH, `team/resend` (400), and a string/number/null body answers 400 everywhere (`"Expected a JSON object or array as the request body."`). No traceback leaks (plain Django 500 page). `ensure_object_body` is the existing pattern (commit `86f8fe1`).

#### SCH-R4-04 — Two R3-L13 spots are still hardcoded English in every locale — STILL OPEN (partial)
- Team page `frontend/src/app/[locale]/school/team/page.tsx:174` `confirm(\`Are you sure you want to remove this ${isPending ? 'pending invitation' : 'team member'}?\`)` and `:184` fallback `'Failed to remove'` — captured live on `/it/school/team` (owner clicking "Rimuovi" on the staff row) and again as the co-owner: dialog text `"Are you sure you want to remove this team member?"`. The i18n sweep commit `8c26c4a` did not touch this file.
- Compensation page `compensation/page.tsx:188/216/230` placeholders `"e.g. Standard, Senior, Guest"`, `"e.g. 5"`, `"e.g. 9 (or leave empty)"` — captured in it and de (`sweep-owner-desktop.json` → `comp-placeholders-it/-de`).
- Locations page still shows the backend's English text verbatim in the Italian UI: room capacity 0 → "Capacity must be at least 1.", cost −10 → "Cost cannot be negative." (`school-locations-room-cap0-it.png`, `school-locations-room-costneg-it.png`); the package form's own errors are localized ("Scegli almeno un tipo di lezione…").
- Everything else listed under R3-L13 for the school panel is fixed (see §3).

#### SCH-R4-05 — `POST /api/school/team/resend/` on an active member re-sends an invite whose link re-runs `complete-invite` on the live account — NEW
- **Repro (18:57:01 UTC):** owner `POST /api/school/team/resend/ {"id":"48"}` (48 = the *onboarded* staff member) → **200 `{"sent":true}`**; `qa-r4-school-staff@` received "✉️ Invito da QA R4 School B — Staff" at 18:57:02 with a fresh `/it/setup-account?uid&token`; `POST /api/auth/complete-invite/ {uid, token, first_name, last_name, password}` with that token → **200** (the account's password and name were re-set; same values were used, and the member still logs in).
- **Teacher variant, UI-reachable:** the Teachers page offers "Rinvia invito" on *every* teacher row (the API returns `active:true` for onboarded and pending alike — 2 buttons for T1+T2); `POST /api/school/teachers/resend/ {teacher_id: T1 (onboarded)}` → 200 `{"sent":true}` and `qa-r4-school-t1@` received a new "✉️ Invito … — insegnante" with a setup link at 19:30:23 (not followed; T1 still logs in with her password).
- **Expected:** 4xx (`already_active`) or a no-op — the Team UI hides "Rinvia" on active rows (API-only there), the Teachers UI does not. **Actual:** an "invite" e-mail ("sei stata invitata… scegliere la tua password") goes to an active colleague and its link resets her credentials. Low: only owner/admin can trigger it and the mail goes to the member's own address.

#### SCH-R4-06 — Credits-low warning is per package: a student holding two packages gets two "sta per finire" mails within 20 s, the first while 10.5 credits remain — R3-L1 follow-up
- **Repro (18:51 UTC):** s1 held a manual 10-credit grant (no expiry) and an 8-credit recurring package (expires 11/10). Bookings drained the recurring package first: at 18:51:34 "💳 Il tuo pacchetto sta per finire — 3 lezioni rimaste" ("nel tuo pacchetto QA R4 Abbonamento Mensile … ti restano 3 lezioni (scade il 11-10-2026)") while the other package still held 10.5 credits; at 18:51:44 a second "— 4 lezioni rimaste" for the manual grant ("nel tuo pacchetto con QA R4 School B ti restano 4 lezioni." — no blank clause). Each package warned exactly once across nine further bookings/cancellations (the R3-L1 fix works as designed: `credits_low_sent_at` per StudentPackage).
- **Expected (product):** one warning when the *wallet* at that school is running out, or copy that names the package and mentions the remaining wallet. **Actual:** package-level warnings that read as "you are about to run out" while the student is not.

#### SCH-R4-07 — Manual-credits page filters are single-select — Carlo's rule
- `/school/credits`: "Motivo" and "Pacchetto" are plain `<select multiple=false>` (6 and 2 options) with labels; "Allieva", "Dal", "Al", "Pagato da €" are inputs. The course-detail and lessons filters use checkbox popovers with "Seleziona tutto" (multi-select ✓). Reports "Lezioni studenti" / "Pacchetti e abbonamenti" tabs use dropdown filters (`Studente / Insegnante / Sede / Aula / Prodotto / Tipo / Stato`) whose selection mode was not verified.

#### SCH-R4-08 — Validation nits (new)
- **a.** `POST /school/attendance-statuses/ {"name":"qa r4 presente"}` → 201 next to "QA R4 Presente" (duplicate check is case-sensitive; an exact duplicate is refused). Row deleted.
- **b.** `PATCH /school/classes/<id>/ {"max_capacity": 0}` → 200 (room capacity 0 is refused since R2-M9, class capacity 0 is not).
- **c.** `POST /student/documents/ {type_ref, school, files: []}` → 201 (`eff3900c…`, no attachment) and school `PATCH /school/documents/eff3900c…/ {"action":"validate"}` → 200 `status: valid`. The booking gate correctly ignores it (`.exclude(files=[], file_url="")`, test `test_content_less_valid_document_does_not_satisfy_required_gate`), and the student cannot delete it afterwards (`approved_locked`), so an empty "valid" record stays in `GET /school/documents/` (`["eff3900c", type_id null, "valid", 0 files]`); it is invisible in the student sheet (no `type_ref`). R2-H6's "file-less record rejected" holds only client-side.
- **d.** `POST /school/packages/` etc. return backend English verbatim on some paths that the UI shows raw (e.g. `DELETE /school/packages/<with grants>/` → 400 `["Package has purchases; deactivate it instead."]`).

#### SCH-R4-09 — A consumed `/setup-account` link still renders the empty form — STILL OPEN (R3 observation)
- After the admin completed her invite (18:32 UTC), re-opening the same `/it/setup-account?uid&token` renders "Completa il tuo account… Nome Cognome Password" again; only the submit answers `400 invalid_or_expired_token` (`school-setup-account-admin-reuse-it.png`). R3-L7 fixed the reset page only.

### Observations (not rated)
- The co-owner sees "Modifica" and "Rimuovi" on the founder's row; both actions fail with the localized founder message — cosmetic (the buttons could be hidden as they are for admins).
- No e-mail is sent to the student when the school approves or rejects a document (none exists in the registry; not a bug unless specified).
- `PATCH /api/school/profile/ {}` as staff → 200 and the body still exposes `grace_period_days`, `free_trial_ends_at`, `ical_token`, `owner` (unchanged since R3).
- The school profile page prefixes `https://` to whatever is typed in "Sito web" (`normalizeWebsite`), so `javascript:alert(1)` is sent as `https://javascript:alert(1)` and accepted; the API alone rejects the bare scheme (`400 "Use a full http:// or https:// address."`), and also rejects `www.example.com` without a scheme while the UI silently adds it.
- There is no timezone control anywhere in the school UI (grep of `frontend/src/app/[locale]/school` for `timezone` → none); the field is HQ/API-only, so R3-M11's "settings page dropdown" cannot be exercised by a school.
- The invite greeting uses the first token of the single "Nome" field of the team form ("Ciao QA 🌸") — explained as a form inconsistency in commit `587cc8c`, unchanged.
- `GET /api/school/students/?search=zzz` ignores `search` (returns every student; the page filters client-side).
- `POST /school/teachers/` with an existing teacher's address in upper case → 201 `existing_account:true, email_sent:false` (idempotent re-link, no ghost).

---

## 3. Round-3 fix re-verification (School scope)

| R3 item | Commit / PR | Verdict | Live evidence (UTC 2026-09-11) |
|---|---|---|---|
| **R3-H4** co-owner removes founder / silent demotion / irreversible promotion | `8aa678b` #130 | **VERIFIED FIXED LIVE** | Founder `PATCH /school/team/ {id:47, school_sub_role:"owner"}` → 200; co-owner (whoAmI `a87b7cfd…`, `/auth/me` `school_sub_role:"owner"`) `DELETE /school/team/ {id:44}` → **403 `cannot_remove_founder`**, `PATCH {id:44, school_sub_role:"staff"}` → **403 `cannot_change_founder_role`** (also for `"admin"`), profile-only PATCH on the founder → 200; founder `GET /school/profile/` still 200; founder `PATCH {id:47, school_sub_role:"admin"}` → **200 `school_sub_role:"admin"`** (reverted, `/auth/me` confirms); founder self-`DELETE` → 403 `cannot_remove_founder`; admin on founder → 403 `forbidden` ×2, admin promotes to owner → 403 `only_owner_assigns_owner`, admin invites owner → 403, `godmode` → 400 `invalid_school_sub_role`. UI: as co-owner, "Rimuovi" on the founder row → DELETE 403 → banner "L'account del fondatore non può essere rimosso da qui. Per cambiare il titolare, scrivi a HQ." (`school-team-coowner-remove-founder-it.png`); as admin the founder row has no buttons and the invite select offers admin/staff only. |
| **R3-H2** Stripe onboarding gate (gate only) | `11fc41f` #126 | **VERIFIED FIXED LIVE** | staff (`0b3be338…`, `GET /school/transactions/` → 403 `section_forbidden payments`) `POST /api/stripe/onboard/ {}` → **403 `{"error":"section_forbidden","section":"payments"}`**; teacher token → 400 `no_active_school`; anonymous → 401. Admin → **200** `{"url":"https://connect.stripe.com/setup/e/acct_1UEZHp3q9VUHwNOx/…"}` — the fix decided the gate is `school_section_allowed(user,"payments")` and the built-in `admin` SchoolRole holds `payments` (`GET /school/permissions/`), so admin is allowed by design; the school now carries `stripe_account_id acct_1UEZHp3q9VUHwNOx` (`stripe_onboarding_complete false`). Payments page: owner and admin see "Aggiorna Stato" + "Continua l'Onboarding"; staff is redirected to the dashboard. Owner never called the endpoint. |
| **R3-M2** invite e-mail honesty (`teachers/resend`) | `e3082d7` #138 | **VERIFIED (positive path)** | `POST /school/teachers/resend/ {teacher_id:T2}` → 200 `{"sent":true}` and the mail really arrived (`qa-r4-school-t2@` 18:57:02 "✉️ Invito da QA R4 School B — insegnante", 1 s). UI: Teachers page "Rinvia invito" → 200 `sent:true` → banner "Invito inviato di nuovo a QA R4 Teacher Due." and the mail in 1 s. The template-off path was not exercised (HQ e-mail setting; outside this agent's remit). |
| **R3-M5** expired required document still books | `6be4a14` #144 | **VERIFIED FIXED LIVE** | `block_booking_on_documents=true`; document `d6e80ba2…` (`type_ref` = "QA R4 Certificato medico", required, has_expiry): pending → `POST /bookings/` 400 `documents_required ["QA R4 Certificato medico"]`; validated → 201 (`66bb8e41…`); `PATCH /school/documents/d6e80ba2…/ {"action":"expiry","expires_at":"2026-09-01T00:00:00Z"}` → 200, `GET /documents/d6e80ba2…/` `status:"valid"` → `POST /bookings/ {lesson: 28/09}` → **400 `documents_required`**; expiry `2027-06-30` → 201 (`b1b7ae29…`). Student UI (mobile, 19:23): with the past expiry, "Prenota" → modal "Conferma prenotazione … Sì, Prenota Ora" → `POST /bookings/` 400 → the page shows **"Per prenotare servono documenti validi: QA R4 Certificato medico. Caricali dal tuo profilo."** (`school-student-book-doc-error-it.png`); expiry restored to 2027-06-30. Note: my first attempt posted `document_type` instead of `type_ref` and produced an untyped document — discarded and deleted. |
| **R3-M6** compensation payment overwrite / paid_at / amount ≤ 0 | `e0c251e` #146 | **PARTIAL** | Summary POST: second POST → 200 same id, `note`/`payment_method` kept, `paid_at` set on paid, explicit `note:""` clears; `amount:-5` → 400 `amount_negative`, `"abc"` → 400, `month 2026-13` → 400, unknown teacher → 404 `teacher_not_at_school`; `amount:0` + pending → 201 (by design), `0` + paid → 400 `amount_required`. **Not covered:** free-text `status` on both paths and `amount:-3` / `paid_at` on the viewset PATCH → SCH-R4-02. |
| **R3-M7** Reports page overflow on 390 px | `a4c8ab7` #148 | **VERIFIED FIXED LIVE** | Mobile owner: `/it/school/reports` and each of the 5 tabs → `scrollWidth 390 = clientWidth`; inner scrollers present (`div.overflow-x-auto` 636/358 and 974/356). New overflows elsewhere → SCH-R4-01. |
| **R3-M9** negative `credit_cost` on `PATCH /school/courses/<id>/` | `bb1786f` #152 | **VERIFIED FIXED LIVE** | `PATCH /school/courses/85fb7b9e…/ {"credit_cost":-1}` → 400 `["credit_cost must be greater than zero"]`, `0` → 400, `-0.5` → 400, `1.25` → 400 (1 decimal place), `1.5` → 200 stored 1.5. Create path: −1/0 → 400, 1.25 → "half-credit steps", "abc" → "must be a number". Edit page: input `min=0.5 step=0.5`, −1/0 blocked client-side ("Value must be greater than or equal to 0.5."), 1.5 → PATCH 200. |
| **R3-M11** `School.timezone` unvalidated | `3fad7e4` #156 | **VERIFIED FIXED LIVE** | `PATCH /school/profile/ {"timezone":"Mars/Olympus"}` → 400 `["Unknown timezone 'Mars/Olympus'. Use an IANA name such as Europe/Rome."]`; `""` → 400 "may not be blank"; `"Europe/Madrid"` → 200 and read back; restored to Europe/Rome. No dropdown exists in the school UI (observation). |
| **R3-M12** invalid invite e-mail creates a ghost user | `27b93b6` #158 | **VERIFIED FIXED LIVE** | `POST /school/team/` and `POST /school/teachers/` with `not-an-email`, `a@b`, `spaced out@example.com` → 400 `{"email":["'…' is not a valid e-mail address."]}` ×6; `GET /school/team/` pending list contains only the two real invites — no ghost. |
| **R3-M16** inbox "View student profile" 404 | `c8fc651` #166 | **VERIFIED FIXED LIVE** | Thread `77405d08…`: link "Visualizza Profilo Studente" href `/school/students?student_id=a5e1aeb4…` → lands on `/it/school/students?student_id=…`, `GET /school/students/detail/` 200, the sheet opens with "QA R4 Studentessa Uno · Profilo · Documenti", no 404 text (`school-inbox-view-student-profile-it.png`). |
| **R3-L1** credits-low blank expiry / repeated | `f7b6479` #169 | **VERIFIED FIXED (as designed)** | Manual-grant mail reads "ti restano 4 lezioni." — no "(scade il )"; recurring-package mail "(scade il 11-10-2026)"; each package warned once over 9 later bookings/cancellations. Per-package semantics → SCH-R4-06. |
| **R3-L5** `active_subscriptions_count` | `4f74fd2` #177 | **VERIFIED FIXED LIVE** | `GET /school/reports/` `active_subscriptions_count` 0 → **1** right after `POST /school/credits/grant/ {package_catalog_id: <recurring pkg 288fdde8…>}`; dashboard "ABBONAMENTI ATTIVI 1" (owner and admin). |
| **R3-L10** closure range / last default status / `javascript:` website / stale seats | `587cc8c` #189 | **VERIFIED FIXED LIVE** | API `POST /school/closures/ {date:"2026-12-30", end_date:"2026-12-28"}` → 400 `["The end date cannot be before the start date."]`; UI settings form now sends what was typed and shows "La data di fine non può precedere quella di inizio." (`school-settings-closure-bad-it.png`), a valid range → 201. Deleting the default status (`f34e45d5…`) promoted the next in sort order (`qa r4 presente`, order 0) → exactly one default. `PATCH /school/profile/ {"website":"javascript:alert(1)"}` → 400 `["Use a full http:// or https:// address."]`, `data:` → 400, `https://…` → 200 and published by `/api/schools/public/`. Cancelling classes/courses with bookings leaves the orphans with `current_bookings 0` (5 cancelled D lessons). |
| **R3-L13** school-panel English strings | `8c26c4a` #195 | **PARTIAL** | Fixed in all 5 locales: calendar year initials (it "L M M G V S D", es "L M X J V S D", fr "L M M J V S D", de "M D M D F S S"), "11 lezioni / clases / cours / Stunden", add-class modal "Crea lezione / Annulla" (es "Crear clase", fr "Créer le cours", de "…/Abbrechen") and "Seleziona corso…", package placeholders ("es. Pacchetto Starter / Breve descrizione…"), Reports CSV headers (4 exports, e.g. `Lezione,Data,Insegnante,Sede,Aula,Costo Aula (€),…,Stato`), teacher edit `email_taken` → "Questa email è già usata da un altro account.", class-move error "La scuola è chiusa il 06/10/2026. Scegli un'altra data.", package delete `confirm()` in Italian, lesson cancel "Clicca di nuovo per confermare" / "Nessuna allieva prenotata. Annullare la lezione?"; Payments "Package/Subscription" now goes through `t('typePackage')` (code; no transaction rows on this tenant to render). **Still English:** team `confirm()` + "Failed to remove", compensation plan placeholders → SCH-R4-04. |
| R2-H3 foreign location room | #89 | **VERIFIED** | R3 School C location `35d9cd81…`: POST room → 400 "Location does not belong to your school.", PATCH own room to it → 400; unknown id → 400 "object does not exist". |
| R2-H5 settings save | #83 | **VERIFIED** | UI 48 h / 3 h / en → `PATCH /school/profile/` 200 (body without `grace_period_days`), "Impostazioni salvate", read back 48/3/en; restored 24/2/it. |
| R2-H6 documents born pending / gate | #95 | **VERIFIED** | upload → `pending`; gate blocks missing/pending/rejected (`400 documents_required` ×3) and lets a validated one through; `approved_locked` on student DELETE; UI sheet Approva/Rifiuta/Segnala → `{"action":…}`. Nit SCH-R4-08c. |
| R2-H7 added class inherits plan | #97 | **VERIFIED** | `POST /school/classes/ {course_id, date:"2026-09-23", …}` without plan → lesson `compensation_plan b613f9ff…` = course plan; T1's September compensation lists them at €22. |
| R2-H8 staff deletes student | #89 | **VERIFIED** | staff `DELETE /school/students/delete/` → 403 `forbidden`; owner UI delete → see §6 (s2). |
| R2-M5 attendance timing | ee646cf | **VERIFIED** | future 28/09 and today 23:30 → 400 `lesson_not_yet_occurred`; past 01/09 and 08/09 → 200 (`completed`). |
| R2-M6 foreign teacher payment | #89 | **VERIFIED** | `teacher_id 9c987f88-0000…` → 404 `teacher_not_at_school`. |
| R2-M7 closure reporting | ee646cf | **VERIFIED** | course A `skipped_closure_dates ["2026-09-21","2026-10-05"]`, single-on-closure-only course → 400 with the date, add class on 21/09 → 400 `school_closed`, weekly add reports skipped 08/10, move onto 06/10 → 400 (API + class page message), wizard banner "1 lezione non è stata creata: la scuola è chiusa il 21/09/2026." |
| R2-M8 `credits_used` / counters | ee646cf | **VERIFIED** | reports `credits_used 14.5` = 16 deducted − 1.5 refunded (burned cancellation counted), `bookings_total 10`, `bookings_cancelled 2`, `no_shows 1`; deleting course D refunded exactly the 5×2 credits (4 → 14). |
| R2-M9 validation values | ee646cf | **VERIFIED** | cancellation −5, notice −1, language `xx`, blank e-mail/name, bad e-mail → 400 with field messages; fee/grace → 403 `forbidden fields`; room cap 0 / cost −10, plan base −5 / bonus max < threshold, simulate −1, package validity 0 / cap −1 / credits 0 / 1.25 / price −1 / unknown lesson type, discount 150 % / max_uses −1 / dup code (case) → 400 each. |
| R2-M10 invalid status_id | ee646cf | **VERIFIED** | per-row `{"ok":false,"error":"invalid_status_id"}`, nothing written. |
| R2-M12 staff enrol ignores capacity | #112 | **VERIFIED** | Lesson `ad8b08d5…` (11/09 09:00) set to `max_capacity 1`; enrol s1 → 200 `overbooked:false`; enrol s2 → **409 `{"error":"lesson_full","message":"Class is full","current_bookings":1,"max_capacity":1,"allow_overbooking_required":true}`**; with `allow_overbooking:true` → 400 "Student has no valid credits or subscription" (s2 had no wallet — the credit requirement wins, as it should; the overbooked write itself was not reached). |
| R2-M13 two language fields | ee646cf | **VERIFIED** | student `PATCH /student/profile/ {"language_preference":"de"}` also flips `User.language_preference` (`/auth/me` → de); the next school-side no-show mail arrived as "👻 Du hast uns gefehlt — …, 08-09-2026" (1 s) while the school is `it`; restored to it. |

---

## 4. E-mails verified (all from `support@alinaquintana.com`, delivery 0–16 s)

| Trigger (UTC) | Recipient | Subject | Locale | Delivery | Links / notes |
|---|---|---|---|---|---|
| Team invite admin (UI) 18:31:12 | qa-r4-school-admin | ✉️ Invito da QA R4 School B — Amministratore | it ✓ | 6 s | `/it/setup-account?uid&token` → completed via UI → dashboard ✓ |
| Team invite staff (UI) 18:31:12 | qa-r4-school-staff | ✉️ Invito da QA R4 School B — Staff | it ✓ | 9 s | completed via API ✓; reuse → 400 `invalid_or_expired_token` |
| Teacher invite ×2 (UI) 18:31:25/28 | t1 / t2 | ✉️ Invito da QA R4 School B — insegnante | it ✓ | 13 / 16 s | t1 completed via API ✓ (`/teacher/schools/` lists the school + plan) |
| Register s1 (UI, mobile, deep link) 18:35:20 | s1 | 🩰 Benvenuta in Danza Classica No Under 40! | it ✓ | 5 s | `/it/student/profile?for=…` |
| No-show via API 18:42:34 | s1 | 👻 Ci sei mancata — Danza Classica No Under 40 - Base e Fondamenta, 01-09-2026 | it ✓ | 0 s | date in subject/body ✓ |
| Bookings ×7 (API) 18:51:32–54 | s1 / school inbox | ✅ Prenotazione confermata — <corso>, <data> / 📅 Nuova prenotazione: QA R4 Studentessa Uno — <corso>, <data> | it ✓ both | 1–2 s | body: corso, data, ora (durata), insegnante, sede·aula, indirizzo, "Le mie lezioni" link |
| Credits low 18:51:34 / 18:51:44 | s1 | 💳 Il tuo pacchetto sta per finire — 3 / 4 lezioni rimaste | it ✓ | 1 s | no blank clause; one per package (SCH-R4-06) |
| Cancel < 24 h / > 24 h 18:51:55–57 | s1 / school inbox | ❌ Prenotazione annullata — …, 11-09-2026 / …, 14-09-2026 and ❌ Prenotazione annullata: QA R4 Studentessa Uno — … | it ✓ | 1 s | |
| Teacher resend (API) 18:57:00 | t2 | ✉️ Invito da QA R4 School B — insegnante | it ✓ | 2 s | |
| Team resend on ACTIVE member (API) 18:57:01 | qa-r4-school-staff | ✉️ Invito da QA R4 School B — Staff | it ✓ | 1 s | link re-ran complete-invite → SCH-R4-05 |
| Course D deleted with 5 bookings (API) 19:09:51 | s1 | 🚫 Lezione annullata — Flex No Under 40, <16/18/23/30-09, 02-10> ×5 | it ✓ | 1–2 s | "Il tuo credito è già tornato nel pacchetto" ✓ |
| No-show with student language de (API) 19:16:01 | s1 | 👻 Du hast uns gefehlt — …, 08-09-2026 | de ✓ (student's language, school is it) | 1 s | R2-M13 |
| Register s2 (UI, mobile, `/de/register?next=/student/book?school=<uuid>`) 19:37:43 | s2 | 🩰 Willkommen bei Danza Classica No Under 40! | de ✓ | 3 s | `/de/student/profile?for=…`; enrolled (`POST /student/school/` 200) |
| Teacher resend to ONBOARDED T1 (API) 19:30:22 | t1 | ✉️ Invito da QA R4 School B — insegnante | it ✓ | 1 s | SCH-R4-05 teacher variant (link not followed) |
| Teachers page "Rinvia invito" (UI) 19:29:55 | t2 | ✉️ Invito da QA R4 School B — insegnante | it ✓ | 1 s | banner "Invito inviato di nuovo a QA R4 Teacher Due." |
| Student deleted by the school (UI edit modal → "Elimina allieva" → "Clicca di nuovo per confermare" → confirm) 19:41:25 | s2 | 🕊️ Dein Konto wurde von QA R4 School B gelöscht | de ✓ (student's language) | 1 s | "…dein Profil, deine Buchungen und deine Dokumente wurden entfernt. Das war keine Anfrage von dir…"; login afterwards → 401 |
| Document approved / rejected | — | no such e-mail exists (observation) | | | |
| Stripe `school.stripe_connected` | school | not received (onboarding not completed — hCaptcha, out of scope) | | | expected |

School inbox `qa-r4-school-b@uberip.com`: 7 × 📅, 2 × ❌, plus the HQ "— Titolare" invite. Total verified: 39 e-mails (14 to s1, 2 to s2, 2 admin/staff invites + 1 resend, 4 teacher invites/resends, 9 school copies, welcome ×2, account-deleted ×1 …). No expected e-mail failed to arrive; nothing arrived in the school's language when the student's differed.

---

## 5. Verified good (passed)

- **Team:** UI invites admin/staff → 201 `email_sent:true`, both onboarded (admin through the real `/setup-account` form, staff via API); owner sees "Proprietario" in the role select, admin sees admin/staff only; edit modal (first/last name, e-mail, phone, role, "Invia reset password" button present — not fired, budget); resend on a pending invite; `already_a_member` on re-invite (400); unknown membership → 404; malformed id → 400 (SCH-R3-06 fixed); founder protections (R3-H4) in API and UI; staff → `section_forbidden team`.
- **Teachers:** UI invites ×2 with honest banner (`?added=…&emailSent=1`), onboarding, edit phone/bio, string booleans (`"true"` accepted, `"maybe"` → 400), e-mail collision → 400 `email_taken` mapped to Italian, resend, existing-account invite (`existing_account:true`, no mail), two-click remove ("Rimuovi → Sicuro? Clicca di nuovo" → `{"deleted":1}`), teacher sees the school + compensation plan in `/teacher/schools/`; `GET /school/teachers/x/` → 404, resend with id `x` → 400.
- **Locations/rooms/closures:** create via API + UI, all validation values, foreign/unknown location → 400, closures single/range/partial, UI end<start error and valid range, delete.
- **Compensation:** plans (bonus range), simulate 5 → €32 / −1 → 400 "between 0 and 1000", summary lists occurred lessons only (T1: 01/09, 08/09, 11/09 09:00 → €66), payment upsert with `paid_at`, UI plan summary "€22/lezione · +€5/studente (3–10 studenti)".
- **Courses:** wizard end to end (Sbarra a Terra, T1, 1.5 credits, Piano Base, single-on-closure + weekly) with the closure banner; plain create ×3 with closure skipping; PATCH validation; reorder/overview endpoints; bulk-edit form fully localized; course detail filters are multi-select checkboxes with "Seleziona tutto"; delete without bookings (hard, `classes_cancelled 1`) and with bookings (cancel + refund + 5 e-mails, orphans `course:null`, seats released).
- **Lessons/calendar:** add like-the-course inherits the plan, add on closure → 400, weekly add skips the closure, move onto closure → 400 with localized message on the class page, move back → 200, single class cancel via API (`{"cancelled":true,"refunded":0}`) and via the two-click UI button with confirm; calendar day/week/month/year in all locales, add-class modal creates a lesson (`created:1`).
- **Packages/discounts:** one-off, recurring, drop-in, custom half-credit, UI create with the lesson-type guard message ("Scegli almeno un tipo di lezione…"), Duplica → pre-filled "(copia)" → 201, Modifica → PATCH, Disattiva → PATCH `active:false`, Elimina → Italian `confirm()` → 204, delete of a package with grants → 400 (deactivate instead), all validation values, client-side half-credit step, discount code create + validation (name required, 0–100 %, max_uses ≥ 1, unique code case-insensitively), `/subscriptions` → `/packages` redirect, public catalogue hides nothing unexpected.
- **Statuses:** create ×3, exact duplicate → 400, second default unsets the first, deleting the default promotes the next by sort order, page buttons localized.
- **Settings/profile:** full save cycle + restore, closures, identity PATCH + restore, validation and HQ-only fields, timezone and website scheme validation, profile page shows the DRF field message.
- **Students:** self-registration on mobile through `/it/register?next=/student/book?school=<uuid>` → `POST /student/school/ {school_id}` 200 → enrolled and listed in `/school/students/`; search; edit modal (Ripristina Password / Salva Modifiche / Elimina allieva); sheet from the list and from the inbox link; XLS (16.7 KB) and PDF (6.1 KB) exports downloaded; free-lesson flag both ways; staff delete → 403.
- **Credits:** API grants 10 / 8 (recurring package → expiry derived, `active_subscriptions_count` 1) / 0.5 (staff, `manualCredits`), modal grant 0.5 with note → 201, bounds 0.3 / 0 / −1 / 100000 / NaN → 400, unknown student → 404, grants list with granter.
- **Documents:** types CRUD (dup code → 400), upload → pending, reject/validate/expiry/flag via API and via the UI sheet (Approva/Rifiuta/Segnala), gate incl. expiry, `approved_locked`, school can delete a student document.
- **Bookings (student side, for the school's data):** confirmed with credits deducted (1.5 / 2 / 1), duplicate → 400 `already_booked`, unknown lesson → 404, past lesson → 400 `lesson_already_started`, cancel outside policy → `credit_refunded:false, outside_policy`, inside → `credit_refunded:true, within_policy`, second cancel → 400 `not_cancellable`; wallet 18.5 − 16 + 1.5 + 10 (course D refund) − 0.5 … reconciles at every step.
- **Attendance:** school-side enrol on past lessons (200, duplicate → 400 "Student already booked"), marking via UI and API (Presente/Assente), lesson → `completed`, credits untouched by any mark, future/today-later → 400, invalid status → per-row error, no-show mail in the student's language.
- **Inbox:** student and teacher threads visible, unread `{"total":2,"by_type":{"school_teacher":1,"school_student":1}}` and sidebar "Messaggi 2", quick reply "QA R4 Saluto" inserted and sent (201), free reply (201), priority/status selects, "Visualizza Profilo Studente".
- **Reports:** 5 tabs with real numbers (4 lessons, 50 % absence, 33.3 % cancellation, 1 student, 4 credits, 2 used), 4 localized CSV exports with BOM, teacher rows (Uno 4 lessons €66.00).
- **i18n / responsive:** 120 owner pages × 5 locales + 40 staff + 20 admin pages: 0 raw keys / ICU vars / `undefined`, 0 non-4xx console errors, 0 desktop overflow; 25 mobile pages: 0 overflow except SCH-R4-01.
- **Robustness:** malformed ids → 400/404 on team/teachers/classes/students endpoints; string/number/null bodies → 400; `POST /school/courses/ {}` → 400 field list; `POST /school/subscriptions/` → 405 (retired engine).

---

## 6. Test data created (tenant QA R4 School B `75e2de47-02db-4e36-a2bd-4e18b7a501ca`) — left in place unless noted

- **Memberships:** 44 owner/founder (`qa-r4-school-b@`), 47 admin (`qa-r4-school-admin@`, promoted to owner at 18:34:49 and 19:10:09, demoted back both times — final `admin`), 48 staff (`qa-r4-school-staff@`; password re-set to the same value by the SCH-R4-05 probe at 19:07:35). Rejected probes: `qa-r4-school-ownerx@` (owner invite by admin, 403), `qa-r4-school-godmode@` (400), `qa-r4-staff-invite@` (staff, 403), three invalid addresses (400).
- **Teachers:** `e65014b8…` QA R4 Teacher Uno (user `ccfda330…`, phone +39 333 1112223, `can_view_all_lessons true`; **removed by mistake at 19:10:48 by an over-broad locator and re-linked at 19:12:25** — see §8), `713f1a0b…` QA R4 Teacher Due (invite pending, resent once). `qa-r4-staff-teacher@` invite by staff → 403 (not created).
- **Locations:** `2486284d…` "QA R4 Sede Centrale" (rooms `ba07df13…` Sala Grande 12/€20, `6f427854…` Sala Piccola 2/€0), `00f415c3…` "QA R4 Sede Nord"; room `e874228a…` "QA R4 Sala UI" (8, €12.5, created from the Locations UI at 19:31). Stripe Connect account `acct_1UEZHp3q9VUHwNOx` created by the admin gate probe (test mode, onboarding incomplete; cannot be removed from the panel).
- **Closures:** `77cc797e…` 2026-09-21, `fe46391d…` 2026-10-05→09, `11b7a4da…` 2026-10-22 partial 14:00. Created and deleted: `ec9fca80…` 30–31/12 (UI). Rejected: 30→28/12 (API + UI), partial without time.
- **Compensation:** plans `b613f9ff…` "QA R4 Piano Base" (€22, +€5 from 3 to 10), `1f731e66…` "QA R4 Piano Flat" (€25.5); payment `87c94daf…` T1 2026-09 **€50 paid, paid_at 18:38:14Z, note "QA R4 settembre", bank_transfer** (restored after the SCH-R4-02 probes). Deleted: `57f37792…` €0 2026-10.
- **Attendance statuses:** `671bde69…` QA R4 Presente (default), `ab835def…` QA R4 Assente (burns), `2bf5b410…` QA R4 Giustificata. Deleted: `f34e45d5…` Default2 (promotion probe), `313e9967…` "qa r4 presente" (case-dup probe). Staff-created `qa_r4_staff` document type `c27c89de…` and quick reply `ec416f6e…` "QA R4 staff qr" left in place (matrix evidence).
- **Document types:** `5942fb86…` QA R4 Certificato medico (required, expiry), `c737a68a…` QA R4 Liberatoria.
- **Packages:** `81a908e3…` Pacchetto 10, `288fdde8…` Abbonamento Mensile (recurring, has grants), `0d26c181…` Lezione Singola (drop-in; deactivated by a UI click at 19:09 and re-activated at 19:19), `69a50434…` Pacchetto Custom (5.5, Flex only, cap 2), `989dab93…` "Pacchetto 10 (copia)" (API, active), UI-created "QA R4 Pacchetto UI" `48c0423d…` (**deleted via UI**) and its duplicate `55e9fb8b…` "QA R4 Pacchetto UI (copia)" (kept, active). Discount code `f4062353…` QAR4TEN (10 %, 100 uses). Quick reply `9dfa7681…` "QA R4 Saluto".
- **Courses:** A `85fb7b9e…` Flex, T1, 1.5 cr (lessons 14/09 [booking cancelled/refunded], 23/09 → moved to 24/09 [**cancelled via UI**], 28/09 [booked by s1], 01/10, 12/10, 15/10, 19/10, 26/10, 05/11 [added from the calendar modal, **cancelled via API**]); C `8e1011be…` DCN40-BF, T1, 1 cr (01/09 completed — s1 Assente, 08/09 completed — s1 Presente [briefly Assente for the R2-M13 mail], 11/09 09:00 `ad8b08d5…` max_capacity 2 → **1** [s1 enrolled `cd736997…`, s2 capacity probe], 11/09 23:30 [booking cancelled/burned]); W `30f15379…` Sbarra a Terra (wizard, 4 lessons Nov). **Deleted:** D `17db9d08…` (8 lessons → 5 cancelled orphans `course:null` with 0 seats, 5 refunds), staff course `6fdd071f…` (1 lesson). Cancelled orphan count 6 + 05/11.
- **Students:** s1 (grants: 10 manual `bec7309a…`, 8 recurring `180f4e10…`, 0.5 staff `506c3373…`, 0.5 modal `2b1b0e5c…`; wallet **13.5** at 19:41 (18.5 granted − 16 deducted + 1.5 refund + 10 course-D refund − 0.5 … − 1 cap-1 enrolment `cd736997…`); bookings 28/09 confirmed, 01/09 no-show, 08/09 present, cancelled 14/09 + 11/09 23:30, 5 D bookings cancelled by the school; documents `d6e80ba2…` valid exp. 2027-06-30 note "QA R4 nota scuola"? (note lives on `ac447ae9…` which was deleted) and `eff3900c…` file-less, valid; language it). **s2** `qa-r4-school-s2@` (de; user `9f345332…`, Student `bc5b3c13…`, SchoolStudent `e8f1d045…`): registered 19:37:43, **deleted by the owner through the UI at 19:41:25** (`DELETE /school/students/delete/?student_user_id=9f345332…` → 204; `/school/students/` no longer lists her, login → 401 "No active account found", `active_students` 2 → 1).
- **Chat:** `77405d08…` school↔s1 (3 messages), `bcb99b1e…` school↔t1.
- **Settings changed and restored:** timezone (Mars/Olympus ×, Europe/Madrid → Europe/Rome), website (probes → ""), cancellation 48→24, notice 3→2, language en→it; `block_booking_on_documents` false → **true** for the gate tests → **restored to false at 19:40:51**; final `GET /school/profile/` differs from the 18:30 baseline only in `stripe_account_id` (`""` → `acct_1UEZHp3q9VUHwNOx`); student s1 language de → it; attendance 08/09 Assente → Presente.
- **Ledger:** 2 registrations (18:35:20 s1, 19:37:43 s2 — a first s2 attempt at 19:36:40 failed on a form selector before any `/auth/register/` call), 0 password resets (budget unused). Final reports: `active_students 1, lessons_completed 2, lessons_scheduled 14, bookings_total 11, bookings_cancelled 7, no_shows 1, credits_used 5.5, active_subscriptions_count 1`. Inboxes created: `qa-r4-school-{admin,staff,t1,t2,s1,s2}@uberip.com`.

---

## 7. Screenshots (`$SP/shots/school-*.png`, ≈ 290 files)

Team: school-team-before-invite-it, -after-invite-admin-it, -after-invite-staff-it, -it, -edit-modal-it, -as-admin-it, -as-coowner-it, -coowner-remove-founder-it, -coowner-edit-founder-it · Setup: school-setup-account-admin-it, school-dashboard-after-setup-admin-it, school-setup-account-admin-reuse-it · Teachers: school-teachers-empty-it, school-teacher-invite-form-it, school-teachers-after-invites-it, school-teachers-it, -edit-modal-it, -edit-email-collision-it, -after-remove-it · Calendar: school-calendar-default/giorno/settimana/mese/anno-it, school-calendar-add-class-it, -add-class-done-it, school-calendar-year-<locale>, school-calendar-add-class-<locale> · Courses: school-course-wizard-step1/step2/step2-filled/result-it, school-course-detail-it, school-course-edit-it, school-course-edit-neg-it, school-class-edit-it, school-class-move-closure-it, school-course-detail-filter-it, school-course-cancel-lesson-armed-it · Lessons: school-lessons-it, school-lessons-cancel-armed-it, school-lessons-after-cancel-it · Compensation: school-compensation-it, -plan-form-it, -payments-it, school-compensation-form-<locale> · Packages: school-packages-it, -form-it, -form-filled-it, -create-attempt-it, -after-create-it, -duplicate-form-it, -after-duplicate-it, -validation-it, -discounts-it, school-discounts-form-it · Statuses: school-statuses-it · Settings/profile: school-settings-it, -closure-bad-it, -closures-it, -after-save-it, school-profile-it, school-profile-website-it · Students/credits/docs: school-students-it, school-student-edit-modal-it, school-student-sheet-it, school-credits-it, school-credits-modal-it, -modal-filled-it, -after-grant-it, school-documents-it, -sheet-it, -sheet-after-it, -sheet-rejected-it, -sheet-approved-it · Attendance: school-attendance-page-it, -submitted-it · Inbox: school-inbox-it, -student-conv-it, -quick-replies-it, -quick-reply-it, -student-conv-after-it, -teacher-conv-it, school-inbox-view-student-profile-it · Payments/reports: school-payments-owner-it, school-payments-admin-it, school-reports-it, school-reports-tab0..4-it · Register/student: school-mobile-register-it, -register-filled-it, school-mobile-student-after-register-it, school-student-book-it, -book-list-it, -book-modal-it, -book-doc-error-it, school-mobile-register-de, school-mobile-student-after-register-de, school-student-edit-modal-s2-it, school-student-delete-armed-it, school-students-after-delete-it, school-locations-room-cap0-it, -room-costneg-it, -final-it, school-teachers-after-resend-it, school-reports-filter-*-it, school-lessons-filter-it, school-documents-sheet-fileless-it · Sweeps: school-<page>-<locale> ×120 (owner), school-staff-<page>-it ×20, school-admin-<page>-it ×20, school-mobile-<page>-it ×25 (+ reports tabs).

Text dumps: `$SP/reports/dumps/school-*.txt` (≈ 280 files; `scanhits.jsonl` empty — 0 hits).

---

## 8. Assumptions, decisions, hazards

- **Self-inflicted incident, disclosed:** at 19:10:48 the teachers-page "Rimuovi" test used an over-broad locator (`*:has-text("QA R4 Teacher Due") … .last()`) and removed **Teacher Uno** instead of Teacher Due (`DELETE /school/teachers/ → {"deleted":1}`). Re-linked at 19:12:25 via `POST /school/teachers/ {email: t1}` (`existing_account:true`, no e-mail) and grants/phone restored; courses A/C/W kept their `teacher` FK throughout (the Teacher row is not deleted by an unlink). The two-click UI flow itself ("Sicuro? Clicca di nuovo" → DELETE) is therefore verified; Teacher Due's pending invite is still in place.
- A first document was created with `document_type` instead of `type_ref` (the model FK) and was therefore never linked to the required type; every gate verdict in §3 is from the second, correctly typed document `d6e80ba2…`. The untyped one was deleted by the school (204).
- The R3-H2 admin call intentionally created a real Stripe test-mode Connect account on this tenant (`acct_1UEZHp3q9VUHwNOx`); the onboarding link was never opened (hCaptcha, out of scope). Nothing was called as the owner.
- The read-only cross-tenant call (`GET /school/locations/` with the cached R3 School C owner token) was used only to obtain a real foreign location id; no mutation on any other tenant.
- The password-reset budget (1) was not spent (the Team-modal button and the Students "Ripristina Password" button were verified present, not fired).
- `enabled.team_invite` was left as the coordinator set it; the R3-M2 template-off path was not exercised.
- Severity calls: SCH-R4-01 Medium (broken page in one viewport, same class the coordinator rated Medium in R3-M7); SCH-R4-02 Low because the affected write path is API-only and owner-scoped, although the record is financial — coordinator may raise it to Medium as R3-M6 PARTIAL; SCH-R4-03 Low (no user-reachable path, no traceback) although R3 graded the family Medium.
- "Attendance never changes credits" and "a cancelled booking inside the policy window burns the credit" are treated as the domain rules (§4 of CLAUDE.md), and confirmed.
- Throttles respected: 2 registrations (both after the shared hourly window had room), 0 resets, no login 429; all other accounts came from invite flows.
- The Locations UI room created at 19:31 and the s2 capacity enrolment are the only writes after the 19:28 draft of this report; every id above was re-read at 19:40 (`32_final_restore.js`).
