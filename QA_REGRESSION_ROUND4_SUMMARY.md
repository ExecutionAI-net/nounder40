# QA Regression Round 4 — Live End-to-End Summary

**Date:** 2026-09-11 (18:19 → 19:55 UTC)
**Environment:** live dev deployment `https://dev.danzaclassicanounder40.com` — `develop` @ `f4155c9` (deployed 2026-09-10 11:00 UTC by CI run 34468639545 "Build and deploy to EC2"), real Docker/EC2 stack, real Stripe **test mode** (connected account "Hakan Sandbox"), real transactional e-mail. Not localhost, not production.
**Why this round exists:** every round-3 finding (`QA_REGRESSION_ROUND3_SUMMARY.md`, 2 Critical / 5 High / 16 Medium / 14 Low) was fixed by PRs #117–#195 (40 PRs, 180 files, merged 2026-09-08 → 2026-09-10) and verified with unit tests and the backend suite only. This is the independent, from-scratch pass on the real deployment: confirm those fixes live, catch the regressions they introduced, and find what was missed.
**Method:** one coordinator + 6 parallel agents — HQ, School, Teacher, Student/payments, cross-cutting (authorization matrix, cross-tenant isolation, WebSockets, public surface, auth flows, robustness) and a dedicated i18n/responsive crawl. Every agent had its **own isolated browser profile** (Playwright 1.55 / Chromium, persistent context, desktop 1366×900 + mobile 390×844) and its **own tenant** (three fresh schools created by the coordinator through the real HQ invite flow, plus the Stripe-connected QA Test School for payments; round-3 tenants read-only for the i18n crawl and as cross-tenant attackers). Every UI observation was cross-verified with an independent API call on a token decoded first (`whoAmI`). All users, courses, lessons, packages and payments were created through the product's own flows (no Django Admin, no DB access). E-mails were verified end to end on disposable inboxes (mail.tm, `@uberip.com`): subject, per-recipient locale, delivery time, link validity. ≈ 1,960 screenshots and ≈ 1,800 text dumps were produced; every role's pages were crawled in all 5 locales on desktop and mobile (1,010 loads by the i18n agent alone); the authorization matrix ran 5,786 token × endpoint requests; 11 real Stripe test-mode payments were made; 7 self-registrations and 2 password resets were used. The coordinator independently re-reproduced every High and the most consequential Medium findings before accepting them (see "Coordinator verification").

Per-panel detail (repro steps, request/response evidence, screenshot names, exact test-data ids):

- [QA_REGRESSION_ROUND4_HQ.md](QA_REGRESSION_ROUND4_HQ.md) — findings `HQ-R4-*`
- [QA_REGRESSION_ROUND4_SCHOOL.md](QA_REGRESSION_ROUND4_SCHOOL.md) — findings `SCH-R4-*`
- [QA_REGRESSION_ROUND4_TEACHER.md](QA_REGRESSION_ROUND4_TEACHER.md) — findings `TCH-R4-*`
- [QA_REGRESSION_ROUND4_STUDENT.md](QA_REGRESSION_ROUND4_STUDENT.md) — findings `ST-R4-*`, full purchase table and the webhook-vs-reconciliation timeline
- [QA_REGRESSION_ROUND4_CROSSCUT.md](QA_REGRESSION_ROUND4_CROSSCUT.md) — findings `X-R4-*`, 5,786-request authorization matrix ([condensed table](docs/qa/round4-crosscut-matrix-condensed.md))
- [QA_REGRESSION_ROUND4_I18N.md](QA_REGRESSION_ROUND4_I18N.md) — findings `I18N-R4-*`, 1,010-page-load locale/viewport crawl + static catalogue check
- [docs/qa/round4-screenshots/](docs/qa/round4-screenshots/) — curated key-evidence screenshots (30 files)

---

## Verdict

**The round-3 fix wave is real on the live deployment: of the 7 round-3 Critical/High findings, 6 are verified fixed exactly as described and 1 is only partially closed; none regressed a previously working flow.** Both Criticals are gone — every HQ team write verb (PUT/PATCH/DELETE and the invitation path) is now guarded and a `team`-only role cannot escalate (R3-C1), and the image-upload stored-XSS hole is closed at the choke point with byte-level validation, `nosniff` and a sandbox CSP on everything under `/media/public/` (R3-C2; the round-3 blobs are purged). Narrow HQ roles no longer read, download or delete student documents (R3-H1), Stripe onboarding is gated and idempotent (R3-H2: two concurrent owner calls → one account), a removed HQ member can be re-invited and logs in again (R3-H3), and the missing webhook is now covered by the hourly reconciliation task — the two purchases paid with the return page blocked were activated by the 19:50 beat run with no `verify-session` call (R3-H5; webhook delivery itself is still absent, 0 of 11 payments). The money path is correct end to end for every product type: 11 real Stripe payments (one-off packs plain/−10 %/−€5, a monthly subscription with replay protection, a drop-in, a shop order, a 3DS challenge, a declined card, two blocked-return runs) all produced the right credits, exactly one `Transaction` with the 10 % split and exactly one receipt with the corrected copy. Cross-tenant isolation stayed clean across 5,786 combinations. Of the 16 round-3 Mediums, 11 are fully fixed and 5 partially; of the 14 Lows, 10 fixed, 2 partial, 1 regressed (PhoneInput) and 1 not live (HSTS, an ops step).

**What is still not launch-ready: two new High items, both found by agents and re-reproduced by the coordinator.** A **co-owner can rewrite the founder's login e-mail** through `PATCH /api/school/team/` — the round-3 founder protection guarded role changes and removal but not the identity fields, so the account-takeover-via-e-mail pattern that R3-C1 closed on the HQ side is still open on the school side (R4-H1). And **`DELETE /api/school/lessons/<id>/` on a booked class deletes the students' bookings by cascade with no refund and no e-mail** — the `classes/` endpoint the UI uses refunds correctly, but the parallel `lessons` viewset silently burns paid credits (R4-H2). Ten Medium items follow: `School.timezone` writable by a `staff` member (silent policy drift), the remaining 500s for array bodies and unparsed ids (incl. `POST /api/stripe/checkout/`, which is also why an HQ-owned package still cannot be bought), a teacher writing her own `photo_url`/`active`, the calendar WebSocket never firing for the class views the UI actually calls, a month shown as "Paid" after later attendance raised the total, credits-low e-mails sent on a drop-in purchase and per package, two more mobile pages that overflow, a **regression** of the round-3 PhoneInput fix (typing a full international number now stores a wrong prefix and a truncated number), and RFC-valid-but-unroutable invite addresses still creating ghost users.

Counts: **Critical 0 · High 2 · Medium 10 · Low 18** (consolidated; items found by more than one agent are merged and cross-referenced).

---

## Prioritized findings

Severity scale: **Critical** = money lost / data corruption / privilege escalation / cross-role or cross-tenant data leak / core flow broken; **High** = main flow broken or misleading, silent wrong financial or attendance numbers, destructive action by the wrong role; **Medium** = functional bug with workaround, missing validation with realistic impact, wrong e-mail behaviour, broken page in one locale/viewport; **Low** = cosmetic, copy, minor i18n, data hygiene. "vs R3" says whether the item is NEW, a REGRESSION introduced by a round-3 fix, a round-3 item PARTIALLY fixed, or STILL OPEN.

### Critical

_None._

### High

| ID | Panel | Finding | vs R3 | Detail |
|---|---|---|---|---|
| R4-H1 | School (team) | **A co-owner can rewrite the founder's login e-mail (and phone/name) via `PATCH /api/school/team/`.** `SchoolTeamView.patch` consults `_is_founder_membership()` only inside the role-change branch and in `delete()`; the e-mail write below it has no founder guard. Live: founder promotes a staff member to co-owner (200); the co-owner `PATCH {id:<founder>, email:"qa-r4-x-hijack@…"}` → **200**, and the founder's own `GET /auth/me/` then returns the hijacked address (the login credential moved, not a display copy). Role change and removal of the founder are correctly refused (`cannot_change_founder_role` / `cannot_remove_founder`). With the e-mail moved to an address the attacker controls, the public password reset completes the takeover. A co-owner can mint further co-owners, so any owner-grade member reaches it. Reproduced by the cross-cutting agent and independently by the coordinator on QA R4 School E1 (both reverted). | R3-H4 **PARTIAL** (role/delete closed, identity fields open) | X-R4-01, coordinator §"Coordinator verification" |
| R4-H2 | School / Bookings (money) | **`DELETE /api/school/lessons/<id>/` on a booked class deletes the students' bookings by FK cascade: the deducted credits are never refunded, no cancellation e-mail is sent, nothing remains in the student's history.** `LessonViewSet.perform_destroy` (`catalog/views.py:254-256`) only broadcasts the calendar change and runs the default delete; `Booking.lesson` is `on_delete=CASCADE`. The `classes/<id>/` endpoint the UI uses does it right (`{"cancelled":true,"refunded":1}` + "🚫 Lezione annullata" mail). Live (Student agent, QA Test School; coordinator, QA R4 School E1): book → credits 5 → 4; owner `DELETE /school/lessons/<id>/` → **204**; credits still 4; booking absent from upcoming/past/cancelled; no refund row in the credit history; no e-mail within 25 s; lesson 404. Reachable by any school member with a school token (the viewset has no extra role gate) and by any integration using the `lessons` resource; the calendar drag/drop already uses this viewset for PATCH. | NEW | ST-R4-01, coordinator §"Coordinator verification" |

### Medium

| ID | Panel | Finding | vs R3 | Detail |
|---|---|---|---|---|
| R4-M1 | School (settings) | **`School.timezone` is writable by any school member, including a `staff` with no `settings` permission.** `SchoolProfileView.patch` gates `_SCHOOL_HQ_ONLY_FIELDS` (403) and `_SCHOOL_SETTINGS_FIELDS ∪ _SCHOOL_IDENTITY_FIELDS` (needs `settings`); `timezone` is in neither set. Live: staff `PATCH /school/profile/ {timezone:"Europe/Paris"}` → **200** (owner GET confirms) while the same staff gets 403 on `cancellation_policy_hours`. Every lesson's naive date+time is interpreted in `School.timezone`, so an under-privileged member can silently shift the cancellation window and minimum notice of the whole school — the R2-H14/R3-M11 drift, by the wrong role and invisible. Value validation (R3-M11) is correct. Coordinator re-reproduced. | NEW (adjacent to R3-M11) | X-R4-02 |
| R4-M2 | All (robustness) | **R3-M8 is only partly fixed: a top-level JSON array body, wrong-typed items inside a list, and unparsed path/body ids still return 500** (plain Django page, no traceback). Anonymous: `password-reset-confirm`, `password-reset-validate`, `complete-invite`, `logout`, `google` with `[1,2]` or list-valued fields. Authenticated: `POST /school/team/`, `/school/teachers/`, `/school/classes/`, `/school/packages/`, `/school/locations/`, `/school/document-types/`, `/school/quick-replies/`, `PATCH /school/profile/` with `[1]`; `POST /teacher/attendance/<id>/` and `/school/attendance/<id>/` with `{"attendance":"x"}`, `[1,2]`, `[{"student_id":"x","status_id":"y"}]` (the register's main write endpoint); an over-long invite local part. Scalar bodies, query params and `id: "x"` on team/teacher paths are correctly 400 (the part PR #150 fixed). | R3-M8 **PARTIAL** | X-R4-03, TCH-R4-01, SCH-R4-03, X-R4-04 (over-long) |
| R4-M3 | Commerce | **`POST /api/stripe/checkout/` answers a raw 500 HTML page for an HQ-owned package id, a non-UUID `item_id`, a non-UUID `school_id`, or `kind:"subscription"` with a garbage id** (`CheckoutView.post` filters with unparsed ids; for a `school=null` package `str(item.school_id)` becomes the string `"None"`). Controls: random UUID → 404, `{}` → 400. Consequence: an HQ-owned package (`school=null`) is excluded from the storefront by design **and** the direct checkout crashes, so it still cannot be bought by any student (unreachable in rounds 3 and 4). | NEW (R3-M8 class; product gap) | ST-R4-02, §2a P9 |
| R4-M4 | Teacher | **A teacher can set her own `photo_url` (any external URL, or `javascript:`) and `active` through `PATCH /api/teacher/profile/`** — `TeacherSerializer.Meta.fields` includes both, `read_only_fields = ("id",)`. `photo_url` is rendered as a bare `<img src>` to every student browsing the school's lessons (`student/book/page.tsx`), so the R3-C2 upload allow-list is bypassed by a free-text field (third-party fetch/tracking pixel; `javascript:` inert in `<img>` today but stored). `active:false` self-deactivates the teacher's own row. Found by two agents; reverted. | NEW (sibling of R3-M10) | TCH-R4-02, X-R4-05 |
| R4-M5 | Realtime | **The calendar WebSocket never fires for class create / move / cancel done through the school UI.** `broadcast_calendar_change()` is called only from `LessonViewSet` (`/api/school/lessons/`, `catalog/views.py:249-256`); the UI writes through `POST /school/classes/` and `PATCH|DELETE /school/classes/<id>/` (`SchoolClassCreateView` / `SchoolClassDetailView`, `course_views.py:829,924`), which do not broadcast. Proved with a positive control on the same open sockets (a `PATCH /school/lessons/<id>/` → 1 `calendar_event`; the same edit via `/school/classes/<id>/` → 0). A teacher with the calendar open does not see a move or a cancellation until reload. | NEW | TCH-R4-07 |
| R4-M6 | Teacher / Compensation | **A month recorded as paid keeps its "Pagato / Paid" badge after later attendance raises the total** — the school paid €40, the total became €65 after a late bonus lesson was marked, and the teacher's page shows "€65.00 · Pagato" with the paid amount visible nowhere; the school's summary shows `total 65` and `payment.amount 40` side by side with no outstanding state and no lock on a paid month. Misleading financial status (money not lost by the system). | NEW | TCH-R4-10 |
| R4-M7 | Notifications | **Credits-low e-mail semantics are wrong in two realistic cases.** (a) A **drop-in** purchase auto-books the lesson, the 1-credit package hits 0 and the buyer receives "💳 Your package is running low — 0 lessons left … Renew my package" 30 s after paying €8.50 for a single lesson. (b) The warning is **per package**: a student holding a manual 10-credit grant and an 8-credit subscription got two "sta per finire" mails within 20 s, the first claiming "3 lezioni" while 10.5 credits sat in the other package. The R3-L1 fix itself works (once per package, no blank expiry). | NEW (R3-L1 follow-up) | ST-R4-03, SCH-R4-06 |
| R4-M8 | School (mobile) | **Two more school pages overflow horizontally on 390 px once they hold data**: Course detail (`scrollWidth` 444) and Manual credits (407). PR #148 (R3-M7) fixed Reports and the teacher Compensation table only; both re-verified at 390 px. Coordinator re-measured all four pages. | NEW (R3-M7 class) | SCH-R4-01 |
| R4-M9 | All panels (PhoneInput) | **REGRESSION of the R3-L9 "+39 +39" fix (`9481d5d`): typing a full international number into the national field now ends with the prefix select on a non-existent "+3"/"+4" and the number missing its first digit** ("+39 3331234567" → "+3 93331234567", "+34612345678" → "+3 4612345678"). `emit()` re-splits on every keystroke and `splitPhone()`'s fallback regex accepts a 1-digit prefix, so the second keystroke "+3" is committed as the prefix. Pasting works (the only path the fix was verified with). Every phone field in every panel and on `/register`; the wrong number looks plausible. Found independently by the i18n and HQ agents; confirmed in code by the coordinator. | **REGRESSION** of R3-L9 (HQ-R3-12) | I18N-R4-01, HQ report observation |
| R4-M10 | School / Invites | **R3-M12 is partial: RFC-valid but unroutable addresses (`a@localhost`, `"quoted"@uberip.com`, `a@[127.0.0.1]`) still create ghost User/Teacher rows with `email_sent:true`** on `POST /school/team/` and `/school/teachers/`; obvious junk is now 400. | R3-M12 **PARTIAL** | X-R4-04 |

### Low

| ID | Panel | Finding | vs R3 | Detail |
|---|---|---|---|---|
| R4-L1 | Notifications | `school.new_booking` (built-in copy **and** the stored DB rows, all 5 locales) still hardcodes `📍 {{location_name}} · {{room_name}}` instead of `{{location_line}}` — the exact bare "📍 · " class R3-M14 targeted; the three student-facing keys are fixed and in effect (receipts verified live). | R3-M14 **PARTIAL** | HQ-R4-02 |
| R4-L2 | HQ (i18n) | HQ › Emails renders the raw key `hq.emails.var_package_expiry_line` in the template-variables palette in all 5 locales (key missing from all five message files) with a `MISSING_MESSAGE` console error on every load; the variable itself substitutes correctly. | NEW | HQ-R4-01, I18N-R4-02 |
| R4-L3 | School (i18n) | R3-L13 leftovers still hardcoded English in every locale: school › Team validation "Email and name are required", native `confirm(…)` and "Failed to …" fallbacks; courses selection bar "1 course selected"; course-detail class bulk-edit panel; calendar "Loading..."; compensation placeholders "e.g. …"; Locations room errors verbatim; "Popular" badge; HQ school "Not found."; `title="Custom color"`, `aria-label="Back"`; student book "🌐 Online / Join" chip. | R3-L13 **PARTIAL** | I18N-R4-03/04, SCH-R4-04 |
| R4-L4 | i18n (catalogue) | Key names translated as content and visible live ("Título de Invitación / Inviter un titre / Einladungstitel" as the invite-form heading; de homepage buttons "Speichern-Schaltfläche / Vorschau-Schaltfläche"; "Sala de Etiqueta…"); broken en source strings ("Export C S V", "Added Success", "Method P O S"); mistranslations (it "Ultimo Accesso" for last attendance, de "Ausfallquoten", "Rückerstattungsgrund" for the Refund option, fr "Réservation" button, "Frais de base"); English Title Case carried into it/es. | NEW | I18N-R4-05/06/08/09 |
| R4-L5 | Exports | HQ report CSV file names are Italian in every locale (`hq-report-scuole-…`); student-classes CSV cells `package` / `no_show` and HQ payments `stripe` raw; export tooltips without a singular ("Export 1 students"). Headers, PDF captions and type/status cells are localized (fixed). | R3-L13 **PARTIAL** | I18N-R4-07/11 |
| R4-L6 | All (formatting / a11y / filters) | Money is dot-decimal everywhere except the subscription block ("€185.00" in it/de); `formatDate` dd/mm/yyyy for en too; tap targets < 40 px unchanged; landing hamburger `aria-label="Lessons & Schedule"`; single-select native filters on HQ/school Payments, HQ Reports, school Credits and Students (Carlo's rule: filters multi-select with labels). | STILL OPEN / NEW | I18N-R4-10/12/13/14, SCH-R4-07 |
| R4-L7 | School / Compensation | R3-M6 partial: the plain viewset `PATCH /school/compensation-payments/<id>/` stores `status:"weird2"` and `amount:-3` and leaves `paid_at` null; the summary POST accepts free-text `status` (a settled row un-paid by a typo). API-only, owner/admin. | R3-M6 **PARTIAL** | SCH-R4-02 |
| R4-L8 | School / Invites | `POST /school/team/resend/` and `/school/teachers/resend/` on an already-onboarded member send a fresh "invite" whose `/setup-account` link re-runs `complete-invite` (200 → password reset); the Teachers page offers "Rinvia invito" on every row. | NEW | SCH-R4-05 |
| R4-L9 | Auth | `/setup-account` renders the full form for a spent or invalid link and errors only on submit (R3-L7 added the pre-check to the reset page only). | NEW (sibling of R3-L7) | TCH-R4-05, SCH-R4-09 |
| R4-L10 | Teacher / Uploads | A 9 MB photo dies on an nginx 413 (HTML) below the backend's 8 MB `too_large` branch; the UI shows a generic "Caricamento non riuscito" (hint says 4 MB) and clears the preview although the old photo is kept. | NEW | TCH-R4-03 |
| R4-L11 | Teacher / Attendance | The register page of a **cancelled** lesson says "Non ancora svolta" and offers "Aggiungi un'allieva"; the API refuses both (`lesson_cancelled`); the PASSATE row still shows a live "Registra" button. | NEW | TCH-R4-04 |
| R4-L12 | Validation nits | Empty chat message (`content:""`) accepted (201); concurrent duplicate create on a unique `(school, code)` → uncaught `IntegrityError` 500 (sequential → 400); case-insensitive duplicate attendance status accepted; class `max_capacity: 0` accepted; a file-less student document can be created and school-validated (gate ignores it, but the row stays and is `approved_locked`); some backend English shown verbatim ("Package has purchases; deactivate it instead."). | NEW | TCH-R4-06, X-R4-06, SCH-R4-08 |
| R4-L13 | Teacher / Library (i18n) | `level: "all"` still renders raw ("all · Inglese") on library cards — `LEVEL_KEYS` maps only entry/intermediate/advanced although `levelAll` exists in all five files; compensation header "1 Lezioni / 1 Lessons / 1 Lektionen" (bare noun, no ICU plural). | R3-L2 **PARTIAL** / NEW | TCH-R4-08/09 |
| R4-L14 | Student | Header credit badge stale on the purchase success page (pre-purchase count until the next navigation); closure-day lesson card still advertises "10 posti disponibili · Prenota" (only the modal says the school is closed — R3-L8 fixed the modal); the student cancellation e-mail uses the same hedged sentence for a refunded and a burned cancellation although the server knows `credit_refunded`; "free first lesson" is granted per `free_lesson_used` regardless of prior paid bookings (design question, copy says "first"). | NEW / R3-L8 sibling | ST-R4-04/05/06/07 |
| R4-L15 | School / Profile | `staff` still reads `owner` (User UUID), `ical_token`, `grace_period_days`, `free_trial_ends_at` on `GET /school/profile/` (Stripe/fee fields are correctly hidden). | NEW (minor) | X-R4-07 |
| R4-L16 | Auth (informational) | JWTs minted by register / complete-invite / password-reset-confirm carry only `user_id` (no `role`/`roles` claims) yet are accepted everywhere; only `login` tokens carry role claims. Harmless today (the frontend reads `/auth/me/`), noted for JWT-decoding clients. | NEW | X-R4 §4 note, I18N §8 |
| R4-L17 | Infra / deploy | HSTS on dev still lacks `includeSubDomains` (`strict-transport-security: max-age=63072000` on `/`, `/en`, `/api/health/`); `nginx/edge.conf` has it and PR #193 documents the manual edge-nginx step — not executed yet. | R3-L12 **NOT LIVE** | X-R4 §3, coordinator |
| R4-L18 | Data hygiene | Rows this round could not remove through the product: 3 ghost Users from the RFC-valid-address probe (`a@localhost`, `"quoted"@uberip.com`, `a@[127.0.0.1]`), 3 inactive HQ throwaways (`qa-r4-hq-invite1/2/3@uberip.com`), 2 Stripe test Connect accounts (`acct_1UEZHp3q9VUHwNOx` on School B by the admin-role gate probe, `acct_1UEZQbKWw09Twzen` on E1 by the concurrency probe), 1 live test subscription (`sub_1UEZb63zWjCsyE11gMi7rTNM`, €30/month), ~11 orphan real-image blobs, a coordinator-side lesson deletion and a compensating credit grant on E1. Round-3 residues still present: inactive `qa-r3-hq-invite1/2`, R3 ghost users, R3 Stripe accounts and subscriptions. See Action items. | NEW | all reports §6 |

---

## Round-3 Critical/High findings — live re-verification

| R3 | Finding | Verdict | Live evidence (agent) |
|---|---|---|---|
| R3-C1 | `PUT /api/hq/team/<id>/` bypassed the hierarchy guard | **VERIFIED FIXED** — `team`-only custom role (throwaway attacker) vs owner/super_admin targets: PATCH/PUT/DELETE on e-mail, name, phone, active, sub_role → 403 (`forbidden` / `role_exceeds_caller_permissions` / `only_owner_assigns_owner`); PUT/PATCH self → owner/super_admin/operations → 403; invitation create/PATCH/PUT/approve with an owner-equivalent target or role → 403; unknown sub_role → 400; lateral support/finance invites still 201; owner and super_admin rows byte-identical afterwards. Also from every narrow `qa.hq.*` token (cross-cutting). | HQ, Cross-cutting |
| R3-C2 | Stored XSS through image uploads | **VERIFIED FIXED** — teacher photo and course/package/brand images: `.html` with `<script>`, scripted `.svg`, HTML body named `.png`, PNG-header polyglot → **400 `invalid_type`**; real PNG/JPEG/WebP/GIF → 200 stored under an extension derived from the bytes (a real PNG named `photo.html` → `….png`), served `image/*` + `X-Content-Type-Options: nosniff` + `Content-Security-Policy: default-src 'none'; sandbox`; round-3 blobs (`5c6860bd….html`, `c2cabceb….html`, `66b9d849….svg`) → 404; private documents typed from bytes (a `<script>` `.txt` sent as `text/html` → `text/plain` inline + `nosniff`, `?mime=` ignored; a private key with no file → redirect). Brand-logo `.html` → 400. Coordinator confirmed the purge and the headers. | Teacher, Cross-cutting, HQ |
| R3-H1 | Narrow HQ roles read/download/delete every student document | **VERIFIED FIXED** — `support`, `tech_support`, `finance`, `analytics` → 403 on `GET /api/documents/<id>/`, on `…/file/?path=&token=` and on `DELETE` (own-tenant fixture); owner/super_admin/operations → 200; cross-tenant → 403; traversal/path-guess → 404. | Cross-cutting, HQ (read half) |
| R3-H2 | Stripe Connect onboarding ungated and not idempotent | **VERIFIED FIXED** — staff → 403 `section_forbidden: payments`; teacher/student/narrow HQ → 400 `no_active_school`; anonymous → 401; **two concurrent owner calls → exactly one account** (`acct_1UEZQbKWw09Twzen`, same link both times, sequential call reuses it); bad bodies → clean 400 (no leaked Python error). `admin` is allowed by design (the built-in admin role holds the `payments` section). | Cross-cutting, School |
| R3-H3 | Removed HQ member could never be re-added | **VERIFIED FIXED** — throwaway member: DELETE → 204, login 401 `No active account`; re-invite → approve → 201 `email_sent:true` → **login 200**, `/auth/me/` role hq / sub_role support. `qa.hq.super_admin` (left deactivated by round 3) logs in again. | HQ |
| R3-H4 | Co-owner could remove the founder / silent demotion / irreversible promotion | **PARTIAL** — co-owner DELETE founder → 403 `cannot_remove_founder`; demote founder → 403 `cannot_change_founder_role` (no silent 200); an owner promotion is reversible (founder demotes the co-owner → 200); admin on founder → 403; `godmode` → 400. **But the co-owner can rewrite the founder's login e-mail/phone → R4-H1.** | School, Cross-cutting, coordinator |
| R3-H5 | Stripe webhook never reached dev; blocked-return purchases never fulfilled | **VERIFIED FIXED (reconciliation); webhook delivery still absent** — pack €12 paid 18:36:58Z and shop order €16.99 paid 18:37:14Z with the return navigation aborted; no `verify-session` call; polled every 60 s: nothing at the 18:50 beat (sessions younger than the 30-minute floor, as designed), **both activated by the 19:50 run** — StudentPackage `70d0b0f4…` (5 credits, `purchased_at` 19:50:00.764Z, `pi_3UEZJn3zWjCsyE111YOdVGp9`) with Transaction `aff0e3c7…` €12 / fee €1.20 at 19:50:00.762Z, i.e. 73.4 min after payment, and shop order `b2f976cd…` → `paid` in the same run, 73.1 min after payment; receipts followed (Student report §2b). 0 of the 11 payments this round were webhook-driven; the 9 normal-flow ones were activated by the return page's `verify-session` 14–22 s after Stripe. Declined/abandoned shop orders now expire through the hourly stale-order sweep (36 h) instead of staying `pending` forever. | Student |

Round-3 Medium fixes re-verified live: **fixed** — R3-M1 (no role above your own: self/invite/pending/approve → 403), M2 (all three invite-honesty surfaces: `teachers/resend` `sent:false`, school-detail "Nothing was sent", Team approve amber warning; positive path delivers in 1 s), M3 (Activate/Deactivate refreshes without reload), M4 (slug deep link enrols: `POST /student/school/ {school_id:"qa-test-school"}` → 200, student lands enrolled), M5 (expired required document blocks booking in API and UI, both tenants), M7 (Reports and teacher Compensation scroll inside their container at 390 px in all 5 locales — new overflows elsewhere → R4-M8), M9 (`PATCH /school/courses/<id>/` −1/0/−0.5/1.25 → 400, 1.5 OK; edit page too), M10 (student self-profile `school`/`email`/`user`/`ical_token` ignored; `/auth/me/` ignores role/e-mail/password), M13 (calendar WebSockets refuse support/tech_support/finance/analytics; owner/operations/teacher accepted; forged headers rejected), M15 (language switcher on the four auth pages at both viewports, `uid`/`token`/`next` kept; landing mobile menu with the 5 locales), M16 (inbox "View student profile" opens the student sheet). **Partial** — M6 (summary POST updates in place with `paid_at`; the viewset PATCH and free-text `status` are unvalidated → R4-L7), M8 (→ R4-M2/M3), M11 (value validated; writer ungated → R4-M1), M12 (→ R4-M10), M14 (three student keys fixed and in effect, `school.new_booking` missed → R4-L1). Round-3 Lows: L1 fixed as designed (once per package, expiry clause; semantics → R4-M7), L2 fixed for entry/intermediate/advanced and languages (`all` → R4-L13), L3 documented and observable (empty past lesson pays the base fee; cancelling removes it), L4 (real count, offset pagination, "Mostrate 30 lezioni su 1224" = API), L5 (`active_subscriptions_count` follows recurring packages), L6 (all 8 anonymous student pages show the sign-in card), L7 (used reset link → `/login?error=reset_expired`, no form; `/setup-account` sibling → R4-L9), L8 (closure-day modal offers only "Annulla", logged-in and anonymous; card copy → R4-L14), L9 fixed (negative stats 400, dead `/hq/courses/` gone, "1 teacher" singular, "(copia)" per column, test-send per-locale samples) **except PhoneInput → R4-M9 REGRESSION**, L10 fixed (closure end < start → 400 + UI message, last default status promotes the next, `javascript:` website → 400, seats released), L11 fixed (chat-create gate, empty rows 400, case-insensitive login, simulate bounds, typed chat content), L12 **not live** (→ R4-L17), L13 partial (404 localized with `lang`, Spanish dates, Package/Subscription labels, CSV/PDF headers, browser-locale formatting, catalogue gaps fixed; school-panel strings and export residuals → R4-L3/L5), L14 partly cleaned (blobs purged, super_admin repaired; R3 ghost users and inactive throwaways still present).

Round-2 items re-checked for regression (all hold): C2 (teacher chat isolation at REST and WebSocket incl. forged `X-Panel-Role`/`?as=`), C3 (subscription replay → `already_processed`, credits unchanged, one receipt), C4 (shop `session_id` + verify), H3, H5, H6, H7, H8, H10, H11, H12, H13, H14 (23.4 h burn / 24.5 h refund, modal = API), M2, M5–M10, M12 (409 `lesson_full` + `allow_overbooking_required`, override dialog), M13 (student language drives mail locale), M14, M15 (429 copy, mocked), M16, M18, L10 (password-change UI now exists).

---

## Findings — detail

Full repro steps, request/response evidence and screenshot names are in the panel reports under the IDs quoted above; this section gives the coordinator-level detail for every High item and a compact entry for Medium/Low.

### R4-H1 — Co-owner rewrites the founder's login e-mail (X-R4-01)
- **Where:** `backend/schools/views.py` `SchoolTeamView.patch` — the founder guard (`_is_founder_membership`) is evaluated only inside `if role_changes:` (lines 719-726) and in `delete()`; the e-mail write (lines 731-743) checks only "shared account" and uniqueness, then `user.email = new_email`; `user.save()` at 755. Coordinator confirmed in code.
- **Repro (coordinator, QA R4 School E1, 19:08 UTC, tokens decoded first):** founder (membership 46) `PATCH /api/school/team/ {id:49, school_sub_role:"owner"}` → 200; as the new co-owner: `PATCH /api/school/team/ {id:46, email:"qa-r4-coord-hijack@uberip.com"}` → **200** `{"email":"qa-r4-coord-hijack@uberip.com","school_sub_role":"owner"}`; founder's token `GET /auth/me/` → `email: qa-r4-coord-hijack@uberip.com`; co-owner `PATCH {id:46, school_sub_role:"staff"}` → 403 `cannot_change_founder_role`. Restored: founder e-mail back (200, `/auth/me/` verified), co-owner demoted (200), team `46:owner | 49:staff`. The cross-cutting agent had reproduced the same at ~18:5x UTC including the phone field.
- **Expected:** every write on the founder's membership by a non-founder refused (the R3-C1 pattern: one guard for every verb and field). **Actual:** identity fields rewritable by any owner-grade member.
- **Impact:** within-tenant account takeover of the school founder (e-mail moved → public password reset → login as founder). Requires an owner-grade co-owner, which any co-owner can create.

### R4-H2 — `DELETE /api/school/lessons/<id>/` burns the students' credits (ST-R4-01)
- **Where:** `backend/catalog/views.py:254-256` `LessonViewSet.perform_destroy` (broadcast + default delete); `backend/bookings/models.py:24` `Booking.lesson = ForeignKey(..., on_delete=CASCADE)`; both `lessons` (router, `config/api_school.py:70`) and `classes/<pk>/` (`api_school.py:113`, the refunding `SchoolClassDetailView.delete`) are live. Coordinator confirmed in code.
- **Repro (coordinator, QA R4 School E1, 19:37 UTC):** owner `POST /school/classes/` → lesson `1a5d5cef…` (2026-10-27 11:00); student `POST /bookings/ {lesson_id}` → 201 booking `87f029a6…`, credits 5 → 4; owner `DELETE /api/school/lessons/1a5d5cef…/` → **204**; `GET /student/credits/` → **4**; `GET /student/bookings/?status=upcoming|past|cancelled` → the booking is in none; `GET /student/credit-history/` → no refund row; no "Lezione annullata" e-mail within 25 s; `GET /school/lessons/1a5d5cef…/` → 404. Coordinator granted the credit back through the product (`8b6d8691…`). The Student agent had shown the same on QA Test School (pack stuck at 4 after six bookings; the `classes/` delete of a sibling lesson → `{"cancelled":true,"refunded":1}` + mail 1 s later).
- **Expected:** the `lessons` destroy runs the same cancellation engine as `classes/` (refund per policy, e-mail, counters) or refuses a lesson with bookings (409). **Actual:** row and bookings vanish; credits stay consumed; no trace.

### Medium and Low — compact detail
- **R4-M1:** staff `PATCH /school/profile/ {"timezone":"Europe/Paris"}` → 200, owner GET `Europe/Paris`; control `{"cancellation_policy_hours":48}` → 403 `forbidden`. `schools/views.py:404-422` field sets omit `timezone`. Restored by both agent and coordinator.
- **R4-M2:** full endpoint list in X-R4-03 (`state.json → anon_bodies`, 15 × 500), TCH-R4-01 (attendance shapes; `bookings/attendance_views.py:97-116` `raw.get(...)` on untyped items, `AttendanceStatus.objects.filter(pk="y")` unparsed) and SCH-R4-03; the existing pattern is `core.params.ensure_object_body` / `parse_uuid`.
- **R4-M3:** `commerce/stripe_views.py:62-68` — `parse_uuid` is imported and used only for `transaction_id` (line 453). HQ packages: `GET /student/school-packages/` → 38 packages, 0 with `school=null` (storefront filter by design).
- **R4-M4:** `teachers/serializers.py` `fields = (…, "photo_url", "active")`; `PATCH /teacher/profile/ {"photo_url":"https://evil.example/x.png"}` → 200 read back; `{"active":false}` → 200 (display flag, no access effect). Rendered at `student/book/page.tsx` (`<img src={lesson.teachers.photo_url}>`).
- **R4-M5:** `grep broadcast_calendar_change backend/` → only `catalog/views.py:249,253,256`; `course_views.py` has no realtime import. Probe on open sockets: `/school/classes/` create/patch/delete → 0 events; `/school/lessons/<id>/` patch → 1 `calendar_event` (control).
- **R4-M6:** payment `2eb989b4…` (T1, 2026-09, €40 paid) then a bonus lesson marked → `GET /teacher/compensation-overview/` `{"total":65,"payment":{"amount":40,"status":"paid"}}`; UI "€65.00 · Pagato" (`teacher-r4-ui-compensation-after-bonus-it.png`). Coordinator kept Medium (no money lost by the system; both numbers exposed by the API) — listed under "misleading financial numbers".
- **R4-M7:** P5 (drop-in €8.50, S2 en) → third mail "💳 Your package is running low — 0 lessons left" 30 s after payment; School B s1 with two packages → "3 lezioni rimaste" (18:51:34) then "4 lezioni rimaste" (18:51:44). `bookings/services.py:725` `_dispatch_credits_low` runs in the path used by `commerce/services.py:380 book_paid_lesson`.
- **R4-M8:** coordinator, mobile profile 390×844 as the School B owner: `/it/school/credits` 407, `/it/school/courses/85fb7b9e…` 444, `/it/school/reports` 390, `/it/school/dashboard` 390. Shots `school-mobile-credits-it.png`, `school-mobile-courses_85fb7b9e….png`.
- **R4-M9:** `components/ui/PhoneInput.tsx` — `emit()` (comment "HQ-R3-12") re-splits any value starting with "+" on every keystroke; `splitPhone()` fallback `^(\+\d{1,4})\s*(.*)$` accepts "+3". Shots `i18n-verify2-phone-register-after-type.png`, `i18n-verify-phone-en.png`. HQ agent: typed "+39 06 9999 8888" → stored "+3 90699998888"; paste/`fill` and national-only typing are correct.
- **R4-M10:** `parse_email()` uses Django's `validate_email`, which accepts `a@localhost` etc. → 201 `email_sent:true` on both invite endpoints (ghost rows created and deleted, User rows remain); 300-char local part → 500 (see R4-M2).
- **Lows:** each has the exact string, file and screenshot in its panel report. R4-L1: `backend/notifications/brand_templates.py:234-238` (coordinator confirmed) + `GET /api/hq/email-templates/` `school.new_booking` ×5. R4-L2: `hq/emails/page.tsx:160,178,830` vs `frontend/messages/*.json` (`var_package_expiry` present, `var_package_expiry_line` absent in all five — coordinator confirmed). R4-L8: `schools/views.py:832-852` (no onboarded check) + `accounts/views.py:271-281` (`check_token` → `set_password`). R4-L17: `curl -I` on `/`, `/en`, `/api/health/` at 18:21 UTC.

---

## Coordinator verification

Every High and the most consequential Medium findings were independently re-reproduced by the coordinator before acceptance, with fresh tokens decoded first (`work/coord/*.log` in the session scratchpad):
- **R4-H1** (19:08 UTC, QA R4 School E1): promote staff → owner (200); co-owner PATCH founder e-mail → 200; founder `/auth/me/` shows the hijacked address; co-owner PATCH founder role → 403. Reverted (e-mail, role, team verified).
- **R4-H2** (19:37 UTC, QA R4 School E1): create class → book (5 → 4) → `DELETE /school/lessons/<id>/` 204 → credits 4, booking gone from every tab, no refund row, no e-mail in 25 s, lesson 404. Credit granted back (`8b6d8691…`).
- **R4-M1** (19:07 UTC): staff PATCH timezone → 200 (Europe/Paris), control field → 403. Restored to Europe/Rome.
- **R4-M8** (19:47 UTC): mobile `scrollWidth` 407 / 444 vs 390 on the two pages; 390 on Reports and Dashboard.
- **Code confirmations:** R4-M2 (`attendance_views.py:97-116`), R4-M3 (`stripe_views.py:62-68`), R4-M4 (`teachers/serializers.py`), R4-M5 (`broadcast_calendar_change` callers), R4-M9 (`PhoneInput.tsx` `emit`/`splitPhone`), R4-L1 (`brand_templates.py:234-238`), R4-L2 (message files), R4-L8 (`schools/views.py:832-852`, `accounts/views.py:271-281`).
- **Deployment and R3 residues:** case-insensitive login 200 (`QA.HQ.Support@…`), anonymous malformed scalar body → 400, round-3 blob → 404 with `nosniff` + sandbox CSP, `qa.hq.super_admin` login 200, HSTS without `includeSubDomains` — all at 18:19–18:21 UTC before the agents started.

---

## Methodology, decisions and hazards

- **Tooling.** No browser-automation MCP tool was available; the round-3 Playwright harness (`qa.js`: per-agent persistent Chromium profiles, shared on-disk token cache with `/auth/refresh/` instead of re-logins, `whoAmI` JWT decode, JWT injection into the app's `localStorage` keys, screenshot/text-dump/i18n-scan helpers; `mailtm.js`; `mk_school.js`; shared registration ledger) was reused from the previous session's scratchpad, together with the round-3 agent scripts as read-only reference (the 5,786-request authorization matrix is a re-run of the round-3 runner on the new deployment and tenants).
- **Tenancy.** The coordinator created **QA R4 School B** (School agent), **C** (Teacher agent) and **E1** (cross-cutting agent) through the real HQ flow (`POST /hq/schools/` → `resend-invite` → e-mail `✉️ Invito da QA R4 School X — Titolare` → `/it/setup-account` → `complete-invite` → login as `owner`). QA Test School (Stripe-connected) was reserved for the Student/payments agent; the HQ agent created and deleted its own "QA R4 School A" (cascade verified: admin and teacher login 401, HQ↔A thread 404); round-3 tenants were crawled read-only by the i18n agent and round-3 E1 served as the cross-tenant attacker (untouched afterwards). The three production-like schools were never touched.
- **Coordinator incident (disclosed).** The first tenant-creation loop passed the label and the inbox as one argument (zsh does not word-split an unquoted variable), creating three schools with wrong names and one shared admin `undefined@uberip.com`; they were deleted through the product within a minute (`DELETE /api/hq/schools/<id>/` → 200 ×3, the shared admin no longer logs in). Cost: three extra "new school" e-mails to all 10 HQ members (six in total this round instead of three).
- **Coordinator interventions on the environment.** `enabled.team_invite` was `false` at 18:20 UTC (as round 3 left it); set to `true` at 18:23 UTC for the round and **restored to `false` at 19:45 UTC** (verified). The HQ agent toggled it off for 12 s for the honesty test (restored). `student-shop-visibility` was `false` at the start; the Student agent set it to `true` at 18:36 UTC for the shop tests and **restored it to `false` at 19:50 UTC** (verified 19:52 UTC). Final HQ baseline at 19:45 UTC: 10 team members with the same sub-roles as at 18:20, 0 pending invitations, 14 schools (11 baseline + the 3 round-4 tenants), homepage stats and brand logo restored by the HQ agent.
- **Agent pauses.** Two agents had to wait for shared windows (the 5/hour registration window and the hourly reconciliation beat) and stopped; the coordinator resumed them with a timer and a message. No agent lost work.
- **Shared-IP throttles.** Registration and password reset are 5/hour per client IP, login 10/minute; all agents shared one IP. Budgets were allocated up front (registrations: Student 2, Teacher 1+1, School 1+1, cross-cutting 1, HQ/i18n 0) and logged in a shared ledger: 7 registrations (5 between 18:32 and 18:36 UTC, 2 after 19:33 UTC), 2 password resets, no login 429 thanks to the shared token cache.
- **E-mail.** Gmail connector not usable; mail.tm inboxes (`qa-r4-*@uberip.com`) for every new account; the platform delivered within 0–16 s. ≈ 110 e-mails verified for subject, locale, timing and links (school/HQ/teacher/staff invites in it/en/de, welcome it/en/de/es, password reset fr, receipts for pack/subscription/drop-in/shop, booking confirmed/cancelled to student and school inbox, credits low, no-show with date in it/de, lesson cancelled, account deleted, template test-sends per locale).
- **Django Admin** excluded on every agent's instructions.
- **Severity calls by the coordinator** that differ from an agent: TCH-R4-02/X-R4-05 merged as **Medium** (the teacher agent's call; the cross-cutting agent had Low) because the free-text URL is rendered cross-role; HQ-R4-01 (raw key) kept **Low** (the HQ agent had Medium; the i18n agent Low) — consistent with round-3 severity for raw-key leaks; TCH-R4-10 kept **Medium** although misleading on a financial page (no money lost, both numbers exposed by the API); ST-R4-02 kept as its own Medium (R4-M3) rather than folded into R4-M2 because of the HQ-package consequence; SCH-R4-01 kept Medium (broken pages in one viewport).
- **Not covered this round:** purchase of an HQ-owned package (unreachable — R4-M3); school shop products with commission (QA Test School has none); subscription renewal/grace (needs a billing cycle); Stripe Connect completion for a new school (hCaptcha); the login throttle itself; Google login (not configured on dev); the declined shop order's 36-hour expiry sweep (outside the window); document approve/reject e-mails (none exist in the registry).

---

## Action items (not done by this round)

1. **Fix round** for R4-H1, R4-H2 and the Mediums (see Recommendations); R4-H2 is a money bug reachable by any school token.
2. **Stripe test-mode cleanup:** cancel subscription `sub_1UEZb63zWjCsyE11gMi7rTNM` (S1 `qa-r4-student-s1@uberip.com`, €30/month) plus the round-3 ones still listed in `QA_REGRESSION_ROUND3_SUMMARY.md`; delete Connect test accounts `acct_1UEZHp3q9VUHwNOx` (QA R4 School B, incomplete) and `acct_1UEZQbKWw09Twzen` (QA R4 School E1, incomplete) plus the round-3 orphans.
3. **DB hygiene for Hakan** (no product path): delete ghost Users `a@localhost`, `"quoted"@uberip.com`, `a@[127.0.0.1]` (QA R4 School E1 probe), the inactive HQ throwaways `qa-r4-hq-invite1/2/3@uberip.com`, and the round-3 residues (`qa-r3-hq-invite1/2@`, `not-an-email`, `qa-r3-x-*-off@`).
4. **Register the Stripe webhook endpoint + signing secret for dev** (still 0 webhook deliveries) — no longer blocking money thanks to the reconciliation task, but the 30–90-minute activation delay for a blocked return page remains until it is done.
5. **Redeploy the edge nginx** on EC2 with the current `nginx/edge.conf` (HSTS `includeSubDomains`, R4-L17 / R3-L12).
6. `enabled.team_invite` is back to `false` on dev: invites are silently dropped again (the APIs and UIs now say so — R3-M2 fixed). Decide the dev default.
7. Clean up the round-4 tenants when convenient (below).

---

## Test data created this round (left in place unless noted; all names/e-mails carry `QA R4` / `qa-r4-`; password `QaRound4!2026` unless noted)

**Coordinator**
- Schools via the real HQ flow (language it, Milano, fee 10 %, timezone Europe/Rome, first membership `owner`): **QA R4 School B** `75e2de47-02db-4e36-a2bd-4e18b7a501ca` (`qa-r4-school-b@uberip.com`, user `7694be95…`), **QA R4 School C** `5ba0f084-903c-40f9-8a64-c97c2d9155f4` (`qa-r4-school-c@`, user `dc1f3953…`), **QA R4 School E1** `57cc25d1-2253-4ab0-8386-fd13f8ef6ec2` (`qa-r4-school-e1@`, user `01c771e0…`). Each creation e-mailed all 10 HQ members.
- Mis-created and **deleted** schools `b293b685…`, `517aecf2…`, `d3f2c5b5…` (shared admin `undefined@uberip.com`, cascaded; mail.tm inbox `undefined@uberip.com` exists, unused).
- HQ e-mail setting `enabled.team_invite`: `false` → `true` (18:23 UTC) → `false` (19:45 UTC).
- On E1: R4-H1 probe (founder e-mail/role, reverted), R4-M1 probe (timezone, reverted), R4-H2 probe — lesson `1a5d5cef…` **deleted**, booking `87f029a6…` cascaded, compensating credit grant `8b6d8691…` (+1); mobile screenshots of School B pages (read-only).

**HQ agent** (HQ report §6): "QA R4 School A" `b96b3345-5afa-4db8-93cd-cd72aeed104d` created and **deleted** (admin `qa-r4-hq-schoola@`, teacher `5741f9ea…`, HQ↔A thread cascaded); HQ throwaways `qa-r4-hq-invite1/2/3@uberip.com` invited, onboarded and **removed** (User rows inactive); custom role `qa-r4-team-coordinator` **deleted**; HQ packages, lesson types `QA-R4-LT*`, library "QA R4 HQ Video", product "QA R4 Product", discount `QAR4HQ10` all **deleted**; `team_invite` off 12 s (restored); homepage stats and brand logo restored (orphan brand PNG `940bc06f….png`). 0 registrations.

**School agent** (School report §6): on School B — admin `qa-r4-school-admin@`, staff `qa-r4-school-staff@` (onboarded), teachers T1 `qa-r4-school-t1@` (onboarded; removed by mistake at 19:10 and re-linked 90 s later) and T2 `qa-r4-school-t2@`; location + rooms, closures, plans, statuses, document types, packages incl. recurring `288fdde8…`, discount, courses A/D (D deleted, 5 cancelled orphans), student s1 `qa-r4-school-s1@` (it; grants, bookings, no-show, documents `d6e80ba2…`), student s2 (de) **deleted** (account-deleted test); compensation payment `87c94daf…` (€50 paid); **Stripe Connect account `acct_1UEZHp3q9VUHwNOx`** (admin-role gate probe, incomplete); chat threads; profile back to baseline except `stripe_account_id`. 2 registrations, 0 resets.

**Teacher agent** (Teacher report §6): on School C — teachers T1 `4b61d500…` `qa-r4-teacher-t1@` (onboarded via UI), T2 `qa-r4-teacher-t2@`; students S1 `qa-r4-teacher-s1@` (it), S2 `qa-r4-teacher-s2@` (en); location/room, 4 statuses, plan (threshold left 1), 2 courses, ~14 lessons (several PATCHed into the past, one cancelled), bookings/attendance, compensation payment `2eb989b4…` (€40 paid, month total now €65 — R4-M6), 4 HQ Library fixtures `QA R4 …` (left), ~10 orphan real-image blobs (teacher/course photos), document `57f04e5a…` with 7 test files, T1 staff grants left ON. 2 registrations, 0 resets.

**Student agent** (Student report §6): on QA Test School — S1 `qa-r4-student-s1@` (it, slug deep link), S2 `qa-r4-student-s2@` (en, uuid form; password reset once, restored); packages "QA R4 Credit Pack" (€25/10 cr), "QA R4 Abbonamento Mensile" (€30/month, 8 cr), "QA R4 Reconcile Pack" (€12/5 cr); discount codes `QAR4PCT10`, `QAR4FIX5`; courses/lessons `QA R4 …`; closure 2026-09-26; documents `59e3ae04…` (expired), `12d876d9…` (valid). **Real Stripe test-mode payments (all fulfilled):** P1 €25 `pi_3UEZRG…`, P2 €22.50 `pi_3UEZS4…`, P3 €20 `pi_3UEZSs…`, P4 subscription €30/month **`sub_1UEZb63zWjCsyE11gMi7rTNM`** (active), P5 drop-in €8.50 `pi_3UEZiH…` (auto-booked), P6 shop order `abee1dba…` €16.99 (ShopSale `284ccf0d…`), P7 declined order `b4a6841d…` (pending until the stale sweep), P8 3DS €25 `pi_3UEZYD…`, P10 three unpaid sessions, H5-pkg €12 and H5-shop order `b2f976cd…` €16.99 (blocked return, activated by the reconciliation task). Transactions listed in the Student report §2a. S2 **deleted** at the end through the product's delete-account flow (204 → login 401 → account-deleted e-mail; her transaction kept with `student=NULL`). Settings `free_first_lesson`, `block_booking_on_documents` and HQ `student-shop-visibility` (`false` → `true` → `false`) restored. 2 registrations, 1 reset.

**Cross-cutting agent** (Cross-cutting report §6): full fixture set on E1 (staff membership 49 `qa-r4-x-e1-staff@`, teacher `2e994407…` `qa-r4-x-e1-teacher@`, student `8915dd7f…` `qa-r4-x-e1-student@` (es; password reset once, restored), location/room, document type `qa_r4_x_med` + document `aa70c212…`, statuses, plan, package, discount `QAR4X10`, closure 2026-12-24, course `943a44ee…` + 3 lessons, 4 chat threads, credit grants 5 + 0.5 + 0.5); 61 matrix probe rows created and deleted; 8 ghost invite rows deleted; **Stripe Connect account `acct_1UEZQbKWw09Twzen`** (concurrency probe, single, incomplete); **ghost Users** `a@localhost`, `"quoted"@uberip.com`, `a@[127.0.0.1]`; every setting/e-mail/role probe reverted; attacker tenant round-3 E1 untouched. 1 registration, 1 reset.

**i18n agent:** nothing created; `language_preference` flips restored (API-verified).

**Disposable inboxes** (mail.tm, `@uberip.com`): `qa-r4-school-{b,c,e1,admin,staff,t1,t2,s1,s2}`, `qa-r4-hq-{schoola,teachera,invite1,invite2,invite3,tsend}`, `qa-r4-teacher-{t1,t2,s1,s2}`, `qa-r4-student-{s1,s2}`, `qa-r4-x-e1-{staff,teacher,student}`, `qa-r4-x-hijack`, `qa-r4-coord-hijack`, `undefined`.

---

## Recommendations (fix plan, in order)

1. **Security and money first (R4-H1, R4-H2, R4-M1, R4-M4, R4-M10).** Move the founder guard in `SchoolTeamView.patch` above every field write (mirror `_hq_hierarchy_guard`: any write on the founder's membership by a non-founder → 403, e-mail included). Make `LessonViewSet.perform_destroy` run the same cancellation engine as `SchoolClassDetailView.delete` (refund per policy, e-mail, counters, broadcast) or refuse with 409 when bookings exist — or drop the `lessons` DELETE route if the UI never needs it. Add `timezone` to `_SCHOOL_SETTINGS_FIELDS`. Make `photo_url` and `active` read-only on `TeacherSerializer` (the upload view sets the photo) and validate any URL field with the R3-L10 scheme check. Tighten `parse_email` (require a dotted, resolvable domain; cap the length).
2. **Robustness (R4-M2, R4-M3).** Apply `ensure_object_body` to every list item in `_apply_marks` and to the remaining views that read `request.data.get`; `parse_uuid` on `item_id`/`school_id`/`lesson_id` in `CheckoutView`; answer a clean 4xx (or build the purchase path) for `school=null` packages.
3. **Product correctness (R4-M5, R4-M6, R4-M7, R4-M8, R4-M9).** Call `broadcast_calendar_change()` from the class views and the course wizard; show paid amount + outstanding/partial state on both compensation pages (or reopen the payment when the total changes); skip the credits-low warning for `is_drop_in` packages and compute it on the wallet (or name the package and the remaining wallet); wrap the course-detail lesson list and the credits table in `overflow-x-auto`; in `PhoneInput.emit()` only re-split when the typed prefix matches a known code (or on blur/paste), never on a 1–2-digit fragment.
4. **Polish (Lows).** `school.new_booking` → `{{location_line}}` (+ re-sync the DB rows); add `var_package_expiry_line` to the five message files; the school-panel string leftovers and export file names/enum cells; catalogue clean-up (key-name translations, "Export C S V", Title Case, the listed mistranslations); compensation-payment viewset validation; refuse resend-invite on onboarded members and hide the button; pre-check the `/setup-account` token; 413 handling and copy; cancelled-lesson register page; the validation nits; `level: "all"` label and the compensation plural; student cosmetics; staff profile field exposure; edge-nginx redeploy; data-hygiene cleanup.
