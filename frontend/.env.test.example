# Template for .env.test — copy to .env.test and fill in real values.
# .env.test is git-ignored: NEVER commit real credentials or the service role key.

TEST_HQ_EMAIL=
TEST_HQ_PASSWORD=

TEST_SCHOOL_EMAIL=
TEST_SCHOOL_PASSWORD=

TEST_TEACHER_EMAIL=
TEST_TEACHER_PASSWORD=

TEST_STUDENT_EMAIL=
TEST_STUDENT_PASSWORD=

# Supabase — copy from .env.local (needed by helpers/db.ts and cleanup.mjs)
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
