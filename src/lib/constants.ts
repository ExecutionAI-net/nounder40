/** Default brand color used as fallback for packages, courses, and email templates */
export const BRAND_COLOR = '#6B1F3A'

/** Platform fee percentage applied when school has no explicit override */
export const DEFAULT_PLATFORM_FEE_PERCENT = 10

/** Hours before lesson start within which cancellations burn the credit */
export const DEFAULT_CANCELLATION_HOURS = 24

/** Maximum active packages returned per booking lookup */
export const MAX_ACTIVE_PACKAGES_PER_BOOKING = 20
