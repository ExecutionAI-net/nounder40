# QA Round 4 — Student panel & payments (live regression)

> **Artifacts note (coordinator):** paths written as `$SP/...` refer to the QA session scratchpad (Playwright profiles, scripts, ~2,000 screenshots and text dumps) which is not committed; the key-evidence screenshots named in this report are in [docs/qa/round4-screenshots/](docs/qa/round4-screenshots/). Round-4 environment: dev `develop @ f4155c9`, 2026-09-11.

**Agent:** `student` · **Playwright profile:** `student` (desktop 1366×900) + `student-mobile` (390×844) · **Inbox prefix:** `qa-r4-student-` · **Screenshot prefix:** `student-` (`$SP/shots/`) · **Scripts:** `$SP/work/student/*.js` (numbered 00–15, shared helpers in `lib.js`) · **Raw results:** `$SP/work/student/{purchases,h5,h5-poll,booking-api,api-battery1,api-battery2,pagination-p9,book-ui,filter-closure,cancel-policy,profile-support,noshow-enrol-delete,delete-check,anon-reset,sweep,register-result,inventory}.json`, logs `h5-poll.log`, `sweep.log`
**Tenant:** QA Test School `33807f7b-5c55-43e5-ab5a-e84794d07601` (slug `qa-test-school`) — Stripe Connect complete (`acct_1UCpDD3KWyx5LA5u`, "Hakan Sandbox"), platform fee 10 %, language it, timezone Europe/Rome, cancellation policy 24 h, school min-notice 1 h, `free_first_lesson` false, `block_booking_on_documents` false, `shop_commission_percentage` 0 (flags toggled and restored during the run, see §6)
**Accounts:** S1 `qa-r4-student-s1@uberip.com` / `QaRound4!2026` (it — registered through the **slug** deep link `/it/register?next=/student/book?school=qa-test-school`; User `c894b6ac-7e91-49d1-be5e-e6b77429056e`, Student `fff61a65-475b-4d5d-9e3d-4a3e7d25d975`), S2 `qa-r4-student-s2@uberip.com` (en — `/en/register?next=/student/book?school_id=<uuid>`; User `5866ddbd-ac39-408d-9b61-e98bb2f45ca5`, Student `8647e477-a228-4340-9530-4d30f5b7d5e5`; **password changed to `QaRound4!2027` by the reset test at 19:13Z; account deleted at the end of the run — see §6**), owner `qa.school.owner@qa-nounder40.test` (fixtures, flags, attendance, class delete, transactions/reports reads), HQ owner `qa.hq.owner@qa-nounder40.test` (read-only + `student-shop-visibility` toggle + shop-sales/transactions reads)
**Environment:** https://dev.danzaclassicanounder40.com — `develop` @ `f4155c9` · Stripe test mode (4242…, decline 4000…0002, 3DS 4000 0027 6000 3184) · browser TZ Europe/Rome (CEST, UTC+2) · e-mails via mail.tm
**Time window (UTC):** 2026-09-11 18:31 → 19:53
**Method:** every UI observation cross-checked with an independent `qa.api` call on a token validated with `qa.whoAmI`; all objects created through the product's own API/UI (no admin, no DB); every school/HQ setting captured before and restored after; one `qa.login` per account then cached tokens (0 login 429s); 2 registrations + 1 password reset, all logged in `REGISTRATION_LEDGER.md`.

## 0. Executive summary
- **R3-H5 is closed the way the PR intended:** two purchases paid on Stripe's hosted page with the return navigation blocked (pack €12 at 18:36:58Z, shop order €16.99 at 18:37:14Z) sat unfulfilled through the 18:50 beat run (younger than the 30-minute floor, as designed) and were **activated by the 19:50 `reconcile_stripe_checkout_sessions_task` run without any `verify-session` call** — Transaction `aff0e3c7…` and StudentPackage `70d0b0f4…` created at 19:50:00.76Z (73 min after payment), order `b2f976cd…` `paid` + HQ ShopSale `139dc097…` at 19:50:00.74Z, one receipt and one order e-mail each at 19:50:02Z; a later manual `verify-session` on both sessions → `already_processed` with nothing duplicated (§2b). **No webhook-driven fulfilment was observed at any point** (0 of 11 real payments in this round were credited before the return page's `verify-session` fired, 14–15 s after Stripe; the blocked ones waited for the hourly sweep). Webhook delivery to dev is therefore still absent — it just no longer matters for money.
- **All 11 real Stripe test-mode payments were fulfilled correctly in the app** — one-off ×3 (plain / −10 % / −€5), subscription (€30/month, replay ×3 + reload → `already_processed`, credits unchanged, one receipt — R2-C3 holds), drop-in (auto-booked), shop (order `paid`, HQ ShopSale, order mail), 3DS (challenge completed, pack activated), declined card (order stays `pending`, clear Stripe message, "Pagamento annullato" banner on return), cancel/back (both shop and package), plus the two blocked-return ones. Every paid row has exactly one `Transaction` with the 10 % split and exactly one receipt e-mail with the **corrected copy** ("✨ tutte le tipologie di lezione (10 crediti)", "📅 21-09-2026 · 🕐 18:00 (60 min)", no bare "📍 · ") — R3-M14 is in effect on dev.
- **Round-3 student fixes verified live:** R3-M4 (slug link enrols — `POST /student/school/ {school_id:"qa-test-school"}` → 200, student lands enrolled), R3-M10 (`school`/`email`/`user`/`ical_token` ignored on PATCH), R3-M14, R3-L1 (credits-low once, expiry clause present), R3-L4 (real `count`, offset pagination, UI "Mostrate 30 lezioni su 1224" = API count), R3-L6 (all 8 anonymous pages show the sign-in card, no spinner, 0 API errors), R3-L7 (used link → `/login?error=reset_expired`, no form), R3-L8 (closure-day modal shows only "La scuola è chiusa in questa data · Annulla", logged-in and anonymous), R3-M5 (validated-but-expired document still blocks booking), R3-L5 (`active_subscriptions_count` 4 → 5 after the subscription).
- **New — High (ST-R4-01): `DELETE /api/school/lessons/<id>/` cascades the students' bookings away without refund or notification.** The owner's delete of a booked class through the `lessons` viewset returned 204; the student's booking vanished from every tab, the deducted credit was never returned (pack stayed at 4, credit history shows no refund) and no 🚫 mail was sent. The `classes/` endpoint the UI uses does it right (`{"cancelled":true,"refunded":1}` + mail) — two delete paths, one of them silently burns money.
- **New — Medium (ST-R4-02): `POST /api/stripe/checkout/` answers a raw Django 500 HTML page** for an HQ-owned package id (`school=null` → `School.objects.filter(pk="None")`), for a non-UUID `item_id`, a non-UUID `school_id`, and `kind:"subscription"` with a garbage id — the R3-M8 class of bug, missed on this view. This is also why an HQ package (P9) cannot be bought: it is excluded from the storefront by design and the direct checkout crashes.
- **New — Medium (ST-R4-03): a drop-in purchase sends a "💳 Your package is running low — 0 lessons left" e-mail** right after the auto-booking (the 1-credit drop-in package hits 0 and trips the credits-low warning; the mail even invites her to "renew" a single-lesson product).
- Lows: header credit badge stale on the success page (shows the pre-purchase count until the next navigation, body shows the new total); closure-day card still advertises "10 posti disponibili · Prenota" (the modal is the only place that says the school is closed); the student cancellation e-mail uses the same conditional sentence for a refunded and for a burned cancellation; the "free first lesson" is granted per `SchoolStudent.free_lesson_used` regardless of prior paid bookings (S1 with 8 bookings got one) — design question, not a regression.
- Everything else — registration (slug + uuid deep links, client validation), the whole booking engine (notice, closure, started, multiple, full, documents incl. expiry, no-show, owner cancel with refund, staff-enrol 409/allow_overbooking, cancellation policy in the school timezone), discount codes (percent/fixed/expired/invalid/case-insensitive), subscription card/invoices/PDF/portal, profile edit/language/iCal/documents/delete button, support chat round-trip, M-7, logout, anonymous price-first modal, password reset, and the 80-page 5-locale desktop/mobile sweep — passed (details §5).

## 1. Coverage
| Page / flow | Account | Locales | Viewport | Result |
|---|---|---|---|---|
| `/register` slug deep link (S1) + uuid deep link (S2), client-side bad-email validation | S1 it, S2 en | it, en | desktop | ✅ both land enrolled; welcome mails it/en in < 1 s |
| `/login` forgot-password → `/reset-password` (fresh link form, similar-password refusal, set, auto-login, reused link, garbage link) | S2 | en | desktop | ✅ (R3-L7 fixed) |
| `/student/dashboard` | S1 | en it es fr de | desktop + mobile | ✅ |
| `/student/book` school view (uuid + slug deep link, filter labels + multi-select, list view + "Carica altre" with real total, calendar day, confirm modal with credits, closure-day modal, free-lesson modal S1/S2, drop-in two-option modal S2, anonymous price-first modal) | S1, S2, anonymous | it, en (+5 sweep) | desktop + mobile | ✅ (ST-R4-05, ST-R4-07 cosmetic) |
| `/student/book` network browse (no filter) | anonymous, S1 | it | desktop | ✅ "Mostrate 30 lezioni su 1224" = API `count` (R3-L4) |
| `/student/bookings` (Upcoming/Past/Cancelled, cancel modal refund/burn vs API, no-show badge, drop-in success banner) | S1, S2 | it, en (+5) | desktop + mobile | ✅ |
| `/student/buy` (catalogue, purchase modal + discount code, start-choice modal, subscription card, invoice history + PDF, portal, `?payment=cancelled`) | S1, anonymous | it (+5) | desktop + mobile | ✅ |
| `/student/packages` (`payment=success` return, replay/reload, wallets, subscription badge) | S1 | it (+5) | desktop + mobile | ✅ (ST-R4-04 header badge) |
| `/student/shop`, `/student/shop/<id>`, cart, checkout, "I miei acquisti", success/cancelled returns (HQ flag on) | S1, anonymous | it (+5) | desktop + mobile | ✅ (no product has variants on dev — variant UI not exercisable) |
| `/student/profile` (edit phone via UI, language select, iCal card + feed, documents tab, address tab, delete button) | S1 | it (+5) | desktop + mobile | ✅ |
| `/student/support` (FAQ, chat round-trip with the owner, unread) | S1 + owner | it (+5) | desktop + mobile | ✅ |
| Header language switch (M-7), `/select-role`, logout | S1 | it→es, fr, de, en | desktop | ✅ |
| Anonymous visits to all 8 student pages | anonymous | it | desktop | ✅ (R3-L6 fixed) |
| Stripe Checkout (hosted): one-off ×3, subscription, drop-in, shop ×2 (paid + declined), 3DS, cancel ×2, blocked-return ×2 | S1, S2 | en-US (Stripe) | desktop | ✅ 11 paid |
| Stripe billing portal | S1 | — | desktop | ✅ opens (no cancellation performed) |
| R3-H5 reconciliation (blocked return, 100-minute poll, no verify-session) | S1 | — | API | ✅ see §2b |
| Booking engine API battery (notice, closure, started, multiple, full, documents + expiry, no-show, owner delete ×2 paths, staff enrol 409, free lesson, iCal, credits-low) | S1, S2, owner | — | API | ✅ / ❌ ST-R4-01 |
| Profile mass-assignment, language mirroring, role isolation, anonymous API, pagination walk, checkout robustness | S1, S2, anon | — | API | ✅ / ❌ ST-R4-02 |
| E-mails (welcome ×2, receipts ×7, subscription receipt, drop-in receipt + booking, shop order, booking confirmed ×N, booking cancelled ×2, lesson cancelled by school, no-show, credits-low, password reset, account deleted) | s1 / s2 inboxes | it / en | — | ✅ delivery 1–34 s; copy defects ST-R4-03, ST-R4-06 |
| Locale sweep 8 pages × 5 locales × desktop/mobile (80 loads) | S1 | 5 | both | ✅ 80/80: 0 raw keys, 0 console errors, 0 API errors, 0 horizontal overflow; `spotsLeft` plural correct in 5 locales ("10 posti disponibili / 9 posti disponibili", "10 spots left", "10 lugares disponibles", "10 places restantes", "10 verbleibende Plätze"); h1s localized (Ciao/Hi/Hola/Bonjour/Hallo QA R4 👋, Prenota una Lezione / Book a Lesson / Reserva una Clase / Réserver une Séance / Stunde buchen, …); mobile closure modal identical to desktop |
| Delete account (S2, end of run): two-click armed button → native confirm → `DELETE /api/student/profile/` | S2 | en | desktop | ✅ 204, logged out to `/en`, old token 401, login 401, "🕊️ Your account has been deleted" +2.2 s, transaction row kept with `student: null`, S2 gone from the school roster |

## 2a. Purchase table (Stripe test mode, connected account "Hakan Sandbox"; times UTC; "submit" = click on Stripe's Pay button; fulfilment path = who created the wallet/order row)
| # | Product | Buyer | Type | Amount on Stripe | Checkout session | Stripe object | Fulfilment path | Seconds to activation | Verified in the app |
|---|---|---|---|---|---|---|---|---|---|
| P1 | QA R4 Credit Pack (10 cr, €25) via Buy page | S1 | package | **€25.00** | `cs_test_a1FvnSveOeQ8paZ0sL0VNlFDqF4wRXNXbu4gONGTBeFlXFrM1dWwtqim82` | `pi_3UEZRG3zWjCsyE111jswFgUt` | return page `/it/student/packages?payment=success&session_id=…` → `verify-session` 18:44:56.6Z → `package_activated` (submit 18:44:42.0Z; no webhook) | 14.6 | StudentPackage `f1c03732-f661-4aac-95e2-4099bb271163` 10/10 active, expires 2026-12-10; Transaction `ff40da7d-bcaa-475a-993f-b46d971f2a5e` type package €25 / fee €2.50 / school €22.50 `completed`; page "Pagamento riuscito"; receipt ×1 (+27 s); replay `verify-session` → `already_processed`, credits/`purchased_at` unchanged |
| P2 | QA R4 Credit Pack + `QAR4PCT10` (−10 %) | S1 | package | **€22.50** (modal "Codice QAR4PCT10 applicato: −€2.50 · Totale con lo sconto: €22.50") | `cs_test_a1W3xNVSmiozoHRp1hMnCx35WU3XZKILr5oDky53las52w4rQTouto6tGs` | `pi_3UEZS43zWjCsyE111BRy4II2` | return page → `verify-session` 18:45:46.7Z → `package_activated` (submit 18:45:32.0Z) | 14.7 | StudentPackage `ec81f558-700b-4b04-85d3-73a168f6f533` 10/10; Transaction `7bfc4f7f-e254-49e9-b575-314c6f60812e` €22.50 / €2.25 / €20.25; code `usage_count` 0→1; receipt ×1 "💶 Importo: €22.50"; replay idempotent |
| P3 | QA R4 Credit Pack + `QAR4FIX5` (−€5) | S1 | package | **€20.00** (modal "−€5.00 · Totale €20.00") | `cs_test_a1KQsOz6fdO00yfULdnjYF7h4pYhDJHucQ9y7oLzo4prS3J4JfVx4Lygyx` | `pi_3UEZSs3zWjCsyE110rKAogQJ` | return page → `verify-session` 18:46:36.1Z → `package_activated` (submit 18:46:21.5Z) | 14.5 | StudentPackage `9a1efce6-4d23-4392-a7e5-95dd45728d9c` 10/10; Transaction `05f076c8-6d61-4e67-95da-ed8bb8168d10` €20 / €2 / €18; code usage 1; receipt ×1 "€20.00"; replay idempotent |
| P4 | QA R4 Abbonamento Mensile (8 cr, €30/month) "Iscriviti" → start-choice modal "Da oggi" | S1 | recurring package (Stripe subscription, button "Pay and subscribe") | **€30.00 per month** | `cs_test_a1kECRKiJw0wJXYD0JUc7jEUZLBj0e65NxUA8FrfwbktTtOUBANHkNm6ov` | **subscription `sub_1UEZb63zWjCsyE11gMi7rTNM`**, invoice `in_1UEZb43zWjCsyE11PwhjvSc1` €30 paid | return page → `verify-session` 18:55:07.9Z → `recurring_package_activated` (submit 18:54:48.9Z) | 19.0 | StudentPackage `e910c552-87d1-496b-a25d-d5171e36ff3b` 8/8, `stripe_subscription_id` set, expires 2026-10-11T18:54:50Z, badge "Abbonamento"; **Transaction `86d72eb4-8b3b-4397-83d5-50ec64dfd9a0` type `subscription` €30 / fee €3 / school €27** (`stripe_payment_id` = sub id) in `/school/transactions/`, `/hq/transactions/`, `/school/reports/` (monthly_revenue 404 → 434, fee 40.4 → 43.4, `active_subscriptions_count` 4 → 5); receipt ×1 (+34 s) "✨ tutte le tipologie di lezione (8 crediti) · 💶 Importo: €30.00 · 📅 Valido fino al 11-10-2026". **R2-C3 replay:** booked L_SUB1 (8 → 7) → `verify-session` ×3 (API) + reload of the success URL (page call logged `already_processed`) → all `already_processed`, credits **7**, `credits_total` 8, `purchased_at` **unchanged** (18:55:07.653Z), still exactly 1 Transaction, no extra receipt after 15 s. `GET /api/stripe/invoices/` → **200** (1 invoice paid 3000, `invoice_pdf` present; subscription `active`, `next_payment_at` 1791744890 = 2026-10-11, `next_payment_amount` 3000); Buy page card "Abbonamenti attivi · Gestisci abbonamento · QA R4 Abbonamento Mensile · Mensile · Attivo · Crediti utilizzati 1 / 8 (13%) · Prossimo pagamento 11 ott 2026 · 30,00 € · CRONOLOGIA PAGAMENTI 11 set 2026 30,00 € Pagato Scarica PDF" (PDF link `https://pay.stripe.com/invoice/acct_1THlnm3zWjCsyE11/test_…`); "Gestisci abbonamento" → `POST /stripe/portal/` 200 → `billing.stripe.com` portal "QA R4 Monthly Subscription €30.00 per month · next billing date October 11, 2026 · Visa •••• 4242 · Cancel subscription" (not cancelled) |
| P5 | Drop-in from the booking modal (S2, 0 credits) on lesson 14-09 18:00 "QA Course" (`86ccacb3…`; the resolver picked the cheapest `is_drop_in` package, "QA R3 Drop-in Lesson" €8.50 `2b4801ce…`) | S2 (en) | package (drop-in, `lesson_id` in checkout metadata) | **€8.50** (modal: "Buy just this lesson — €8.50 · Paid and booked straight away / Buy a package and save — With "QA R4 Reconcile Pack" this lesson would cost you €2.40") | `cs_test_a1L99XlDrvgvcWEyJa9mUsz1kFzlNveit1KvYkliIGDSp2OLSMBNV35CCl` | `pi_3UEZiH3zWjCsyE111bwPG1U0` | return `/en/student/bookings?payment=success&session_id=…` → `verify-session` 19:02:31.9Z → `package_activated_booked` (submit 19:02:17.3Z) | 14.6 | Booking `861ee877-c082-456e-afd6-fb0b103294d1` confirmed, `access_source: package`, `credits_deducted 1`; StudentPackage `d2c7bac9-c00f-4847-8e65-d902bd5b3d34` 1→0 `exhausted`; Transaction `38ce30a1-fc9b-4433-81d2-0e617569215b` €8.50 / €0.85 / €7.65; banner "Payment successful! Your lesson is booked — you'll find it below among the upcoming ones."; e-mails (en) "🛍️ Thank you! Your package QA R3 Drop-in Lesson is active" (+29 s, "✨ all lesson types (1 credit) · 💶 Amount: €8.50 · 📅 Valid until 11-10-2026") + "✅ Booking confirmed — QA Course, 14-09-2026" (+29 s, "📅 14-09-2026 · 🕐 18:00 (60 min)") **+ "💳 Your package is running low — 0 lessons left" (ST-R4-03)** |
| P6 | Collezione Libri (HQ product, `school=null`) €12 + shipping €4.99 via Shop UI (card "AGGIUNGI AL CARRELLO" → cart `?cart=1` "Subtotale €12.00 · Spedizione €4.99 · Totale €16.99" → "PROCEDI AL PAGAMENTO") | S1 | shop order `abee1dba-5535-4004-9d22-04a15a16158c` | **€16.99** | `cs_test_b156FuCsqWIRaGuFahAF9W2ADEai15kuLvv6YiRDb61DGYQa3EXQ68X67E` | PI not exposed by the API | return `/it/student/shop?payment=success&order_id=…&session_id=…` → `verify-session` 18:50:35.5Z → `shop_order_activated` (submit 18:50:21.3Z) | 14.3 | order `status: paid` in "I miei acquisti"; banner "Pagamento completato! Ordine ricevuto — riceverai la conferma via email."; **HQ ShopSale `284ccf0d-c55d-41a2-9813-efe199e82ef9`** (qty 1, €12, shipping €4.99, commission 0, source online, stripe, school null); no school `Transaction` (HQ product — expected); order e-mail "🛍️ Ordine confermato #abee1dba — €16.99" (+29 s: "🧾 Ordine #abee1dba del 11-09-2026 · 🛍️ 1× Collezione Libri · 💶 Articoli: €12.00 · 🏷️ Sconto: €0.00 · 📦 Spedizione: €4.99 · 💳 Totale pagato: €16.99"); replay `verify-session` → `already_processed` |
| P7 | Collezione Libri, card `4000 0000 0000 0002` | S1 | shop order `b4a6841d-697e-43bc-bf39-378c7f69be2d` | €16.99 | `cs_test_b1KYi75YR7H1ejbYhQjvItlzgl0nsLm9w7v5X3Z7sGaNtlQtyUOeI0IYas` | declined | — | — | Stripe: "Your credit card was declined. Try paying with a debit card instead." (clear, on the Stripe page); order stays `pending` (no failure webhook reaches dev; the hourly `expire_stale_shop_orders_task` beat row at minute 20 closes it after `STALE_ORDER_HOURS = 36`, i.e. around 2026-09-13 07:20Z — not observable in this run); "Back" → `/it/student/shop?payment=cancelled&order_id=…` → banner "Pagamento annullato. Il carrello è ancora qui quando vuoi." + cart kept |
| P8 | QA R4 Credit Pack with 3DS card `4000 0027 6000 3184` | S1 | package | **€25.00** | `cs_test_a1xj0wQRPgBNc8ZA6tACdFr4Ee7uIuWdSCRbqsduFSW4T5WMW8b6Vi746K` | `pi_3UEZYD3zWjCsyE1102crZt7A` | 3DS challenge iframe (`testmode-acs.stripe.com/3d_secure_2_test/…`) "Complete" clicked 0.2 s after it appeared → return page → `verify-session` 18:52:14.9Z → `package_activated` (submit 18:51:52.9Z) | 21.9 | StudentPackage `3d3572ce-6215-4e59-8839-9b8aee336693` 10/10; Transaction `fb0ab906-8b4b-4704-9724-850266785db2` €25 / €2.50 / €22.50; receipt ×1 |
| P9 | HQ-owned package (`school=null`: "QA R3 HQ Package" `9d950947…` €99, "QA R2 Pacchetto HQ 1" `3f43fd4b…`) | S1 | — | — | — | — | **unreachable** | — | Not listed on the Buy page: `StudentSchoolPackagesView` filters `school__isnull=False` on purpose (no Stripe account to route the money to); `GET /student/school-packages/` without filter → 38 packages, 0 with `school=null`. Direct `POST /stripe/checkout/ {kind:"package", item_id:<HQ id>}` → **500 HTML** (ST-R4-02); with `school_id` → 404 `school_not_found`. So an HQ package still cannot be bought by a student — by design in the storefront, by crash in the API |
| P10 | QA R4 Credit Pack — Stripe "Back" link | S1 | package | €25 | `cs_test_a1XNp5aSBrk6f3OSqfgaWUmTsvhxIH70svIfTyPp6vF8bKxuiSw8Lt0F4X` (+ `cs_test_a1J31Y5a9XOaAVSdXTnIOigqr7NC8KMlwwjhKvTP696A3CUzyE3WwLIYE7`, `cs_test_a1bQiXc3yNTkQf1a2xuLO7atZDDInlf9n32QDlQgFQZXq6YwYYRbq53Ovs` from two earlier attempts where my Back-link locator failed) | unpaid | — | — | Back → `/it/student/buy?payment=cancelled` → banner "Pagamento annullato" |
| H5-pkg | QA R4 Reconcile Pack (5 cr, €12) — **return blocked**, no `verify-session` | S1 | package | **€12.00** | `cs_test_a1m074xsT2HEGx3NbXEtEu3vKG1fvz9zeHXinV9PNZVHPoGSXoUFvp1auR` | `pi_3UEZJn3zWjCsyE111YOdVGp9` | **`reconcile_stripe_checkout_sessions_task`, 19:50 beat run** (paid 18:36:58Z; no webhook) | 4 382 | StudentPackage `70d0b0f4…` 5/5; Transaction `aff0e3c7…` €12 / €1.20 / €10.80; receipt ×1 at 19:50:02Z; later manual replay → `already_processed` (§2b) |
| H5-shop | Collezione Libri €16.99 — **return blocked**, no `verify-session` | S1 | shop order `b2f976cd-368e-41a1-a573-709dd2e6b1e5` | **€16.99** | `cs_test_b1Je16Qa73kBw2AH1k54mmqnSkWGMLXYBYhxX63XH3yAlkRHonJo4E1s90` | PI not exposed by the API | **same 19:50 beat run** (paid 18:37:14Z; no webhook) | 4 366 | order `paid`; HQ ShopSale `139dc097…` (€12 + €4.99); order e-mail ×1 at 19:50:02Z (§2b) |
Unpaid Checkout sessions created (expire in 24 h): the three P10 sessions above, P7 (declined).

## 2b. R3-H5 — blocked-return purchases, webhook vs reconciliation timeline (`03_h5_blocked.js`, `03b_h5_check.js`, poller `04_h5_poll.js` every 60 s, results `h5.json` / `h5-poll.json` / `h5-poll.log`)
Setup: checkout sessions created through the product API (`POST /stripe/checkout/` for the pack, `POST /student/shop/checkout/` for the order), paid on Stripe's hosted page with `page.route(<app origin>, abort)` so the success redirect could never reach the app; Stripe's page re-opened afterwards showed "You're all done here — You've either completed your payment or this checkout session has timed out" (sessions complete). **No `verify-session` was called for either session until after the observation window.** Poller: `GET /api/student/packages/` (S1), `GET /api/school/transactions/` (owner), `GET /api/student/shop/orders/` (S1) and the S1 mail.tm inbox, every 60 s from 18:44:10Z (first poll 7 min after payment; the sub-30-s webhook window was additionally covered by the return-page timing of P1–P8: every activation there was the page's own `verify-session`, never earlier).
| Run | Paid on Stripe (submit, UTC) | Beat runs while pending | Webhook fulfilment (< 30 s) | Activated by | Activation (server `created_at`) | Seconds after payment | Rows created |
|---|---|---|---|---|---|---|---|
| H5-pkg €12 (`cs_test_a1m074…`) | 18:36:58.5Z | 18:50 (session 13 min old → below the 30-min floor, correctly skipped) → 19:50 | **none** — package absent at every one of the 65 polls from 18:44:12Z to 19:49:20Z | `reconcile_stripe_checkout_sessions_task`, 19:50 beat run | **19:50:00.762Z** (Transaction) / 19:50:00.764Z (StudentPackage `purchased_at`); first seen by the poller at 19:50:20.5Z | **4 382 s (73 min 02 s)** | StudentPackage `70d0b0f4-980c-4033-a187-9a54d056076d` 5/5 active, expires 2026-11-10; Transaction `aff0e3c7-a44f-4c1e-bf41-9c96c3625954` type package €12 / fee €1.20 / school €10.80 `completed`, `stripe_payment_id` **`pi_3UEZJn3zWjCsyE111YOdVGp9`**; receipt "🛍️ Grazie! Il tuo pacchetto QA R4 Reconcile Pack è attivo" at 19:50:02Z (**exactly one**; "✨ tutte le tipologie di lezione (5 crediti) · 💶 Importo: €12.00 · 📅 Valido fino al 10-11-2026") |
| H5-shop €16.99 order `b2f976cd-368e-41a1-a573-709dd2e6b1e5` (`cs_test_b1Je16…`) | 18:37:14.1Z | same | **none** — order `pending` at every poll until 19:49:20Z | same 19:50 run | **19:50:00.736Z** (ShopSale `created_at`); order `paid` seen 19:50:20.5Z | **4 366 s (72 min 46 s)** | order `status: paid`; **HQ ShopSale `139dc097-ae73-46d6-af04-349e4040a2b9`** (€12 + shipping €4.99, source online); order e-mail "🛍️ Ordine confermato #b2f976cd — €16.99" at 19:50:02Z (**exactly one**; "🧾 Ordine #b2f976cd del 11-09-2026 · 🛍️ 1× Collezione Libri · 💶 Articoli: €12.00 · 💳 Totale pagato: €16.99") |
After the window (19:51Z) I replayed `GET /api/stripe/verify-session/` on both reconciled sessions **once, manually, for the record**: both answered `payment_status: paid, activation: already_processed`; package rows still 1, credits 5, `purchased_at` unchanged, order still `paid`, no second receipt. **Verdict: R3-H5 VERIFIED FIXED LIVE — the hourly sweep credits a paid session the browser never came back from, idempotently, within one beat cycle after the 30-minute floor (worst case ≈ 90 min, here 73 min). Webhook delivery to dev is still absent: 0 of 11 payments in this round were fulfilled by a webhook; every non-blocked purchase was activated by its return page 14–22 s after Stripe (`package_activated`, never `already_processed` on the first call), and the two blocked ones waited for the sweep.** Declined/abandoned shop orders still rely on the separate stale-order sweep (36 h) rather than on `payment_intent.payment_failed`.

## 2c. Findings by severity

### Critical
_none_

### High
**ST-R4-01 — `DELETE /api/school/lessons/<id>/` on a booked class deletes the students' bookings by FK cascade: the deducted credit is never refunded and no cancellation e-mail is sent.** NEW.
Where: `backend/catalog/views.py:239-257` (`LessonViewSet`, registered as `lessons` in `config/api_school.py:70`; `perform_destroy` only broadcasts the calendar change and calls the default `super().perform_destroy()`), `backend/bookings/models.py:24` (`Booking.lesson = ForeignKey(..., on_delete=CASCADE)`). The safe path is `SchoolClassDetailView.delete` (`DELETE /api/school/classes/<id>/`, the one the UI calls from `school/courses/[id]/page.tsx:203`).
Repro (live, 18:49–18:53Z): S1 booked lesson L_MULTI2 `b2a12d5f-348f-44bf-8181-895929e37140` via `POST /bookings/multiple/` → booking `e1421e22-906e-414e-900b-d8c9ecbe3e14`, pack `f1c03732…` 10 → … → 4 after six bookings. Owner `DELETE /api/school/lessons/b2a12d5f-…/` → **204**. Then: `GET /student/bookings/?status=upcoming|past|cancelled` → the booking is in **none** of them; `GET /school/lessons/b2a12d5f…/` → 404; `GET /student/packages/` → pack still **4** credits; `GET /student/credit-history/` → 5 `deducted` rows and **no refund row**; S1's inbox: no "🚫 Lezione annullata" mail (20 s and 15 min later). Contrast, same run: owner `DELETE /api/school/classes/5741ffa3-…/` (L_MULTI1, also booked by S1) → **200 `{"cancelled":true,"refunded":1}`**, pack 4 → 5, booking `3b8c7cef…` listed under Cancelled as `within_policy` / `credit_refunded: true`, mail "🚫 Lezione annullata — QA R4 Course, 18-09-2026 · Il tuo credito è già tornato nel pacchetto" 1 s later. Evidence: `$SP/work/student/booking-api.json` (`ownerDeleteL_MULTI2`, `pkAfter6`, `pkFinal`, `cancelledAfterDelete: []`), `delete-check.json`, `noshow-enrol-delete.json` (`classesDelete1`, `pkBeforeAfterClassesDelete`, `emails`).
Expected: either the `lessons` destroy runs the same cancellation engine as `classes/` (refund per policy, notification, `current_bookings` maintenance) or it refuses a lesson with bookings (409, like the `confirm_cancel_bookings` handshake of the classes endpoint). Actual: the row and its bookings disappear; the credits stay consumed.
Impact: silent loss of paid credits for every enrolled student, no trace in the student's history, no e-mail; reachable by any school member with a school token (the viewset has no extra role gate beyond the school scope) and by anything that integrates the `lessons` resource (the calendar drag/drop uses the same viewset for PATCH, so a DELETE from that surface is one wiring away). Also inconsistent with R2-M8 ("counters kept in sync on delete") which was verified on the `classes/` path only.
vs R3: NEW (round 3 exercised only `DELETE /school/classes/<id>/`).

### Medium
**ST-R4-02 — `POST /api/stripe/checkout/` returns a raw Django "Server Error (500)" HTML page for malformed or HQ-owned ids.** NEW (R3-M8 class, view not covered by PR #150).
Where: `backend/commerce/stripe_views.py` `CheckoutView.post` — `model.objects.filter(pk=item_id, active=True)` with a non-UUID `item_id`, `School.objects.filter(pk=school_id)` with a non-UUID `school_id`, and `school_id = request.data.get("school_id") or str(item.school_id)` which becomes the string `"None"` for an HQ package (`school=null`) — all three raise `ValidationError` inside the ORM → 500 (`core.params.parse_uuid` is used elsewhere in the same file but not here).
Repro (S1 token, `whoAmI` `c894b6ac…`, 18:57Z, each twice): `{kind:"package", item_id:"9d950947-e938-4a95-8ba1-7d9a7a2b6d16"}` (HQ package) → **500** `<!doctype html>… Server Error (500)`; `item_id:"3f43fd4b-2928-4edf-b9a3-3d3b3079c5c0"` → 500; `item_id:"not-a-uuid"` → 500; `{kind:"package", item_id:<valid>, school_id:"garbage"}` → 500; `{kind:"subscription", item_id:"garbage"}` → 500. Controls: random UUID → 404 `item_not_found`; `{}` → 400 `invalid_kind`; HQ package with `school_id` set → 404 `school_not_found`. Evidence `$SP/work/student/pagination-p9.json`, `api-battery1.json` (`p9_hq_package_checkout`).
Impact: an authenticated student can trigger unhandled 500s (Sentry noise, HTML body to a JSON client) with trivial payloads; and the only path to buy an HQ package crashes instead of answering a clean error — P9 stays unreachable (see §2a).

**ST-R4-03 — A drop-in purchase triggers the "package running low" e-mail ("💳 Your package is running low — 0 lessons left").** NEW.
Where: `bookings/services.py` `_dispatch_credits_low` (R3-L1) runs after `book_paid_lesson` inside `activate_package_payment`; a drop-in package has `credits = credit_cost`, so the auto-booking always leaves 0 and `0 ≤ threshold` fires the warning.
Repro: P5 (§2a) — S2 received, 30 s after paying €8.50 for one lesson, "💳 Your package is running low — 0 lessons left" with body "your package QA R3 Drop-in Lesson with QA Test School has 0 lessons left (it expires on 11-10-2026) … Renew my package" (mail.tm id in `purchases.json` `p5-dropin.emails`), alongside the correct receipt and booking-confirmed mails.
Expected: no low-credit warning for `is_drop_in` packages (or for a package whose balance went to 0 by the purchase's own booking). Actual: a third e-mail that tells a casual buyer her "package" is exhausted and to renew a single-lesson product. Impact: confusing/unwanted mail on every drop-in sale (the exact audience DROP_IN_BOOKING.md wants to keep).

### Low
**ST-R4-04 — Header credit badge is stale on the purchase success page.** NEW. After every pack/subscription return the header showed the pre-purchase total while the page body showed the new one (P1: header "0 Crediti" vs body 10; P4: header "34 Crediti" vs body "CREDITI TOTALI DISPONIBILI 42"; after a reload 42/42 and `GET /student/credits/` 42). The header fetches credits at page load, before `verify-session` activates the package. Screenshots `student-p1-plain-return.png`, `student-p4-sub-return.png`.
**ST-R4-05 — Closure-day lesson card still says "10 posti disponibili · Prenota".** NEW, cosmetic (R3-L8 fixed the modal only): on 26-09 (closure `4b80e444…`) the card for `QA R4 L_CLOSED` reads "… 18:00 – 19:00 · 60 min · 10 posti disponibili · Prenota"; only the modal says "La scuola è chiusa in questa data". `student-book-closed-card.png`.
**ST-R4-06 — Student cancellation e-mail does not say whether the credit came back.** NEW copy. Both the burned (23.4 h, `outside_policy`) and the refunded (24.6 h, `within_policy`) cancellation produced "❌ Prenotazione annullata — QA R4 Course, 12-09-2026" with the same line "Se l'annullamento è avvenuto entro i termini della scuola, il credito è già tornato nel tuo pacchetto." — the server knows `credit_refunded`, the mail hedges. (`cancel-policy.json` `cancelMails`.)
**ST-R4-07 — "Free first lesson" is granted to a student with prior paid bookings.** Observation/design: `bookings/services.py:684` checks `SchoolStudent.free_lesson_used` only; S1, with 8 paid bookings at the school, saw "La tua prima lezione in questa scuola è gratuita" and booked for 0 credits (booking `47a20109…`). Round 3 accepted the same behaviour; flagging it because the copy says "first lesson".

## 3. Round-3 fix re-verification (live)
| R3 item | Commit / PR | Verdict | Evidence |
|---|---|---|---|
| **R3-H5** webhook never reaches dev → hourly reconciliation | `3a33c70` #132 | **VERIFIED FIXED LIVE (reconciliation); webhook delivery still absent** | §2b: both blocked-return purchases activated by the 19:50 beat run, 0 webhook activations in 11 payments |
| **R3-M4** slug deep link never enrols | `9ba5094` #142 | **VERIFIED FIXED LIVE** | S1 `/it/register?next=/student/book?school=qa-test-school` → `POST /api/student/school/ {school_id:"qa-test-school"}` → **200** `{school:{id:33807f7b…}}` (was 500); landed on `/it/student/book?school=qa-test-school` with "Tutte le scuole · 1"; `/student/profile/` `school` = QA Test School; S2 uuid form → 200 too (`register-result.json`) |
| **R3-M10** profile mass-assignment | `a9291ac` #154 | **VERIFIED FIXED LIVE** | S2 `PATCH /student/profile/ {school:<R3 School B>, email:"qa-r4-hijack@…", user:<S1 user>, ical_token:"deadbeef"}` → 200 but every field unchanged (school `33807f7b…`, email `qa-r4-student-s2@…`, ical `298df0be…`); single-field patches of `school` / `email` likewise ignored; legitimate `city/address` patch applied (`api-battery1.json`) |
| **R3-M14** e-mail copy fixes masked by stored templates | `c587e76` #162 | **VERIFIED FIXED LIVE** | receipts P1–P4/P8 "✨ tutte le tipologie di lezione (10 crediti)" / "(8 crediti)", P5 (en) "✨ all lesson types (1 credit)"; booking_confirmed "📅 21-09-2026 · 🕐 18:00 (60 min)" with **no** "📍 · " line; lesson_cancelled_by_school / booking_cancelled / no-show likewise (§4). Corroborated read-only on `GET /api/hq/email-templates/`: the platform rows `student.after_purchase` (×5 locales) now contain `{{package_summary}}` and no `{{lessons_total}}`, `student.booking_confirmed` / `student.booking_cancelled` (×5) contain `{{location_line}}` and no bare `{{location_name}} · {{room_name}}`, `student.credits_low` (×5) contains `{{package_expiry_line}}` — all `updated_at 2026-09-09` (the sync introduced by the fix), i.e. the DB copies no longer mask the built-ins |
| **R3-L4** network browse capped at 500, fake total | `866e74f` #175 | **VERIFIED FIXED LIVE** | API: `{count:1228, next:…offset=500, results:500}`, `limit=30&offset=30` walks on, `limit=100000` capped at 500, last page `offset=1223` → 5 rows, `next:null`; full walk 1228/1228 unique, 0 gaps; UI anonymous "Mostrate 30 lezioni su 1224" (API count 1224 at that moment) → "60 su 1224" after "Carica altre lezioni"; with the school filter "Mostrate 30 lezioni su 33" |
| **R3-L6** anonymous profile/support empty or spinner | `6b0eb4f` #179 | **VERIFIED FIXED LIVE** | anonymous `/it/student/profile` → "Accedi per vedere il tuo profilo — I tuoi dati, i documenti e la lingua sono legati al tuo account. Registrati · Accedi"; `/it/student/support` → "…La conversazione è con la tua scuola: serve un account."; all 8 pages: 0 non-2xx API calls, 0 console errors, no "Caricamento" (`anon-reset.json`, `student-anon-it-*.png`) |
| **R3-L7** used reset link shows the form again | `8c5e7c4` #181 | **VERIFIED FIXED LIVE** | after the reset (S2, 19:13Z) `POST /auth/password-reset-validate/` → 400 `invalid_or_expired_token`; reopening the e-mailed link → **`/en/login?error=reset_expired`**, 0 password inputs, message "This reset link has expired or was already used" (`student-reset-reuse.png`, `student-login-reset-expired.png`); garbage token → same; fresh link → validate 200 → form shown |
| **R3-L8** closure-day modal offers booking | `37971c6` #183 + `6820dcb` #185 | **VERIFIED FIXED LIVE** | `purchase-options` for L_CLOSED → `school_closed: true`; modal (S1, it): "Conferma prenotazione … Scuola QA Test School · **La scuola è chiusa in questa data** · Annulla" — no "Crediti da detrarre", no "Sì, Prenota Ora", no drop-in button; anonymous modal identical; mobile identical (`student-book-closed-modal.png`, `-anon`, `student-mobile-book-closed-modal.png`); `POST /bookings/` → 400 `school_closed` |
| **R3-L1** credits-low blank expiry / repeated | `f7b6479` #169 | **VERIFIED FIXED LIVE** | S1 pack 10 → 5 at the 5th booking → exactly one "💳 Il tuo pacchetto sta per finire — 5 lezioni rimaste" with "ti restano 5 lezioni (scade il 10-12-2026)"; 6th booking (4 left) and the later refund (5) sent nothing (`booking-api.json` `creditsLowCount: 1`). Side effect on drop-ins: ST-R4-03 |
| **R3-M5** expired required document still satisfies the gate | `6be4a14` #144 | **VERIFIED FIXED LIVE (student side)** | doc A `59e3ae04…` uploaded with `expiry_date 2026-01-01` → validated by the owner (`status: valid`, `expires_at 2026-01-01`) → `POST /bookings/` still **400 `documents_required ["QA ID Document"]`**; doc B `12d876d9…` (2027-12-31) validated → 201 |
| **R3-L5** `active_subscriptions_count` 0 | `4f74fd2` #177 | **VERIFIED FIXED LIVE** | `/school/reports/` 4 → 5 after P4 |
| R3-M14-adjacent: `school.new_booking`, reminders | — | not in scope (school/teacher inboxes) | — |
| **R2-C3** subscription replay refills | #88 | **STILL FIXED** | P4 replay ×3 + reload → `already_processed`, credits 7, 1 Transaction, 1 receipt |
| **R2-C4** shop `session_id` + verify | #88 | **STILL FIXED (code); webhook absent → reconciliation covers it** | P6 `…shop?payment=success&order_id=…&session_id=…` → `shop_order_activated`; H5-shop by the sweep (§2b) |
| **R2-H6** documents pending / gate / approved locked | #95 | **STILL FIXED** | no doc → `documents_required`; pending → blocked; rejected → blocked; validated → 201; `DELETE /documents/<valid>/` → 400 `approved_locked` (both docs); file: header 200, `?token=` 200, no token 401, other student's token 403 |
| **R2-H11** subscription Transaction | #88 | **STILL FIXED** | `86d72eb4…` type subscription €30/€3/€27 in school + HQ lists and reports |
| **R2-H12** `/stripe/invoices/` | #88 | **STILL FIXED** | 200, invoice + subscription detail, PDF link, portal opens |
| **R2-H13** free first lesson in the UI | #103 | **STILL FIXED** | S2 (en, 0 credits) "Your first lesson at this school is free — no credits will be deducted · Yes, book now" → 201 `free_lesson`, 0 deducted (`1356e5d1…`); S1 (it) same → `47a20109…`; flag restored false |
| **R2-H14** policy in the school timezone | #88 | **STILL FIXED** | L_EDGE_OUT 23.39 h ahead (Rome): modal "⚠️ La lezione non viene rimborsata. Mancano 23.4 ore …" → API `outside_policy`, `credit_refunded:false`; L_EDGE_IN 24.59 h: "✓ La lezione torna nel tuo pacchetto … più di 24 ore" → `within_policy`, `true`; cards "Non più rimborsabile"/"Rimborsabile", Cancelled tab "Annullata — non rimborsata/rimborsata" |
| **R2-M12** over-capacity 409 | (815098f) | **STILL FIXED** | owner `POST /school/classes/<L_FULL cap 1, full>/students/ {student_id:S2}` → **409 `lesson_full`, `allow_overbooking_required: true`**; with `allow_overbooking:true` → 400 "Student has no valid credits or subscription" (S2 had 0 credits — correct next gate); student side: second student → 400 `full` |
| **R2-M14** shop return pages / order e-mail / pending forever | #88 | **a+b STILL FIXED; c covered by the stale-order sweep, not by the webhook** | P6/P7 banners + order mail; declined order `b4a6841d…` `pending` at 19:10Z (hourly sweep at :20 closes it after the stale window) |
| **R2-M16** 500 lessons in one page | (815098f) → R3-L4 | **VERIFIED FIXED LIVE** | see R3-L4 |

## 4. E-mails verified (mail.tm inboxes `qa-r4-student-s1@uberip.com` (it) / `-s2@uberip.com` (en); from `support@alinaquintana.com`; times UTC; "+Ns" = seconds after the triggering action)
| Trigger | Inbox | Subject | Locale | Delivery | Links / copy |
|---|---|---|---|---|---|
| Registration S1 via slug deep link | s1 | 🩰 Benvenuta in Danza Classica No Under 40! | it ✅ | +0.9 s | `/it/student/profile?for=qa-r4-student-s1%40uberip.com` ✅ |
| Registration S2 via uuid deep link | s2 | 🩰 Welcome to Danza Classica No Under 40! | en ✅ | +0.7 s | `/en/student/profile?for=…` ✅ |
| P1 / P2 / P3 / P8 one-off packs | s1 | 🛍️ Grazie! Il tuo pacchetto QA R4 Credit Pack è attivo | it ✅ | +27 / +28 / +27.5 / +23 s after submit (≈ 12 s after activation) | **once per purchase** ✅; "✨ tutte le tipologie di lezione (10 crediti) · 💶 Importo: €25.00 / €22.50 / €20.00 / €25.00 · 📅 Valido fino al 10-12-2026 · 🩰 Prenota una lezione" ✅ (R3-M14) |
| P4 subscription | s1 | 🛍️ Grazie! Il tuo pacchetto QA R4 Abbonamento Mensile è attivo | it ✅ | +34 s | exactly one, none after the 3 replays + reload ✅; "(8 crediti) · €30.00 · Valido fino al 11-10-2026" |
| P5 drop-in purchase (S2) | s2 | 🛍️ Thank you! Your package QA R3 Drop-in Lesson is active | en ✅ | +29 s | "✨ all lesson types (1 credit) · 💶 Amount: €8.50 · 📅 Valid until 11-10-2026"; link `/en/student/book?school_id=…&for=…` ✅ |
| P5 auto-booking | s2 | ✅ Booking confirmed — QA Course, 14-09-2026 | en ✅ | +29 s | "📅 14-09-2026 · 🕐 18:00 (60 min) · You can cancel without losing the lesson up to 24 hours before"; no "📍 · " ✅ |
| P5 side effect | s2 | 💳 Your package is running low — 0 lessons left | en | +30 s | ❌ ST-R4-03 |
| P6 shop order | s1 | 🛍️ Ordine confermato #abee1dba — €16.99 | it ✅ | +29 s | items / sconto €0.00 / spedizione €4.99 / totale €16.99 ✅ |
| Bookings via API ×6 + UI ×1 + edge ×2 | s1 | ✅ Prenotazione confermata — QA R4 Course, dd-mm-yyyy | it ✅ | +1–3 s | one per booking; "📅 … · 🕐 18:00 (60 min)", policy sentence, no "📍 · " ✅ |
| credits_low (pack crossed 5 lessons) | s1 | 💳 Il tuo pacchetto sta per finire — 5 lezioni rimaste | it ✅ | +1 s | **once** ✅; "ti restano 5 lezioni (scade il 10-12-2026)" ✅ (R3-L1) |
| No-show marked by the owner (L_NOSHOW moved to 10-09) | s1 | 👻 Ci sei mancata — QA R4 Course, 10-09-2026 | it ✅ | +1 s | "la lezione è stata conteggiata dal tuo pacchetto" ✅ |
| Owner deleted class via `classes/` (L_MULTI1) | s1 | 🚫 Lezione annullata — QA R4 Course, 18-09-2026 | it ✅ | +1 s | "📅 18-09-2026 · 🕐 18:00 (60 min) · Il tuo credito è già tornato nel pacchetto" ✅ |
| Owner deleted class via `lessons/` (L_MULTI2) | s1 | **none** | — | — | ❌ ST-R4-01 |
| Booking cancelled by the student (burn 23.4 h / refund 24.6 h) | s1 | ❌ Prenotazione annullata — QA R4 Course, 12-09-2026 ×2 | it ✅ | +1 s each | same hedged sentence in both (ST-R4-06); no "📍 · " ✅ |
| Password reset (S2) | s2 | 🔑 Reset your password | en ✅ | +1.6 s | `/en/reset-password?uid=…&token=…` ✅ (used once, then dead) |
| Account deleted (S2, 19:30:07Z) | s2 | 🕊️ Your account has been deleted | en ✅ | +2.2 s | "your profile, bookings and documents have been removed as requested … 🩰 Start again here" → `/en/register` ✅ |
| H5-pkg activated by the 19:50 sweep | s1 | 🛍️ Grazie! Il tuo pacchetto QA R4 Reconcile Pack è attivo | it ✅ | 19:50:02Z (+1.3 s after activation, 73 min after payment) | once ✅; "✨ tutte le tipologie di lezione (5 crediti) · 💶 Importo: €12.00 · 📅 Valido fino al 10-11-2026" |
| H5-shop activated by the 19:50 sweep | s1 | 🛍️ Ordine confermato #b2f976cd — €16.99 | it ✅ | 19:50:02Z | once ✅; items/totals correct |
Not triggered: package_expiring, we_miss_you, document_expiring (beat tasks), drop_in_booking_failed (race not reproduced), subscription renewal/grace (needs a billing cycle).

## 5. Verified good (passed)
- **Registration**: slug and uuid deep links both enrol the student and land on the school-filtered booking page; `language_preference` = UI locale (it / en); phone stored E.164 "+39 …"; welcome mail < 1 s in the right language; bad e-mail stopped client-side by the native validator (0 `/api/auth/register` calls).
- **Booking page**: every filter has a label (Paese, Città, Scuola, Lingua, Insegnante, Formato, Tipo di lezione), dropdowns are checkbox multi-selects ("Online o in sede · 2 ▾" after ticking both; "Tutte le scuole · 1" from the deep link), "Cancella Filtri"; list view paginates 30 at a time with the real total; confirm modal "Crediti da Detrarre 1 credito · Sì, Prenota Ora" → 201 and header 41 → 40; drop-in two-option modal with the upsell line computed from the cheapest package (€2.40 with the 5-credit €12 pack); anonymous price-first modal ("ti chiederemo di creare un account prima del pagamento") → "Accedi per prenotare" with `/it/login?next=%2Fstudent%2Fbook` and `/it/register?next=…`.
- **Booking engine (API)**: `min_notice`, `school_closed`, `lesson_already_started` (and the started lesson is absent from `/student/lessons/?school_id=`), `bookings/multiple/` per-lesson results (2 ok + `lesson_not_found`; again → `already_booked`), `full` for the second student on a cap-1 lesson and for a duplicate, recurring package consumed first (subscription 8 → 7 before the one-off packs), `/student/credits/` and `/student/credit-history/` consistent (30 − 6 = 24 → 42 after the subscription and 3DS pack), iCal feed 200 `text/calendar` with 5–6 VEVENTs (bogus token 404).
- **Documents gating** end to end (see R2-H6/R3-M5 rows), profile Documents tab "QA ID Document* · Valido · Il documento scade il 31 dic 2027 · qa-r4-doc-ok.pdf · Sostituisci".
- **No-show**: `POST /school/attendance/<lesson>/ {attendance:[{student_id,status_id}]}` → `roster … attendance_status no_show`, Past tab "Non presentato", credit kept, 👻 mail.
- **Discount codes**: `/student/discount-code/check/` `{code, scope:"packages", lines}` → `amount_off 2.5 / total 22.5` (percent), `5 / 20` (fixed), `discount_code_expired` (QAR4EXP), `invalid_discount_code`, case/space-insensitive; purchase modal: valid codes render "Codice QAR4PCT10 applicato: −€2.50 · Rimuovi · Totale con lo sconto: €22.50", an expired one "Questo codice è scaduto.", a wrong one "Codice non valido." (the "Vai al pagamento" button stays usable at full price — the code is simply not applied); Stripe charged exactly the discounted amounts; `usage_count` incremented once per paid session.
- **Subscription surface**: start-choice modal ("Da oggi" / "Valido dal 10/12/2026 (paghi ora)") because S1 already had active packs; Stripe "Pay and subscribe · €30.00 per month"; Buy page card, invoice history, PDF, portal; school reports count and revenue updated; HQ transactions list all five QA R4 rows with the fee.
- **Shop**: catalogue with the HQ flag on (categories, "-26% NEW" badge), product page, cart with shipping, Stripe €16.99, order `paid`, "I miei acquisti" history ("11/09/2026 … Pagato"), HQ ShopSale, order mail, declined and cancelled paths with localized banners and the cart preserved. Anonymous `/student/shop/` list 200 with the flag on.
- **Profile**: phone edited through the UI → "Profilo Aggiornato" toast → API `+39 3331234599` (restored); language select → fr mirrored to `/auth/me/` (restored it); single language field; iCal card `…/api/calendar/student/<token>.ics` → 200; delete-account button present (not fired on S1).
- **Language/M-7**: header 🇮🇹 → Español → `/es/student/dashboard`, cookies `user_locale=es` + `NEXT_LOCALE=es`, `/auth/me/` and `/student/profile/` both `es`, "Hola QA R4 👋"; `/fr/student/book` "Réserver une Séance", `/de/student/buy` "Pakete kaufen", `/en/student/packages` "My Packages" without redirects; API `tr` → 400 "Unsupported locale"; `PATCH /auth/me/ {role:"hq", roles:["hq"], email:…}` → ignored.
- **Support chat**: "Scrivi alla scuola" → message → owner's `/chat/conversations/` shows the `school_student` thread `8112e746-3ea3-43fa-87ee-8fea504bae94` (unread 1) → owner reply 201 → student sees it after reload, `/chat/unread/` total 0.
- **Auth**: `/select-role` sends the single-role student to the dashboard; logout clears both tokens, lands on `/it`, protected pages then show the sign-in card; anonymous student endpoints 401 (GET ×9, POST ×3); S1 on school/HQ endpoints 403; `password-reset-validate` with junk → 400 `invalid_link`.
- **Password reset**: request → mail +1.6 s → fresh link shows the form → "qa-r4-student-s2" refused ("This password is too similar to your name or email") → `QaRound4!2027` → auto-login to `/en/student/book` → old password 401 / new 200 → API reuse 400 → link reopened → login page with the expired message.
- **Sweep**: 80/80 page loads clean (8 pages × en/it/es/fr/de × desktop/mobile): no raw i18n keys or `{var}` leaks, no console errors, no 4xx/5xx API calls, no horizontal scroll on the 390 px viewport; the Bookings "Past" and Shop "I miei acquisti" tabs were opened in every locale; the mobile "Filtri" sheet opens; the header credit badge shows the same number in all five locales (40).
- **Evidence in the other panels**: `/it/school/payments` (owner) lists the QA R4 rows (StudentUno ×4 Credit Pack, Monthly Subscription, StudentTwo drop-in) under "Stripe Connesso · acct_1UCpDD3KWyx5LA5u · TRANSAZIONI 19"; `/it/hq/payments` (HQ owner) lists the same QA R4 rows; `/it/school/reports` renders the updated KPIs (`student-evidence-*.png`). (`/it/hq/transactions` is not a route — 404 page — the HQ list lives at `/hq/payments`.)
- **Delete account** (S2): first click arms the button ("Click again to confirm", 4 s), second click opens the native confirm "Are you sure you want to delete your account? This cannot be undone.", then `DELETE /api/student/profile/` → 204; the app clears both tokens and lands on the public `/en`; the old access token gets 401 on `/auth/me/` and `/student/profile/`; login with the password → 401; the drop-in `Transaction` `38ce30a1…` survives with `student: null`, `student_name: ""` (money trail kept); the school roster no longer lists her; e-mail in her language within 2.2 s.

## 6. Test data created (all on QA Test School unless noted)
**Accounts:** S1 `qa-r4-student-s1@uberip.com` / `QaRound4!2026` (User `c894b6ac…`, Student `fff61a65…`, iCal token `1e84b236-8557-4cb5-8567-abfc7c012b9a`, address "Via QA R4 2"-style values not set on S1; phone restored, language it); S2 `qa-r4-student-s2@uberip.com` (User `5866ddbd…`, Student `8647e477…`, password `QaRound4!2027` after the reset; address "Via QA R4 2 / 20121 / Milano / MI" set by API; **deleted through the profile button at 19:30:07Z — user, profile, 2 bookings, drop-in package and documents gone; transaction `38ce30a1…` kept with `student=NULL`**).
**Packages:** `59822145-fc39-4832-b7aa-37e99fc0e410` QA R4 Credit Pack (€25, 10 cr, 90 d); `fca2d9a7-d3c5-436a-969a-0bad83194241` QA R4 Abbonamento Mensile / Monthly Subscription (€30/month, 8 cr, recurring); `b768f845-2195-4da6-9618-14a3498546b2` QA R4 Lezione Singola / Drop-in Lesson (€8.50, 1 cr, `is_drop_in` — never resolved because the R3 drop-in at the same price wins the tie); `6f6ee895-7d99-4fc9-a605-8b2f5579c12f` QA R4 Reconcile Pack (€12, 5 cr, 60 d). All left active (have purchases).
**Discount codes:** `8f0dffa2-94c1-4499-826a-932e5ad99e7c` QAR4PCT10 (10 %, usage 1); `e87883bb-d578-470f-b3d1-e530cfa35530` QAR4FIX5 (€5, usage 1); `bc2536fb-ebc8-462e-91bf-720f023cf4fc` QAR4EXP (20 %, `expires_at 2026-09-01`).
**Closures:** `847a84c0-941f-4774-bd7c-732844143d63` 2026-09-23; `4b80e444-b199-431b-a774-d61245e36038` 2026-09-26 (created after the lesson, kept).
**Courses:** `66067d06-610a-40af-937a-8be759b1333d` QA R4 Course (QA Lesson Type, QA Teacher, 1 credit, notice 0); `665b17f5-c1ea-4c91-9d79-868ba07e1639` QA R4 Notice Course (notice 4 h). Template lessons 2026-10-22 10:00 `3e72b0d9…`, 2026-10-23 10:00 `45cffc47…`.
**Lessons (notes "QA R4 …"):** L_STARTED `e68d49c1…` (11 Sep 18:00), L_NOTICE `bd02a0f0…` (11 Sep 22:38, notice course), L_DROPIN `39aeb5c0…` (14 Sep — unused: the UI card picker selected the R2 "QA Course" lesson `86ccacb3…` at the same slot for P5), L_FULL `01caf723…` (15 Sep, cap 1, S1 booked), L_DOCS `8bbc83d6…` (16 Sep, S1 booked), L_FREE `2489f7e6…` / L_FREE2 `befe1035…` (17 Sep — unused: the free-lesson bookings landed on round-3's `c55fc348…` / `66da7b8f…` at the same slots), L_MULTI1 `5741ffa3…` (18 Sep — **deleted by the owner via `classes/`**, refunded), L_MULTI2 `b2a12d5f…` (18 Sep 19:30 — **deleted via `lessons/`**, ST-R4-01), L_SUB1 `6bc6b5fa…` (19 Sep, S1 booked from the subscription), L_SUB2 `9d16d346…`, L_NOSHOW `a6dbe3d7…` (moved to 10 Sep, `completed`, S1 no-show), L_SPARE `6a960818…` (21 Sep, S1 booked), L_DROPIN2 `11801a20…` (24 Sep, S1 booked via UI), L_3DS `cf4eaa45…` (25 Sep, unused), L_CLOSED `8fe8bc0e…` (26 Sep, on closure), L_EDGE_OUT `1d8d716d…` (12 Sep 20:28, cancelled/burned), L_EDGE_IN `ccc574da…` (12 Sep 21:40, cancelled/refunded).
**Bookings S1:** `3b8c7cef…` (L_MULTI1 → cancelled by the owner, refunded), `e1421e22…` (L_MULTI2 → **destroyed by cascade**), `5b668211…` (L_FULL), `f4e45db9…` (L_DOCS), `6a63cca6…` (L_NOSHOW → no_show), `f7456b9d…` (L_SPARE), `0e6084d9…` (L_SUB1, subscription), `c3830e6e…` (L_DROPIN2, UI), `47a20109…` (R3 lesson `66da7b8f…`, free lesson), `6160821d…` (L_EDGE_OUT cancelled outside policy), `24af32d4…` (L_EDGE_IN cancelled within policy). S2: `861ee877…` (drop-in on `86ccacb3…`), `1356e5d1…` (R3 lesson `c55fc348…`, free lesson) — both gone with the account deletion.
**Documents (S1):** `59e3ae04-9d8a-4acd-b293-b21ebd04af86` (expired 2026-01-01, validated), `12d876d9-2a32-4e3f-8196-6d92a5e6ec57` (2027-12-31, rejected then validated) — files `documents/c7982c6c-….pdf`, `documents/ef2145cb-….pdf`.
**StudentPackages:** S1 `f1c03732…`, `ec81f558…`, `9a1efce6…`, `3d3572ce…` (Credit Pack ×4), `e910c552…` (subscription `sub_1UEZb63zWjCsyE11gMi7rTNM`), `70d0b0f4…` (Reconcile Pack, created by the 19:50 sweep); S2 `d2c7bac9…` (drop-in, exhausted; deleted with the account).
**Transactions:** `ff40da7d…` €25, `7bfc4f7f…` €22.50, `05f076c8…` €20, `86d72eb4…` €30 subscription, `fb0ab906…` €25 (3DS), `38ce30a1…` €8.50 (drop-in, S2 → `student=NULL` after deletion), `aff0e3c7…` €12 (H5-pkg, by the sweep). **Shop orders:** `abee1dba…` paid (ShopSale `284ccf0d…`), `b4a6841d…` pending (declined), `b2f976cd…` paid by the sweep (ShopSale `139dc097…`).
**Chat:** conversation `8112e746-3ea3-43fa-87ee-8fea504bae94` (S1 ↔ owner, 2 messages).
**Settings touched and restored:** `block_booking_on_documents` false→true→**false** (18:49Z); `free_first_lesson` false→true→**false** (19:01Z); HQ `student-shop-visibility` **false→true** (18:36:51Z) → **restored to false at 19:50:43Z** (`GET` → `{"enabled":false}` verified at 19:52Z); S1 language it→es→it, it→fr→it; S1 phone restored; S2 language en→fr→es→tr(400)→en; S2 password → `QaRound4!2027` (not restored — account deleted).
**HQ `student-shop-visibility` — explicit statement for the coordinator:** baseline captured at 18:36:51Z with `GET /api/hq/student-shop-visibility/` → **`{"enabled": false}`** (`h5.json` `shopVisibilityOriginal`); I set it to `true` at 18:36:51Z for the shop tests (`POST … {enabled:true}` → 200) — that is the `true` value visible during the round (e.g. at 19:45Z); restored with `POST … {enabled:false}` at 19:50:43Z (200) and re-read → `{"enabled": false}` (`h5-final.json` `visibilityRestore` / `visibilityFinal`; re-confirmed by a fresh GET at the time of writing). Net effect on the environment: none.
**Stripe test-mode objects (cleanup list):**
- Subscription **STILL ACTIVE**: `sub_1UEZb63zWjCsyE11gMi7rTNM` (S1, €30/month, next billing 2026-10-11, invoice `in_1UEZb43zWjCsyE11PwhjvSc1`) — cancel from the Stripe dashboard.
- Paid PaymentIntents: `pi_3UEZRG3zWjCsyE111jswFgUt` (P1), `pi_3UEZS43zWjCsyE111BRy4II2` (P2), `pi_3UEZSs3zWjCsyE110rKAogQJ` (P3), `pi_3UEZYD3zWjCsyE1102crZt7A` (P8), `pi_3UEZiH3zWjCsyE111bwPG1U0` (P5), `pi_3UEZJn3zWjCsyE111YOdVGp9` (H5-pkg), P6 and H5-shop PIs (not exposed by the API; sessions `cs_test_b156Fu…`, `cs_test_b1Je16…`); declined PI for P7 (session `cs_test_b1KYi75Y…`).
- Unpaid/abandoned Checkout sessions: `cs_test_a1J31Y5a…`, `cs_test_a1bQiXc3…`, `cs_test_a1XNp5aS…` (P10 ×3), `cs_test_b1KYi75Y…` (P7).

## 7. Screenshots (`$SP/shots/student-*.png`; text dumps `$SP/reports/dumps/student-*.txt`)
Registration: `student-register-it-empty/filled/landed`, `student-register-en-empty/filled/landed`, `student-register-it-bad-email`. Purchases: `student-p1-plain-modal/stripe/return`, `student-p2-pct-*`, `student-p3-fixed-*`, `student-p4-sub-modal/startchoice/stripe/return/replay-reload/buy-page/portal`, `student-p5-dropin-modal/stripe/return`, `student-p6-shop-added/cart/stripe/return/orders`, `student-p7-shop-declined-added/cart/stripe-filled/stripe-error/cancel-return`, `student-p8-3ds-modal/stripe/challenge/return`, `student-p10-package-cancel-return`, `student-h5-package-stripe/blocked/recheck`, `student-h5-shop-stripe/blocked/recheck`, `student-shop-it-list/product`. Booking: `student-book-it-calendar`, `student-book-it-filters-open`, `student-book-filter-uuid-list`, `student-book-filter-slug-list`, `student-book-filter-Tipo_di_lezione-open`, `student-book-filter-Insegnante-open`, `student-book-filter-Formato-open`, `student-book-it-list-end`, `student-book-closed-card`, `student-book-closed-modal`, `student-book-closed-modal-anon`, `student-book-confirm-modal/after`, `student-free-lesson-s1-modal`, `student-free-lesson-s2-modal/after`, `student-bookings-it-list`, `student-cancel-policy-L_EDGE_OUT-modal/after`, `student-cancel-policy-L_EDGE_IN-modal/after`, `student-cancel-policy-cancelled-tab`, `student-bookings-past-tab`, `student-anon-confirm-modal-it`, `student-anon-login-prompt`, `student-anon-it-book-list-nofilter`. Profile/support/auth: `student-dashboard-it`, `student-packages-it`, `student-profile-it`, `student-profile-saved`, `student-profile-address-it`, `student-profile-documents-it`, `student-language-dropdown`, `student-after-language-es`, `student-support-it/sent/reply`, `student-after-logout`, `student-forgot-form/sent`, `student-reset-page/similar/done/reuse`, `student-login-reset-expired`. Anonymous: `student-anon-it-{dashboard,bookings,packages,buy,shop,book,profile,support}`. Evidence: `student-evidence-school-payments`, `student-evidence-school-reports`, `student-evidence-hq-transactions`, `student-evidence-hq-shop-sales`. Sweep: `student-<loc>-_student_<page>[-mobile]` (80), `student-<loc>-book-mobile-filters`, `student-mobile-book-calendar`, `student-mobile-book-closed-modal`. End: `student-delete-account-*`.

## 8. Assumptions / decisions / hazards
- Reconciliation timing: the beat row runs at minute 50 with a 30-minute floor, so I paid the blocked-return sessions at 18:37Z on purpose — skipped by the 18:50 run (13 min old), eligible for the 19:50 run (73 min) — which fits the 100-minute budget without an idle wait. The poller polled every 60 s (finer than the requested 5 min) and never called `verify-session`.
- Both blocked-return payments were confirmed complete by re-opening Stripe's page ("You're all done here"); the aborted redirect itself was not logged by Playwright in this Chromium build (0 aborted navigations recorded), so the "paid" timestamps are the Pay-button clicks (±5 s).
- My card picker on the booking page selects by time slot; three actions therefore landed on other tenants' fixtures at the same slot: P5 auto-booked the round-2 "QA Course" lesson `86ccacb3…` (14-09 18:00) and the two free-lesson bookings used round-3's L_FREE/L_FREE2 (`c55fc348…`, `66da7b8f…`). They are bookings only (no fixture modified); S2's are gone with the account deletion, S1's `47a20109…` remains. Also the drop-in resolver picked round-3's "QA R3 Lezione Singola" (same €8.50 as mine) — the purchase is still a real drop-in.
- The first 10_book_ui run clicked "Cancella Filtri" (my locator matched it) and therefore saw the whole network on the closure day; the re-test with the correct card proved the deep-link filter and R3-L8 — the earlier observation is not a finding.
- `ST-R4-01` uses the `lessons` viewset the school panel does not call for deletes; rated High because the money outcome is silent and the endpoint is a first-class school API resource (the same viewset serves the calendar's PATCH).
- No HQ shop product was created; the 4 existing HQ products have no variants, so the variant selector was not exercisable on dev (noted, not a finding). Shop products with a school commission do not exist on QA Test School (`shop_commission_percentage` 0).
- Not tested: subscription renewal/grace (needs a billing cycle), Google login, PWA, `school_not_connected` (no unconnected school with packages), Stripe Connect onboarding for a new school (hCaptcha), the login throttle.
- Throttles: 2 registrations (18:34Z) and 1 reset (19:13Z) logged in `REGISTRATION_LEDGER.md`; no 429 seen.
- The delete-account button needed a forced click in Playwright before it armed (two plain clicks were swallowed with no state change, no dialog, no request); a real pointer click arms it as designed. Recorded as an automation quirk, not a finding — but worth a manual tap on a phone.
- The HQ transactions page is `/hq/payments` and the HQ shop sales live inside `/hq/shop`; my first evidence attempts at `/it/hq/transactions` and `/it/hq/shop-sales` hit the app's 404 page (screenshots `student-evidence-hq-transactions.png` / `student-evidence-hq-shop-sales.png` show that 404; `student-evidence-hq-payments.png` and `student-evidence-hq-shop.png` the real lists). Those two URLs were my guesses, not links in the product — not a finding.
