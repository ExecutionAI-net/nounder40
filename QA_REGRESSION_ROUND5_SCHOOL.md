# QA R5 — SCHOOL panel (today's commits 24e06b1, 62bdc09, ad749ef, 7f0bd1b)

- Date: 2026-09-13 (18:26–18:45 UTC), base `http://localhost` (local Docker: nginx → Next dev + Django/daphne, celery)
- Accounts used: `qa.school.owner@qa-nounder40.test` (active school = QA Test School A unless stated, switched via `POST /api/school/memberships/`), `qa.student@…` (bookings/credits), `qa.teacher@…` (`GET /api/teacher/lessons/`), `qa.hq.owner@…` (`/api/hq/email-settings/`). Invitees: `qa.hq.finance`, `qa.hq.analytics`, `qa.hq.tech_support`, `qa.hq.support`, `qa.new.member.p8bcqm`, `qa.new.teacher.p8bcqm` (all `@qa-nounder40.test`).
- Scripts: `tooling/school-api-setup.js`, `school-api-purge.js`, `school-ui-format*.js`, `school-ui-purge*.js`, `school-team.js`, `school-cleanup.js`. Full evidence log: `reports/school-evidence.log`. Screenshots in `shots/sch-*.png`, text dumps in `reports/dumps/`.
- E-mail evidence: locally every send ends with `email send failed (key=… to=…): ZEPTO_MAIL_TOKEN not configured` (3 retries), so the celery log only carries `key=`/`to=`. The task **payload** (key, locale, context) was captured read-only with `redis-cli monitor` on the `LPUSH celery` message and base64-decoded (`school-team.js`).

## Findings

| ID | Severity | Title | Repro | Expected | Actual | Evidence |
|---|---|---|---|---|---|---|
| SCH-R5-1 | Medium | Course page never lists cancelled classes, so the new per-class "Elimina definitivamente" button (7f0bd1b) is unreachable there | As school A owner: cancel two classes of "QA Course" (`DELETE /api/school/classes/<id>/` on 2026-09-19 `6e916706…` and 2026-09-20 `75b58abc…`), open `/it/school/courses/1f647597-…`, tab "Tutte" | Cancelled classes listed with the "Annullato" badge and the "Elimina definitivamente" button (commit message: "on cancelled lessons in … the course page") | The page shows 0 "Annullato" badges and 0 delete buttons; the two cancelled classes are simply absent while `GET /api/school/lessons/?course=1f647597…` returns them (`status: cancelled`). Cause: `frontend/src/app/[locale]/school/courses/[id]/page.tsx:184` `.filter(l => l.status !== 'cancelled')` in `loadAll()` (pre-existing, blame 2609f66d 2026-08-19) — the `DeleteLessonButton` added at line 741 sits behind a `cls.status === 'cancelled'` condition that can never be true. Cancel+purge still work from calendar / lessons list / class page. | `sch-purge-course.png` (tab Tutte: only scheduled rows, 6 "Annulla lezione" buttons); log lines `e. course page (tab Tutte) … "Annullato" badges: 0 | delete buttons: 0`, `e. API /school/lessons/?course= returns cancelled rows: ["2026-09-19 6e916706","2026-09-20 75b58abc"]`; `git show 7f0bd1b -- …courses/[id]/page.tsx` |
| SCH-R5-2 | Low | `team_added` e-mail for a school-team invitee is sent in the school's language, not the recipient's saved preference (teacher flow does the opposite) | `/it/school/team` (school A, `School.language = it`): invite existing HQ account `qa.hq.finance@…` (`language_preference = en`) as Staff. Compare with `/it/school/teachers/invite` for `qa.hq.support@…` (also `en`). | An existing account with a saved language gets the notice in her own language (consistent between the two flows; cf. "lingua email = destinataria" rule) | Team flow: task `key=team_added to=qa.hq.finance locale="it" login_url=http://localhost/it/login invite_role="Staff"`. Teacher flow: `key=team_added to=qa.hq.support locale="en" login_url=http://localhost/en/login invite_role="teacher"`. `schools/views.py` `SchoolTeamView.post` uses `_school_invite_locale(body.locale, school)` for existing accounts too; `teachers/views.py` uses `user.language_preference`. | Redis-captured tasks in `school-evidence.log` (`TASK {"key":"team_added","to":"qa.hq.finance@…","locale":"it",…}` vs `…"to":"qa.hq.support@…","locale":"en",…`) |

## Passed checks

**A. Calendar "Formato" filter (24e06b1)**
- Setup: `POST /api/school/classes/` on QA Course 2026-09-14 20:00 `is_online:true, online_link:https://zoom.us/j/1` → 200 `{created:1}` (id `873eeef9…`, feed `is_online:true`); 21:00 in-person → id `6385c394…` `is_online:false`.
- `/it/school/calendar`, week 14–20 Sep: 5 chips unfiltered; "Formato" offers `["📍 In presenza","🌐 Online"]`; selecting Online → 1 chip (the 20:00 · 🌐 one), badge "1" on the button, "Cancella filtri" visible; selecting only "In presenza" → 4 chips, none online; "Cancella filtri" → 5 chips, badge gone, button hidden. Screenshot `sch-format-filter.png`.
- Lesson modal shows row "Formato / 🌐 Online" (`dumps/sch-format-modal.txt` line 116); no raw keys (`scanText` empty).
- Locales: button label + options — en `Format` `["📍 In person","🌐 Online"]`, es `Formato` `["📍 Presencial","🌐 Online"]`, fr `Format` `["📍 En studio","🌐 En ligne"]`, de `Format` `["📍 Vor Ort","🌐 Online"]`, it `Formato`; `filterFormat`/`formatOnline` never appear raw. No console errors.

**B. Cancel → purge (7f0bd1b) — API** (`school-api-purge.js`)
- Setup: lesson L1 `12efe8db…` 2026-09-17 18:00; `POST /api/bookings/ {lesson_id}` as qa.student → 201 `status:confirmed, credits_deducted:1`; `GET /api/student/credits/` A: 10 → 9.
- B1: `DELETE /api/school/classes/12efe8db…/purge/` on scheduled → **409** `{"error":"not_cancelled","hint":"Cancel it first: DELETE /api/school/classes/…/"}`; lesson still in feed, `scheduled`.
- B2: `DELETE /api/school/classes/12efe8db…/` → 200 `{"cancelled":true,"refunded":1}`; `GET …/classes/<id>/` → `status:cancelled, current_bookings:0`; credits A = 10; `GET /api/student/bookings/?status=cancelled` has the booking `status:cancelled, credit_refunded:true, cancellation_type:within_policy`.
- B3: `DELETE …/purge/` → 200 `{"deleted":1}`. Gone from `GET /api/school/lessons-feed/` (false), `GET /api/school/classes/<id>/` → 404, generic `GET /api/school/lessons/?course=` (false), `GET /api/teacher/lessons/?from=2026-09-17&to=2026-09-17` as qa.teacher (200, 0 rows), `GET /api/student/bookings/?status=upcoming|past|cancelled` (0 rows each), `GET /api/student/lessons/` (false). Credits A still 10 after purge.
- B4: L2 `4324a359…` booked (credits 9) → `PATCH …/classes/<id>/ {"status":"cancelled"}` → 200 → `DELETE …/purge/` → **409** `{"error":"has_confirmed_bookings"}`, credits still 9 → `DELETE …/classes/<id>/` → 200 `refunded:1`, credits 10 → purge → 200 `{"deleted":1}`.
- B5 bulk: C1/C2/C3 (QA Course 21/22/23 Sep) + C4/C5 on a second course "QA Course 2 (R5)" (`4d934ba5…`, created via `POST /api/school/courses/`); C1, C2, C4 cancelled; school B lesson BL `0373ea02…` (QA Corso B, 2026-09-21 18:00) cancelled while active on B. `POST /api/school/classes/purge-cancelled/ {}` → 400 `from and to are required`; `{from only}` → 400; `{from:2026-10-01,to:2026-10-07}` → 200 `{"deleted":0}`; `{from:21,to:23,course_id:course2}` → 200 `{"deleted":1}` (only C4 gone, C1/C2 still cancelled, C5 scheduled); `{from:21,to:23}` → 200 `{"deleted":2}` (C1, C2 gone; C3 and C5 survive `scheduled`); teacher feed 21–23: C1/C2 false, C3 true; switching to school B: BL still in B's feed, `cancelled` (tenant isolation).
- B6 cross-tenant: active on A, `DELETE /api/school/classes/0373ea02…(school B)/purge/` → **404** `{"error":"Class not found"}` (GET also 404); as school B → 200 `{"deleted":1}`.

**B7. Purge UI** (`school-ui-purge*.js`, locale it)
- Calendar week 14–20 with 3 cancelled lessons: filter bar shows "Elimina le annullate (3)". Clicking a cancelled (grey, line-through) chip → side panel with "Stato: Annullata dalla scuola" and "Elimina definitivamente" (title "Solo le lezioni annullate si possono eliminare."); 1st click → "Clicca di nuovo per confermare" (`sch-purge-modal.png`); 2nd click → confirm dialog "La lezione annullata sparirà da tutti i calendari (scuola, insegnante, allieve) e non si potrà recuperare. Confermi?" accepted → panel closes, chip gone, button now "Elimina le annullate (2)", API feed no longer has it.
- Bulk: "Elimina le annullate (2)" → armed (`sch-purge-bulk.png`) → dialog "2 lezioni annullate spariranno da tutti i calendari e non si potranno recuperare. Confermi?" → 0 cancelled chips, bulk button removed (count 0), API feed: both gone.
- Scheduled lesson panel has no "Elimina definitivamente" (`dumps/sch-format-modal.txt`: 0 occurrences).
- `/it/school/lessons` (tab Tutte, 10 rows): 7 scheduled rows → 0 delete buttons; 3 cancelled rows → 3 delete buttons, 0 cancel buttons; header "Elimina le annullate (3)". Per-row delete on "Ven 18/09/2026 20:00 … Annullata" (two clicks + dialog) → row gone, header "Elimina le annullate (2)", API P4 gone. `sch-purge-lessons.png`.
- Class detail `/it/school/courses/<A>/classes/6e916706…` (cancelled): "Annullata" badge + "Elimina definitivamente" next to it, edit form hidden; two clicks + dialog → navigates to `/it/school/courses/1f647597…`, lesson gone (API). With `?from=calendar` (P6) → navigates to `/it/school/calendar`, lesson gone. Scheduled class detail (`f9ff35ce…`): 0 delete buttons. `sch-purge-class.png`.
- B8 real-time: with the calendar open, API create → new chip within 13 ms; API cancel → line-through chip + "Elimina le annullate (1)" within 508 ms; API purge → chip and bulk button gone within 506 ms, no reload (`url` unchanged). `sch-purge-realtime-after.png`.

**C. Team notices (62bdc09, ad749ef)** (`school-team.js`)
- C1 `/it/school/team`, new address `qa.new.member.p8bcqm@…` as Staff → API 201 `{"id":"15","existing":false,"email_sent":true}`, banner "Invitato con successo"; celery `email send failed (key=team_invite to=qa.new.member.p8bcqm@…)` (i.e. task ran); payload `key=team_invite locale=it setup_url=http://localhost/it/setup-account?uid=…&token=… invite_org="QA Test School" invite_role="Staff"`. `sch-team-new.png`.
- C2 existing `qa.hq.finance@…` as Staff → API 201 `{"id":"16","existing":true,"email_sent":true}`; banner "QA HQ Finance aveva già un account: aggiunta subito al team, e un'email le dice dove entrare con le sue credenziali abituali." (= `addedExistingEmailed`); celery `… (key=team_added to=qa.hq.finance@…)`; payload `key=team_added locale=it context.login_url=http://localhost/it/login invite_org="QA Test School" invite_role="Staff" user_name="QA HQ Finance"`. `sch-team-existing-emailed.png`.
- C3 `POST /api/hq/email-settings/ {"enabled.team_added":"false"}` as qa.hq.owner → 200 (GET shows `false`); invite existing `qa.hq.analytics@…` → API 201 `{"existing":true,"email_sent":false}`; banner "QA HQ Analytics aveva già un account ed è stata aggiunta al team, ma l'email di avviso non è partita: il modello «Aggiunta al team» è disattivato in HQ → E-mail." (= `addedExistingNoEmail`); celery `18:43:14 email skipped (key=team_added): switched off in HQ > Emails`. Restored with `{"enabled.team_added":"true"}` → 200 (GET `true`). `sch-team-existing-noemail.png`.
- C5 en: `/en/school/team` existing `qa.hq.tech_support@…` → `{"existing":true,"email_sent":true}`, banner "QA HQ Tech Support already had an account: added to the team right away, and an email tells them where to log in with their usual credentials." `sch-team-existing-emailed-en.png`. No raw keys on any page (`scanText` empty).
- C4 `/it/school/teachers/invite` with existing `qa.hq.support@…` → API 201 `{"existing_account":true,"email_sent":true,"teacher_id":"dce4fe4b…"}` → redirected to `/it/school/teachers?added=QA+HQ+Support&emailSent=1&existing=1`, banner "QA HQ Support è stata aggiunta. Aveva già un account: un'email le dice che può accedere con la sua password abituale e troverà il pannello Insegnante." (= `addedExistingEmailed`); celery `… (key=team_added to=qa.hq.support@…)`; payload `locale=en login_url=http://localhost/en/login invite_org="QA Test School" invite_role="teacher"` (recipient's language is en). New address `qa.new.teacher.p8bcqm@…` via `POST /api/school/teachers/ {locale:"it"}` → 201 `{"existing_account":false,"email_sent":true}`; payload `key=team_invite locale=it invite_role="insegnante" invite_org="QA Test School"`. `sch-teachers-existing-emailed.png`.
- C5 teacher-list variants (query params, it + en): `existing=1&emailSent=1` → addedExistingEmailed; `existing=1&emailSent=0` → "…Aveva già un account, ma l'email di avviso non è partita: il modello «Aggiunta al team» è disattivato in HQ → E-mail. Può accedere con la sua password abituale." / en "…the “Added to the team” template is switched off in HQ → Emails. She can sign in with her usual password."; `existing=0&emailSent=1` → "…è stato aggiunto e l'email di invito è stata inviata." / "…has been added and an invitation email has been sent."; `existing=0&emailSent=0` → "…l'email di invito NON è partita (servizio email non configurato)…" / en equivalent. No raw keys.
- Cleanup: `DELETE /api/school/team/ {id, pending}` → 204 for ids 15/16/17/18; `DELETE /api/school/teachers/ {teacher_id}` → 200 `{"deleted":1}` ×2; team back to owner/admin/staff, teachers back to qa.teacher only; HQ users' roles back to `['hq']`, `active_school` None (post_delete signal).

**D. Regression guard (R4-H2)**
- L3 `f61399ee…` booked by qa.student (credits 9) → `DELETE /api/school/lessons/f61399ee…/` → **409** `{"error":"lesson_has_bookings","bookings":1,"confirmed":1,"hint":"Use DELETE /api/school/classes/…/ to cancel it with refunds."}`; lesson still `scheduled`, `current_bookings:1`; then `DELETE …/classes/<id>/` → `refunded:1` (credits 10) → purge → 200.

**Final state** (`school-cleanup.js` + read-only shell): school A feed Sep–Oct = the 3 fixture lessons (`f9ff35ce`, `d2af1514`, `1232b52f`), school B = its 3 fixtures; course "QA Course 2 (R5)" deleted (`DELETE /api/school/courses/4d934ba5…/` → 204); qa.student credits A=10, B=10, 0 bookings; `enabled.team_added` = `true`.

## Not run / limitations
- Teacher calendar UI and student UI after purge: verified via API only (`/api/teacher/lessons/`, `/api/student/bookings/`, `/api/student/lessons/`), not in the browser (outside this agent's panel).
- E-mail context for `team_invite`/`team_added` was verified from the Celery task payload captured in Redis, not from the Celery log (the log has only `key=`/`to=`; `ZEPTO_MAIL_TOKEN` is unset locally so every task ends in `email send failed … not configured` after 3 retries — environment, not a product defect).
- Format-filter screenshots taken in `it` only; en/es/fr/de checked by label/options text.
- `course_id` filter of the bulk purge was tested with a course created through the generic `POST /api/school/courses/` (not the wizard); lessons under it were created with `/api/school/classes/`.

## Notes (not findings)
- `send_team_added_email` queues the Celery task even when the template is off; the worker skips it (`email skipped (key=team_added): switched off`) and the API/UI correctly report `email_sent:false` — same pattern as `team_invite`.
- Team page success text for a *new* address is the generic "Invitato con successo" / "Invited successfully" (the negative case `invitedNoEmail` is explicit); the commit 62bdc09 wording promise ("say when the invite email goes out") is only partly visible here.
- Leftovers that need a DB write to undo (product behaviour, not reverted): `qa.hq.support@…` now has `roles ['hq','teacher']` and a `Teacher` row `dce4fe4b…` (DELETE `/school/teachers/` only unlinks); ghost users `qa.new.member.p8bcqm@…` (no roles) and `qa.new.teacher.p8bcqm@…` (Teacher `6a1dbbd2…`, unusable password) remain. `EmailSetting enabled.team_added` now exists as `'true'` (there was no row before; behaviour identical).
- `qa.hq.invitee.*` users seen in the DB belong to another agent's run (HQ area).
